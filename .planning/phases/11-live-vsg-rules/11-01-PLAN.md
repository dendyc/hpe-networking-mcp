---
phase: 11-live-vsg-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md
autonomous: true
requirements: [RULES-01, RULES-02, RULES-03, RULES-04]
gap_closure: false

must_haves:
  truths:
    - "When AOS8 live mode is announced, Stage 2 explicitly skips paste-parsing for AOS8 data points and proceeds to Stage 3 without waiting for paste"
    - "Stage 3 contains an explicit AOS8 live-mode sub-path that runs BEFORE the universal/AOS6-8/IAP/per-target rule tables"
    - "The live-mode sub-path evaluates RULES-01 (VRRP VIP, REGRESSION, VSG §1654-§1657) from `aos8_get_effective_config(object_name='ap_sys_prof')` Batch 1 data"
    - "The live-mode sub-path evaluates RULES-02 (ARM/dot11a/dot11g/reg-domain profiles, DRIFT, VSG §1163-§1166) from Batch 1 effective-config results"
    - "The live-mode sub-path evaluates RULES-04 (static AP IP, REGRESSION, VSG §1232-§1234) from `aos8_get_ap_database()` Batch 2 `ip_mode` field"
    - "RULES-03 is marked deferred to Stage 4 A11 in Stage 3, and Stage 4 A11 is expanded to perform the AOS8-vs-ClearPass local-user count cross-check using Batch 3 data already in context"
    - "Each new live-mode finding cites a source tool call and Batch number using the format '(source: `tool_name(args)`, Batch N)'"
    - "`tests/unit/test_skill_tool_references.py` continues to pass — every new tool reference resolves to the live catalog"
  artifacts:
    - path: "hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md"
      provides: "Stage 2 AOS8 skip clause + Stage 3 AOS8 live-mode sub-path + Stage 4 A11 cross-check expansion"
      contains: "AOS8 live-mode sub-path — rules evaluated from Stage 1 data"
  key_links:
    - from: "Stage 2 (skip clause)"
      to: "Stage 3 (live-mode sub-path)"
      via: "Conditional: 'If AOS8 live mode (Stage -1 announced API mode)... proceed directly to Stage 3'"
      pattern: "skip paste parsing for AOS8 data points"
    - from: "Stage 3 live-mode sub-path"
      to: "Stage 1 Batch 1/2/3 collected data"
      via: "Per-rule prose names exact field + tool call + batch number"
      pattern: "source: `aos8_(get_effective_config|get_ap_database|show_command)"
    - from: "Stage 3 RULES-03 placeholder"
      to: "Stage 4 A11 cross-check"
      via: "Stage 3 says 'pending Stage 4 A11'; A11 description expanded to consume AOS8 local-user count from Batch 3"
      pattern: "pending Stage 4 A11"
---

<objective>
Layer the AOS8 live-mode rule evaluation prose into `aos-migration-readiness.md` so Stage 3's four AOS8-anchored rules (RULES-01, RULES-02, RULES-04 + RULES-03 deferred to Stage 4 A11) are evaluated directly from Stage 1 batch data — no operator paste required when AOS8 live mode is active.

Purpose: Phase 10 wired Stage -1 detection + Stage 1 four-batch live collection. Phase 11 closes the loop in Stage 3 so the data already in context actually drives REGRESSION/DRIFT findings, with VSG anchors and source tool citations preserved. Markdown-only — no Python code, no new tools, no frontmatter changes.

Output: Three localized edits to one file:
1. Stage 2 skip clause for AOS8 live mode (above the AOS8 extraction table)
2. Stage 3 AOS8 live-mode sub-path block (immediately after the Stage 3 header, before Universal rules)
3. Stage 4 A11 expansion (cross-check using AOS8 Batch 3 local-user count)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-live-vsg-rules/11-CONTEXT.md
@.planning/phases/11-live-vsg-rules/11-RESEARCH.md
@.planning/phases/11-live-vsg-rules/11-VALIDATION.md
@hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md
@hpe-networking-mcp/CLAUDE.md

<interfaces>
<!-- Existing skill landmarks the executor must locate and edit around. -->
<!-- These are the anchor points already in aos-migration-readiness.md. -->

Stage 1 live-mode sub-path header (mirror this style exactly):
```markdown
##### AOS8 live-mode sub-path — used when Stage -1 announced "AOS8 API mode"
No CLI paste required. Calls AOS8 tools directly in **four grouped batches** (COLLECT-01..04). On any tool error, fall back per-batch to an exact-CLI paste request for that batch only.
```

Stage 2 anchor (insert skip clause IMMEDIATELY before this header at line ~183):
```markdown
#### AOS 8 — what to extract from each artifact
```

Stage 3 anchor (insert live-mode sub-path IMMEDIATELY after this header / its intro paragraph at line ~222-225, BEFORE `#### Universal rules`):
```markdown
### Stage 3 — Apply VSG-anchored readiness rules

Different rule sets apply based on **source platform** AND **target SSID mode**. The audit walks the rules below and classifies each finding as **REGRESSION** (must fix before migration), **DRIFT** (should address; not blocking), or **INFO** (operator reference).

#### Universal rules (apply to all source → target combinations)
```

Stage 4 A11 row (line ~328) — current text:
```
| A11 | ClearPass local-user count (cross-reference with `show local-user db` output) — if both sides have local users, that's a sign of dual-source-of-truth that should be consolidated | `clearpass_get_local_users()` | **DRIFT** |
```

Tools referenced (ALL already in skill `tools:` frontmatter from Phase 10 — do not add):
- `aos8_get_effective_config` — Batch 1 source for `ap_sys_prof`, `arm_profile`, `dot11a_radio_prof`, `dot11g_radio_prof`, `reg_domain_profile`
- `aos8_get_ap_database` — Batch 2 source for per-AP `ip_mode`
- `aos8_show_command` — Batch 3 source for `show local-user db` count
- `clearpass_get_local_users` — Stage 4 A11 cross-check

VSG anchors (must match existing rule rows verbatim):
- RULES-01 → VSG §1654-§1657 (paste-mode equivalent: rule C2 at line 247)
- RULES-02 → VSG §1163-§1166 (paste-mode equivalent: rule C4 at line 249)
- RULES-04 → VSG §1232-§1234 (paste-mode equivalent: rule U2 at line 231)

Skill-tool-reference regex (from Phase 10): `_TOOL_REF_PATTERN` matches `(mist|central|greenlake|clearpass|apstra|axis|aos8)_<word>(...)`. Any typo in a new prose tool call fails CI.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Stage 2 AOS8 live-mode skip clause</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md</files>

  <read_first>
    - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md (full file — locate Stage 2 boundary at `### Stage 2 — Parse the pasted bundle` near line 179 and the `#### AOS 8 — what to extract from each artifact` header near line 183)
    - .planning/phases/11-live-vsg-rules/11-CONTEXT.md (D-03 — exact phrasing requirement; Pitfall 4 in research warns against over-broad wording)
    - .planning/phases/11-live-vsg-rules/11-RESEARCH.md (Architecture Patterns → Pattern 1; Pitfall 4)
  </read_first>

  <action>
    Insert a skip clause IMMEDIATELY BEFORE the `#### AOS 8 — what to extract from each artifact` header (which is currently around line 183 of the file).

    The inserted block MUST contain exactly this text (one blank line above and below the new block):

    ```markdown
    ##### AOS8 live-mode sub-path — used when Stage -1 announced "AOS8 API mode"

    If AOS8 live mode is active (Stage -1 announced API mode), Stage 1 already collected every AOS8 data point in the table below via the four-batch live-mode sub-path. **Skip paste parsing for AOS8 data points and proceed directly to Stage 3.** The AOS8 extraction table below remains authoritative for the paste-fallback path (used when AOS8 is unreachable, or for AOS6 / IAP source platforms which always use paste).
    ```

    Constraints:
    - Use D-03's exact narrowing phrase: "skip paste parsing **for AOS8 data points**" (NOT "skip Stage 2 entirely") — this preserves Stage 2 for AOS6/IAP source paths per Pitfall 4 in research.
    - Use the `#####` heading level (matches Stage 1 sub-path heading depth exactly — see Pattern 1 in research).
    - Do NOT delete, reorder, or modify any row of the existing AOS8 extraction table.
    - Do NOT add or remove anything from `#### AOS 6 — extra extraction items (vs AOS 8)` or the IAP extraction table that follow.
    - The `#### AOS 8 — what to extract from each artifact` header must remain unchanged on the line immediately following the inserted block.
  </action>

  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -x</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '##### AOS8 live-mode sub-path — used when Stage -1 announced "AOS8 API mode"' hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 2 (one is the existing Stage 1 header, one is the new Stage 2 header)
    - `grep -c "skip paste parsing for AOS8 data points and proceed directly to Stage 3" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1
    - `grep -n "#### AOS 8 — what to extract from each artifact" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` shows the header still exists, preceded (within 10 lines above) by the new skip clause
    - File still contains the unchanged AOS6 line: `grep -c "AOS 6 — extra extraction items" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1
    - `cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -x` exits 0
  </acceptance_criteria>

  <done>
    Stage 2 contains an explicit AOS8 live-mode skip clause that narrows to "AOS8 data points only" (preserving paste handling for AOS6/IAP). The existing AOS8 extraction table is byte-identical to its prior contents. Skill regression test passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert Stage 3 AOS8 live-mode sub-path block</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md</files>

  <read_first>
    - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md (Stage 3 header at line ~222 and `#### Universal rules` at line ~226 — locate exact insertion point; also Stage 1 live-mode block at lines 95-120 to mirror tone/format; rules C2 line 247, C4 line 249, U2 line 231 to confirm VSG anchors)
    - .planning/phases/11-live-vsg-rules/11-CONTEXT.md (D-01, D-02, D-04, D-05 — locked decisions; specifics block dictates exact field-level guidance)
    - .planning/phases/11-live-vsg-rules/11-RESEARCH.md (Architecture Patterns → Patterns 1/2/3; Pitfalls 1/3/5)
    - .planning/REQUIREMENTS.md (RULES-01, RULES-02, RULES-03, RULES-04 acceptance text)
  </read_first>

  <action>
    Insert a new Stage 3 sub-section IMMEDIATELY AFTER the Stage 3 intro paragraph (the "Different rule sets apply..." sentence near line 224) and IMMEDIATELY BEFORE `#### Universal rules (apply to all source → target combinations)` (near line 226).

    The inserted block MUST contain exactly the following content (preserve markdown formatting, preserve the backticks around tool names, preserve the VSG section numbers, preserve the en-dashes in finding format):

    ```markdown
    #### AOS8 live-mode sub-path — rules evaluated from Stage 1 data (used when Stage -1 announced "AOS8 API mode")

    When AOS8 live mode is active, RULES-01, RULES-02, and RULES-04 are evaluated directly from the Stage 1 batch data already in context — no re-fetching, no operator paste. RULES-03 result is **pending Stage 4 A11** (ClearPass cross-check). After this sub-path, continue with the universal + AOS6/8 + per-target-mode rule tables below — those apply regardless of source path.

    Each finding below uses the format **Severity — Description (VSG §anchor) (source: `tool_call(args)`, Batch N)**.

    ##### RULES-01 — VRRP VIP for AP system profiles (REGRESSION, VSG §1654-§1657)

    From the Batch 1 `aos8_get_effective_config(object_name='ap_sys_prof', config_path='/md')` response, inspect the LMS IP field (and Backup-LMS IP, if present) of each `ap_sys_prof` returned. If the LMS IP matches a controller management IP (any individual managed-device address from `aos8_get_md_hierarchy()` Batch 1) rather than the VRRP virtual IP, emit:

    - **REGRESSION** — AP system profile `<profile_name>` LMS IP is `<lms_ip_value>`, which matches an individual controller management IP rather than the VRRP virtual IP. APs will strand on first controller upgrade. (VSG §1654-§1657) (source: `aos8_get_effective_config(object_name='ap_sys_prof', config_path='/md')`, Batch 1)

    Reference the field by intent ("the LMS IP field in the `ap_sys_prof` response") — do **not** pin a specific JSON key path; introspect the response structure at runtime. If multiple `ap_sys_prof` objects return, emit one finding per profile with the wrong IP.

    *If Batch 1 failed:* RULES-01 cannot be evaluated from live data. Mark as **inconclusive — paste required** and consult any per-batch fallback paste of `show running-config` already supplied. Equivalent paste-mode rule is C2 below.

    ##### RULES-02 — ARM / radio / regulatory-domain profile detection (DRIFT, VSG §1163-§1166)

    From the Batch 1 effective-config results for `arm_profile`, `dot11a_radio_prof`, `dot11g_radio_prof`, and `reg_domain_profile` (each fetched via `aos8_get_effective_config(object_name=<profile_type>, config_path='/md')`), detect presence of any configured object across all four profile types. Empty/absent at every queried path means no DRIFT for that profile type — distinguish "envelope present" from "objects present".

    If any of the four profile types has at least one configured object at the queried `/md` root scope, emit:

    - **DRIFT** — Active legacy RF profiles detected at `/md` root: `<list of profile_type=profile_name pairs>`. These are replaced by Central AirMatch + ClientMatch in AOS 10 (channel, TX power, channel-width, DFS, band-steering all move to Central). (VSG §1163-§1166) (source: `aos8_get_effective_config(object_name='arm_profile|dot11a_radio_prof|dot11g_radio_prof|reg_domain_profile', config_path='/md')`, Batch 1)

    Per-AP-group enumeration is out of scope for this phase — Batch 1 collects at the `/md` root and AOS8 effective config inherits down. Report at the granularity Stage 1 collected.

    *If Batch 1 failed:* RULES-02 cannot be evaluated from live data. Mark as **inconclusive — paste required** and consult any pasted `show configuration effective detail` output. Equivalent paste-mode rule is C4 below.

    ##### RULES-03 — Local user count cross-check (DRIFT) — pending Stage 4 A11

    Stage 1 Batch 3 already collected the AOS8 local-user count via `aos8_show_command(command='show local-user db')`. The cross-check against ClearPass executes in **Stage 4 A11** (`clearpass_get_local_users()`) — do **not** call ClearPass twice. Stage 3 emits no RULES-03 finding here; the determination is deferred. See Stage 4 A11 for the dual-source-of-truth comparison.

    *If Batch 3 failed:* The AOS8 local-user count is unavailable; A11 will note "AOS8 count unknown — paste of `show local-user db` required for full cross-check".

    ##### RULES-04 — Static AP IP detection (REGRESSION, VSG §1232-§1234)

    From the Batch 2 `aos8_get_ap_database()` response, inspect the `ip_mode` field of each AP entry. For every AP whose `ip_mode` is anything other than DHCP (e.g., `static`), emit one finding:

    - **REGRESSION** — AP `<ap_name>` (MAC `<wired_mac>`) has `ip_mode='<value>'` (statically addressed). AOS 10 does not support static AP IPs — APs must be DHCP-addressed before AP convert. (VSG §1232-§1234) (source: `aos8_get_ap_database()`, Batch 2)

    If no APs have a non-DHCP `ip_mode`, emit a single INFO confirmation: "AP database shows all APs DHCP-addressed (RULES-04 PASS)."

    *If Batch 2 failed:* RULES-04 cannot be evaluated from live data. Mark as **inconclusive — paste required** and consult any pasted `show ap database long` output. Equivalent paste-mode rule is U2 (Universal rules table) below.

    ```

    Constraints:
    - Insert ABOVE `#### Universal rules` (D-02 — live-mode runs first, before any rule tables); do NOT insert below the Universal rules block (Pitfall 3).
    - Use heading depth `####` for the section header and `#####` for each per-rule sub-header so nesting reads correctly under `### Stage 3`.
    - Do NOT modify or remove rules C2 (line 247), C4 (line 249), U2 (line 231); they remain authoritative for paste-fallback.
    - Do NOT call `clearpass_get_local_users()` in this block (D-04: no duplicate call). Defer RULES-03 with the explicit "pending Stage 4 A11" wording.
    - Tool calls in prose MUST use the exact catalog names: `aos8_get_effective_config`, `aos8_get_ap_database`, `aos8_show_command`, `aos8_get_md_hierarchy`. Typos fail `test_skill_tool_references.py`.
    - Reference LMS IP / `ip_mode` BY INTENT or by the canonical AOS8 field name (`ip_mode` is documented in Phase 10 prose — keep it). Do NOT invent a specific JSON path for LMS IP (Pitfall 1) — research Open Question 1 confirms the JSON key is unverified without a live box.
    - Each finding MUST include the parenthetical `(source: \`tool_name(args)\`, Batch N)` citation.
    - Each finding MUST include its VSG anchor in `(VSG §####-§####)` form, matching the existing rule rows.
  </action>

  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -x</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "#### AOS8 live-mode sub-path — rules evaluated from Stage 1 data" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1
    - `grep -c "##### RULES-01 — VRRP VIP for AP system profiles" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1
    - `grep -c "##### RULES-02 — ARM / radio / regulatory-domain profile detection" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1
    - `grep -c "##### RULES-03 — Local user count cross-check" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1
    - `grep -c "##### RULES-04 — Static AP IP detection" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1
    - `grep -c "VSG §1654-§1657" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` >= 2 (existing C2 row + new RULES-01)
    - `grep -c "VSG §1163-§1166" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` >= 2 (existing C4 row + new RULES-02)
    - `grep -c "VSG §1232-§1234" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` >= 2 (existing U2 row + new RULES-04)
    - `grep -c "pending Stage 4 A11" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` >= 1
    - `grep -c "aos8_get_effective_config" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` >= 3 (existing Phase 10 references + new RULES-01, RULES-02 references)
    - `grep -c "aos8_get_ap_database" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` >= 2 (existing Phase 10 references + new RULES-04 reference)
    - `grep -c "aos8_show_command" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` >= 2 (existing + new RULES-03 deferred-note reference)
    - The new live-mode sub-path appears BEFORE `#### Universal rules`: `awk '/#### AOS8 live-mode sub-path — rules evaluated from Stage 1 data/{a=NR} /#### Universal rules \(apply to all source/{print (a>0 && a<NR) ? "OK" : "FAIL"}' hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` prints `OK`
    - Existing C2/C4/U2 rule rows are unchanged: `grep -c "C2 | \*\*AP discovery configured properly" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1; `grep -c "C4 | \*\*ARM Profiles" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1; `grep -c "U2 | DHCP available for AP IP assignment" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1
    - No call to `clearpass_get_local_users` inside the new live-mode sub-path block (verify by reading lines between `#### AOS8 live-mode sub-path — rules evaluated` and `#### Universal rules` — must contain ZERO occurrences of `clearpass_get_local_users`)
    - `cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -x` exits 0
  </acceptance_criteria>

  <done>
    Stage 3 has a new AOS8 live-mode sub-path positioned before the Universal rules table. RULES-01, RULES-02, RULES-04 are each evaluated from named Stage 1 batch data with explicit field-level guidance, VSG anchors, and tool-call citations. RULES-03 is explicitly deferred to Stage 4 A11 with no duplicate ClearPass call. Existing C2/C4/U2 rule rows are byte-identical to before. Skill regression test passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Expand Stage 4 A11 to perform AOS8-vs-ClearPass local-user cross-check</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md</files>

  <read_first>
    - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md (Stage 4 table around line 312 and the A11 row at line ~328 — confirm exact current text before editing)
    - .planning/phases/11-live-vsg-rules/11-CONTEXT.md (D-04 and §specifics — A11 expansion requirement)
    - .planning/phases/11-live-vsg-rules/11-RESEARCH.md (Recommended Edit Plan — EDIT 3)
  </read_first>

  <action>
    Replace the existing A11 row in the Stage 4 table.

    Current row (find this exact text in the file):
    ```
    | A11 | ClearPass local-user count (cross-reference with `show local-user db` output) — if both sides have local users, that's a sign of dual-source-of-truth that should be consolidated | `clearpass_get_local_users()` | **DRIFT** |
    ```

    Replace it with this exact text (single table row — keep the leading and trailing pipes; do not break the table by inserting newlines inside the row):
    ```
    | A11 | **RULES-03 cross-check** — Compare ClearPass local-user count against the AOS8 local-user count already collected in Stage 1 Batch 3 via `aos8_show_command(command='show local-user db')`. If both counts are non-zero, emit: **DRIFT** — AOS8 local users: `<X>`. ClearPass local users: `<Y>`. Dual-source-of-truth — consolidate to a single auth backend (AOS 10 has no Internal Auth Server per VSG §1134-§1136). (source: `aos8_show_command(command='show local-user db')` Batch 3 + `clearpass_get_local_users()`). If AOS8 Batch 3 was unavailable in this session, note "AOS8 count unknown — paste of `show local-user db` required for full cross-check" alongside the ClearPass count. | `clearpass_get_local_users()` | **DRIFT** if both non-zero |
    ```

    Constraints:
    - The row MUST stay on a SINGLE LINE (no embedded newlines) so the markdown table parses correctly. Long, but single-line.
    - Do NOT modify any other row of the Stage 4 table (A1..A10, A12, A13).
    - The Tool column MUST still contain the literal `clearpass_get_local_users()` token (the regression test extracts tool names from this column).
    - Reference `aos8_show_command` exactly as written — typos fail the regression test.
    - Do NOT add a new Stage 4 row — A11 is being expanded in place.
    - Do NOT alter Stage 5 / Stage 6 / Decision Matrix / Output Formatting sections.
  </action>

  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -x</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "RULES-03 cross-check" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1
    - `grep -c "Dual-source-of-truth — consolidate to a single auth backend" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1
    - `grep -c "| A11 |" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1 (exactly one A11 row, not duplicated)
    - The new A11 row references both tools: `grep "| A11 |" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` output contains both `aos8_show_command` AND `clearpass_get_local_users`
    - Existing A1, A10, A12, A13 rows still present: `grep -c "| A1 | Central reachability" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1; `grep -c "| A10 | Existing Central RADIUS server groups" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1; `grep -c "| A12 | GreenLake device inventory" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1; `grep -c "| A13 | Central firmware recommendation engine" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns 1
    - `cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -x` exits 0
    - Full unit suite green: `cd hpe-networking-mcp && uv run pytest tests/unit -q` exits 0
  </acceptance_criteria>

  <done>
    Stage 4 A11 row now performs the explicit AOS8-vs-ClearPass local-user cross-check using the Batch 3 count already in context (D-04 honored — no duplicate ClearPass call). RULES-03 is fully resolved (Stage 3 deferred → Stage 4 evaluates). Other Stage 4 rows untouched. Full unit test suite passes.
  </done>
</task>

</tasks>

<verification>
After all three tasks:

1. Run skill regression test: `cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -x` — must exit 0.
2. Run full unit suite: `cd hpe-networking-mcp && uv run pytest tests/unit -q` — must exit 0.
3. Mechanical correctness review (Scenario A precedent — no live AOS8 box):
   - Stage 2 has skip clause that narrows to AOS8 data points
   - Stage 3 has live-mode sub-path before Universal rules with all four RULES-0X sections
   - RULES-03 in Stage 3 defers to A11; A11 performs the cross-check
   - Every new tool reference uses the exact catalog name
   - Every new finding cites its VSG anchor and source tool
   - C2/C4/U2 paste-mode rules are unchanged
4. Pre-push CI (per inner CLAUDE.md): `cd hpe-networking-mcp && docker compose -f docker-compose.yml -f docker-compose.dev.yml run --rm hpe-networking-mcp sh -c "uv run ruff check . && uv run ruff format --check . && uv run mypy src/ --ignore-missing-imports && uv run pytest tests/ -q"` — must pass before commit/push.
</verification>

<success_criteria>
- All goal-backward truths in `must_haves.truths` observable in the modified skill file
- `tests/unit/test_skill_tool_references.py` passes after each task and at end of plan
- No Python files modified (markdown-only per REQUIREMENTS.md Out of Scope)
- No `tools:` or `platforms:` frontmatter changes (all referenced tools already listed from Phase 10)
- C2/C4/U2 paste-mode rule rows unchanged (paste-fallback path remains valid for AOS6/IAP/unreachable-AOS8)
- RULES-03 cross-check appears in exactly one place (Stage 4 A11) — Stage 3 only defers
- Live AOS8 end-to-end execution NOT required for sign-off (Scenario A precedent from Phase 10 — partial-approval pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/11-live-vsg-rules/11-01-SUMMARY.md` documenting:
- Three insertions (Stage 2 skip clause, Stage 3 live-mode sub-path, Stage 4 A11 expansion) with line numbers
- Confirmation that C2/C4/U2 paste-mode rows are unchanged
- Confirmation that no `tools:`/`platforms:` frontmatter changes were needed
- Skill regression test green + full unit suite green
- Any deferred manual verification (live AOS8 end-to-end — Scenario A pattern)
- Open Question 1 disposition (LMS IP JSON key referenced by intent, not pinned)
</output>
