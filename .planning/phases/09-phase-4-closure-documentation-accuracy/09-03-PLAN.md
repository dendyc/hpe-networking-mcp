---
phase: 09-phase-4-closure-documentation-accuracy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-differentiator-tools/04-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - DIFF-01
  - DIFF-02
  - DIFF-03
  - DIFF-04
  - DIFF-05
  - DIFF-06
  - DIFF-07
  - DIFF-08
  - DIFF-09
must_haves:
  truths:
    - "Phase 4 has a delegated VERIFICATION.md that cross-references Phase 7's verification of DIFF-01..09"
    - "REQUIREMENTS.md DIFF-01..09 checkboxes are [x] (audit confirms; no flip needed)"
    - "REQUIREMENTS.md traceability table for DIFF-01..09 reads Complete"
    - "Planning integrity is restored — every phase directory has either plans+summary or a VERIFICATION.md explaining absence"
  artifacts:
    - path: ".planning/phases/04-differentiator-tools/04-VERIFICATION.md"
      provides: "Delegated verification report formally retiring Phase 4"
      contains: "DELEGATED to Phase 7"
      min_lines: 40
    - path: ".planning/REQUIREMENTS.md"
      provides: "DIFF rows 61-69 confirmed [x]; traceability rows 178-186 confirmed Complete"
      contains: "[x] **DIFF-09**"
  key_links:
    - from: ".planning/phases/04-differentiator-tools/04-VERIFICATION.md"
      to: ".planning/phases/07-testing-integration/07-VERIFICATION.md"
      via: "explicit cross-reference link to Observable Truth #1"
      pattern: "07-VERIFICATION.md"
    - from: ".planning/phases/04-differentiator-tools/04-VERIFICATION.md"
      to: ".planning/phases/08-fix-diff-tools-production-bug/08-01-SUMMARY.md"
      via: "explicit cross-reference link to Phase 8 fix"
      pattern: "08-01-SUMMARY.md"
---

<objective>
Restore planning integrity for Phase 4 by creating a delegated `04-VERIFICATION.md` that formally documents the administrative merge into Phase 7 and the Phase 8 correction. Audit `REQUIREMENTS.md` to confirm all 9 DIFF rows already show `[x]` and the traceability table reads Complete (per inspection both are already correct — this plan is a verification + a fix-only-if-needed pass).

Purpose: The v1.0 milestone audit identified that `.planning/phases/04-differentiator-tools/` contains only `04-CONTEXT.md` and `04-DISCUSSION-LOG.md` — no VERIFICATION.md, no SUMMARY, no plans. This is an audit gap: a future reader of the planning history cannot tell from Phase 4's directory alone what happened. The DIFF requirements were implemented in Phase 7 plans 07-01/07-02/07-03 and corrected in Phase 8 plan 08-01; this plan formalizes that history.

Output: 1 new file (`04-VERIFICATION.md`) plus REQUIREMENTS.md verified or minimally corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-phase-4-closure-documentation-accuracy/09-RESEARCH.md
@.planning/phases/04-differentiator-tools/04-CONTEXT.md
@.planning/phases/07-testing-integration/07-VERIFICATION.md
@.planning/phases/08-fix-diff-tools-production-bug/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 04-VERIFICATION.md (delegated)</name>
  <files>.planning/phases/04-differentiator-tools/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/07-testing-integration/07-VERIFICATION.md (mirror its frontmatter shape and Observable Truths table style)
    - .planning/phases/04-differentiator-tools/04-CONTEXT.md (confirm Phase 4 was a planned-but-merged phase, not a Phase that ran)
    - .planning/phases/08-fix-diff-tools-production-bug/08-01-SUMMARY.md (confirm Phase 8 closed the response-contract bug for DIFF-01..09)
    - .planning/ROADMAP.md (lines 75-85 Phase 4 spec; line 14 retirement note)
  </read_first>
  <action>
Create the file at the absolute path `.planning/phases/04-differentiator-tools/04-VERIFICATION.md` with the following content. Copy verbatim — do not paraphrase the cross-reference language; the audit checks for these exact strings.

Frontmatter block (YAML between two `---` lines):
- phase: 04-differentiator-tools
- verified: 2026-04-29T00:00:00Z
- status: delegated
- score: "0/0 truths owned by this phase (all DELEGATED)"
- re_verification: false

Body sections in this order:

1. H1 title: `# Phase 4: Differentiator Tools — Verification Report (Delegated)`

2. Header block (bold key/value lines):
   - `**Phase Goal:** Implement DIFF-01..09 — 9 AOS8 differentiator read tools (Conductor hierarchy, effective config, pending changes, RF neighbors, cluster state, air monitors, AP wired ports, IPsec tunnels, MD health check).`
   - `**Status:** **DELEGATED to Phase 7** (plans 07-01, 07-02, 07-03) and **corrected by Phase 8** (plan 08-01).`
   - `**Verified by:**` followed by a bullet list with two items linking to `.planning/phases/07-testing-integration/07-VERIFICATION.md` (Observable Truth #1) and `.planning/phases/08-fix-diff-tools-production-bug/08-01-SUMMARY.md` (response-contract bug fix).

3. H2 section `## Why this phase has no plans of its own` — 1 paragraph explaining that after Phase 6 closed, the v1.0 milestone audit determined the differentiator tools were tightly coupled to Phase 7's TDD workflow, so the 9 DIFF tools were planned and implemented as part of Phase 7 plans 07-01 (RED scaffold), 07-02 (implementation), 07-03 (registration + regression), and corrected by Phase 8 plan 08-01 (response-contract fix). Note that Phase 4's directory therefore contains only 04-CONTEXT.md and 04-DISCUSSION-LOG.md, and that this VERIFICATION.md closes the audit gap.

4. H2 section `## Cross-Reference Matrix` — Markdown table with columns: `DIFF Requirement | Tool | Implemented In | Verified In`. One row per DIFF-01..DIFF-09 using these tool names in order: aos8_get_md_hierarchy, aos8_get_effective_config, aos8_get_pending_changes, aos8_get_rf_neighbors, aos8_get_cluster_state, aos8_get_air_monitors, aos8_get_ap_wired_ports, aos8_get_ipsec_tunnels, aos8_get_md_health_check. The "Implemented In" column for every row reads: `differentiators.py (Phase 7 P02 — refactored Phase 8 P01)`. The "Verified In" column for every row reads: `07-VERIFICATION.md Truth #1 + tests/unit/test_aos8_read_differentiators.py (13 tests GREEN)`. The DIFF-09 row may add: `— DIFF-09 partial-failure contract verified (error surfaces to result['aps'] root when ap_active or ap_db fails)`.

5. H2 section `## Observable Truths` — 1-paragraph statement that all 5 Observable Truths originally framed for Phase 4 are owned by Phase 7, none locally — followed by a Markdown table with columns `# | Truth | Owned By`, listing the 5 truths verbatim from ROADMAP.md Phase 4 success criteria (lines 79-84):
   1. An operator can render the full Conductor → Managed Device hierarchy with config_path values for every node.
   2. An operator can request the effective resolved configuration for a specific MD or AP group after inheritance is applied.
   3. An operator can identify staged configuration changes that have not yet been persisted via write_memory.
   4. An operator can retrieve a unified per-MD health report (aos8_get_md_health_check) covering APs, clients, alarms, and firmware in one call.
   5. An operator can inspect cluster membership, RF neighbors, air-monitor APs, AP wired ports, and IPsec tunnel state through dedicated tools.
   Every "Owned By" cell reads: `Phase 7 — 07-VERIFICATION.md Truth #1`.

6. H2 section `## Score` — 1 paragraph: `**0/0 truths owned by this phase** — all DELEGATED to Phase 7. Score is intentionally zero to avoid double-counting in any future audit aggregation; Phase 7's verification report carries the full score for these truths.`

7. Footer (italic on separate lines):
   - `_Verified: 2026-04-29_`
   - `_Verifier: Claude (gsd-planner) — administrative closure under Phase 9 plan 09-03_`

After creating the file, run these verification greps (each must satisfy the stated count):
- `test -f .planning/phases/04-differentiator-tools/04-VERIFICATION.md` exits 0
- `wc -l < .planning/phases/04-differentiator-tools/04-VERIFICATION.md` returns at least 40
- `grep -c "DELEGATED to Phase 7" .planning/phases/04-differentiator-tools/04-VERIFICATION.md` returns at least 2
- `grep -c "07-VERIFICATION.md" .planning/phases/04-differentiator-tools/04-VERIFICATION.md` returns at least 2
- `grep -c "08-01-SUMMARY.md" .planning/phases/04-differentiator-tools/04-VERIFICATION.md` returns at least 1
- Loop over DIFF-01..DIFF-09 — every ID must appear at least once
  </action>
  <verify>
    <automated>bash -c 'F=.planning/phases/04-differentiator-tools/04-VERIFICATION.md; test -f $F &amp;&amp; test $(wc -l &lt; $F) -ge 40 &amp;&amp; test $(grep -c "DELEGATED to Phase 7" $F) -ge 2 &amp;&amp; test $(grep -c "07-VERIFICATION.md" $F) -ge 2 &amp;&amp; test $(grep -c "08-01-SUMMARY.md" $F) -ge 1 &amp;&amp; for r in DIFF-01 DIFF-02 DIFF-03 DIFF-04 DIFF-05 DIFF-06 DIFF-07 DIFF-08 DIFF-09; do grep -q $r $F || exit 1; done'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-differentiator-tools/04-VERIFICATION.md` exists
    - File line count is at least 40
    - Frontmatter contains `status: delegated` and `re_verification: false`
    - Body contains the literal string `DELEGATED to Phase 7` at least 2 times
    - Body links to `.planning/phases/07-testing-integration/07-VERIFICATION.md` at least 2 times (string `07-VERIFICATION.md`)
    - Body links to `.planning/phases/08-fix-diff-tools-production-bug/08-01-SUMMARY.md` at least 1 time (string `08-01-SUMMARY.md`)
    - All 9 IDs DIFF-01 through DIFF-09 appear at least once
    - All 9 tool names appear at least once: aos8_get_md_hierarchy, aos8_get_effective_config, aos8_get_pending_changes, aos8_get_rf_neighbors, aos8_get_cluster_state, aos8_get_air_monitors, aos8_get_ap_wired_ports, aos8_get_ipsec_tunnels, aos8_get_md_health_check
    - File contains H2 sections: `## Why this phase has no plans of its own`, `## Cross-Reference Matrix`, `## Observable Truths`, `## Score`
  </acceptance_criteria>
  <done>04-VERIFICATION.md exists, scores 0/0 with DELEGATED status, cross-references Phase 7 and Phase 8, and lists all 9 DIFF requirements + 9 tool names — closing the audit gap.</done>
</task>

<task type="auto">
  <name>Task 2: Audit REQUIREMENTS.md DIFF rows + traceability — fix only if drift found</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — must verify lines 60-69 DIFF block AND lines 178-186 traceability rows match the expected end state)
  </read_first>
  <action>
This is a verify-and-fix-only-if-needed task. Per inspection at planning time, REQUIREMENTS.md already shows DIFF-01..09 as `[x]` and the traceability table reads `Complete` for each. This task confirms that and applies a corrective edit ONLY if drift is detected.

Step 1 — Run these audit commands and capture output:

```
grep -n "\[x\] \*\*DIFF-" .planning/REQUIREMENTS.md
grep -nE "^\| DIFF-0[1-9] \|" .planning/REQUIREMENTS.md
```

The first command MUST return exactly 9 lines, one for each of DIFF-01..DIFF-09.
The second command MUST return exactly 9 lines, each ending with `| Complete |`.

Step 2 — If both commands return the expected 9 lines and no row in the traceability table reads anything other than `Complete`, this task is done with NO FILE EDITS. Record in the SUMMARY: "REQUIREMENTS.md already correct at audit time — no edits applied; verified DIFF-01..09 are [x] (lines 61-69) and traceability rows 178-186 read Complete."

Step 3 — If drift is detected (any DIFF-NN row missing `[x]`, or any traceability row not reading `Complete`), apply the minimal fix:
- For a missing `[x]`: change `- [ ] **DIFF-NN**` to `- [x] **DIFF-NN**` on the affected line.
- For a non-Complete traceability row: change the third cell (status) to `Complete`. Preserve the second cell (Phase column) as `Phase 8 — Fix DIFF Tools Production Bug` per current REQUIREMENTS.md state. Do NOT renumber rows or reorder.

Step 4 — Re-run both audit commands; both MUST now return the expected 9-and-9.

Do NOT touch any other rows (FOUND, CLIENT, READ, WRITE, PROMPT, DOCS, TEST). Do NOT change the Phase column for DIFF rows. Do NOT add or remove rows.
  </action>
  <verify>
    <automated>bash -c 'test $(grep -c "\[x\] \*\*DIFF-" .planning/REQUIREMENTS.md) -eq 9 &amp;&amp; test $(grep -cE "^\| DIFF-0[1-9] \|.*\| Complete \|" .planning/REQUIREMENTS.md) -eq 9'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[x\] \*\*DIFF-" .planning/REQUIREMENTS.md` returns exactly 9
    - `grep -cE "^\| DIFF-0[1-9] \|.*\| Complete \|" .planning/REQUIREMENTS.md` returns exactly 9
    - `grep -c "\[ \] \*\*DIFF-" .planning/REQUIREMENTS.md` returns exactly 0 (no unchecked DIFF rows)
    - No FOUND/CLIENT/READ/WRITE/PROMPT/DOCS/TEST rows were modified by this task — confirm via `git diff .planning/REQUIREMENTS.md` showing only DIFF-related changes (or zero changes if no drift)
  </acceptance_criteria>
  <done>REQUIREMENTS.md DIFF-01..09 confirmed [x] in the requirement list AND Complete in the traceability table; no other rows touched.</done>
</task>

</tasks>

<verification>
- `.planning/phases/04-differentiator-tools/04-VERIFICATION.md` exists, line count is at least 40, contains DELEGATED to Phase 7 references and all 9 DIFF IDs + 9 tool names.
- `grep -c "\[x\] \*\*DIFF-" .planning/REQUIREMENTS.md` returns 9.
- `grep -cE "^\| DIFF-0[1-9] \|.*\| Complete \|" .planning/REQUIREMENTS.md` returns 9.
- `git diff` for this plan shows changes only to the two declared files in `files_modified`.
</verification>

<success_criteria>
1. **Planning integrity restored** — Phase 4 directory now contains a VERIFICATION.md that explains the administrative merge into Phase 7 and Phase 8 correction; no future reader is left wondering what happened to Phase 4.
2. **DIFF-01..09 traceability closed** — REQUIREMENTS.md confirmed (or fixed if drifted) to show all 9 DIFF rows as [x] and Complete in the traceability table.
3. **No collateral edits** — only the two declared files changed; other phases and requirement categories untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/09-phase-4-closure-documentation-accuracy/09-03-SUMMARY.md` covering:
- Confirmation that 04-VERIFICATION.md was created (line count, key strings present)
- Whether REQUIREMENTS.md required any edits (expected: none — verified at planning time)
- Output of the two verification grep commands (the 9-and-9 audit)
- Confirmation that no other planning files were touched
</output>
