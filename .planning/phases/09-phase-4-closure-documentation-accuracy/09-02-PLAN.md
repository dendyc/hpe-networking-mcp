---
phase: 09-phase-4-closure-documentation-accuracy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hpe-networking-mcp/README.md
  - hpe-networking-mcp/CHANGELOG.md
  - hpe-networking-mcp/docs/TOOLS.md
  - hpe-networking-mcp/pyproject.toml
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
must_haves:
  truths:
    - "Every user-facing AOS8 tool count states 47 (not 38)"
    - "CHANGELOG.md has a new [2.4.0.1] entry covering Phase 8 + Phase 9 fixes"
    - "docs/TOOLS.md has a `### Differentiators (9)` subsection listing all 9 DIFF tools"
    - "pyproject.toml version is 2.4.0.1"
  artifacts:
    - path: "hpe-networking-mcp/README.md"
      provides: "Capability table + narrative + diagrams updated to 47 AOS8 tools"
      contains: "**47 + 9 prompts**"
    - path: "hpe-networking-mcp/CHANGELOG.md"
      provides: "[2.4.0.1] entry above [2.4.0.0]"
      contains: "## [2.4.0.1]"
    - path: "hpe-networking-mcp/docs/TOOLS.md"
      provides: "Header updated to 47; new Differentiators (9) subsection"
      contains: "### Differentiators (9)"
    - path: "hpe-networking-mcp/pyproject.toml"
      provides: "Version bumped 2.4.0.0 → 2.4.0.1"
      contains: 'version = "2.4.0.1"'
  key_links:
    - from: "docs/TOOLS.md `### Differentiators (9)` subsection"
      to: "platforms/aos8/tools/differentiators.py 9 tool names"
      via: "name match (aos8_get_md_hierarchy etc.)"
      pattern: "aos8_get_md_hierarchy"
    - from: "CHANGELOG.md [2.4.0.1] entry"
      to: "pyproject.toml version field"
      via: "version string match"
      pattern: "2\\.4\\.0\\.1"
---

<objective>
Sweep all user-facing documentation in the source repo to correct the AOS8 tool count from 38 → 47, add a new Differentiators (9) subsection to docs/TOOLS.md, write a CHANGELOG entry that documents the Phase 8 response-contract fix and Phase 9 doc/server.py fix, and bump the package version to 2.4.0.1.

Purpose: Phase 7 added the 9 differentiator tools but the Phase 6 doc batch had already shipped with 38 in every user-facing string. Phase 7 closed under "authorized doc deviation" (CONTEXT.md D-06). Phase 9 retires that authorization. This plan addresses DOCS-01..04 by **correcting** what was written at Phase 6 — not re-doing it. Without this sweep, an operator reading README.md would reasonably believe AOS8 ships only 38 tools and would not know about the 9 differentiator tools.

Output: 4 files modified — README.md (5 edits), CHANGELOG.md (1 new entry block), docs/TOOLS.md (1 header edit + 1 new subsection), pyproject.toml (1 version bump).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-phase-4-closure-documentation-accuracy/09-RESEARCH.md
@.planning/phases/08-fix-diff-tools-production-bug/08-01-SUMMARY.md

<interfaces>
<!-- The 9 DIFF tool names + descriptions sourced from REQUIREMENTS.md DIFF-01..09 -->

| Tool                          | Purpose                                                                                       |
|-------------------------------|-----------------------------------------------------------------------------------------------|
| `aos8_get_md_hierarchy`       | Conductor → Managed Device tree with config_path for each node                                |
| `aos8_get_effective_config`   | Resolved config a specific MD or AP group sees after inheritance                              |
| `aos8_get_pending_changes`    | Staged Conductor changes not yet persisted via `aos8_write_memory`                            |
| `aos8_get_rf_neighbors`       | ARM neighbor graph for an AP — co-channel and adjacent-channel                                |
| `aos8_get_cluster_state`      | AP cluster membership, master/standby roles, failover state                                   |
| `aos8_get_air_monitors`       | APs in air-monitor mode with scan results                                                     |
| `aos8_get_ap_wired_ports`     | Wired downlink port configuration and state for APs                                           |
| `aos8_get_ipsec_tunnels`      | Site-to-site IPsec and Remote AP tunnel state                                                 |
| `aos8_get_md_health_check`    | Unified per-MD health: APs + clients + alarms + firmware in one call                          |

<!-- Verified target lines from 09-RESEARCH.md (line numbers approximate; locate by content match) -->
README.md:55       → capability table footer "AOS8" column: `**38 + 9 prompts**` → `**47 + 9 prompts**`
README.md:59       → narrative "v2.4.0.0 adds **AOS8** (38 tools + 9 prompts)" → "(47 tools + 9 prompts)"
README.md:199      → startup log example "AOS8: 38 underlying tools" → "47 underlying tools"
README.md:417      → ASCII architecture diagram cell containing 38 tool count for AOS8 → 47
README.md:564      → project tree comment mentioning "38 AOS8 tools + 9 prompts" → "47 AOS8 tools + 9 prompts"
CHANGELOG.md:12    → 2.4.0.0 entry: "38 tools across 6 categories: 8 health/inventory, 4 client, 3 alert/audit, 4 WLAN/config, 7 troubleshooting, 12 write" — leave unchanged (immutable released entry); covered by note in new 2.4.0.1 entry.
docs/TOOLS.md:1922 → header "## Aruba OS 8 / Mobility Conductor (38 tools + 9 prompts)" → "(47 tools + 9 prompts)"
docs/TOOLS.md:~1979 → insert new `### Differentiators (9)` subsection BEFORE the existing `### Writes (12)` subsection.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep README.md tool counts 38 → 47 (5 edits)</name>
  <files>hpe-networking-mcp/README.md</files>
  <read_first>
    - hpe-networking-mcp/README.md (full file — needed to locate every `38` occurrence in AOS8-relevant context; ~600 lines)
    - hpe-networking-mcp/tests/unit/test_aos8_init.py (confirms EXPECTED_TOTAL = 47 = 26 read + 12 write + 9 differentiators is the canonical count)
  </read_first>
  <action>
Make these 5 exact text replacements in `hpe-networking-mcp/README.md`. Each edit is unique enough to locate by content match — do NOT rely on line numbers (file may have shifted slightly).

**Edit 1** — Capability table AOS8 column (was line ~55):
- Find: `**38 + 9 prompts**`
- Replace with: `**47 + 9 prompts**`

**Edit 2** — Narrative paragraph mentioning v2.4.0.0 (was line ~59):
- Find: `v2.4.0.0 adds **AOS8** (38 tools + 9 prompts)`
- Replace with: `v2.4.0.0 adds **AOS8** (47 tools + 9 prompts)`

**Edit 3** — Startup log / dynamic-mode summary (was line ~199):
- Find: `AOS8: 38 underlying tools` (case-sensitive)
- Replace with: `AOS8: 47 underlying tools`

**Edit 4** — ASCII architecture diagram cell (was line ~417):
- Find the box-drawing line that shows the AOS8 column with `38 tools` (e.g., `│38 tools│` or `│ 38 tools │`)
- Replace `38` with `47`, preserving box-drawing alignment (if the cell width changes, adjust trailing whitespace inside the cell so the right border `│` stays in the same column as adjacent cells)

**Edit 5** — Project tree comment (was line ~564):
- Find: `38 AOS8 tools + 9 prompts`
- Replace with: `47 AOS8 tools + 9 prompts`

After all 5 edits, run this verification grep — it MUST return zero results:

```
grep -nE 'aos8.{0,40}38|38.{0,40}aos8|38.{0,40}AOS8|AOS8.{0,40}38' hpe-networking-mcp/README.md
```

If any results remain, they are either (a) a 6th occurrence not on the list — verify and fix, or (b) a legitimate `38` in unrelated content (rare in README) — leave alone and document in SUMMARY.

Also confirm there are now **5 occurrences of `47`** in AOS8 contexts:

```
grep -nE 'aos8.{0,40}47|47.{0,40}aos8|47.{0,40}AOS8|AOS8.{0,40}47' hpe-networking-mcp/README.md
```

Should return exactly 5 lines.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp &amp;&amp; bash -c "test \$(grep -cE 'aos8.{0,40}38|38.{0,40}aos8|38.{0,40}AOS8|AOS8.{0,40}38' README.md) -eq 0 &amp;&amp; test \$(grep -cE 'aos8.{0,40}47|47.{0,40}aos8|47.{0,40}AOS8|AOS8.{0,40}47' README.md) -ge 5"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '**47 + 9 prompts**' hpe-networking-mcp/README.md` returns at least 1
    - `grep -c '47 tools + 9 prompts' hpe-networking-mcp/README.md` returns at least 1
    - `grep -c 'AOS8: 47 underlying tools' hpe-networking-mcp/README.md` returns at least 1
    - `grep -c '47 AOS8 tools' hpe-networking-mcp/README.md` returns at least 1
    - `grep -cE 'aos8.{0,40}38|38.{0,40}aos8|38.{0,40}AOS8|AOS8.{0,40}38' hpe-networking-mcp/README.md` returns 0
    - `grep -cE 'aos8.{0,40}47|47.{0,40}aos8|47.{0,40}AOS8|AOS8.{0,40}47' hpe-networking-mcp/README.md` returns at least 5
  </acceptance_criteria>
  <done>All 5 AOS8 tool count references in README.md show 47, no AOS8-context `38` remains, and ASCII diagram alignment is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add `### Differentiators (9)` subsection + header fix to docs/TOOLS.md</name>
  <files>hpe-networking-mcp/docs/TOOLS.md</files>
  <read_first>
    - hpe-networking-mcp/docs/TOOLS.md (read lines 1900-2010 — covers the AOS8 section header and the boundary between Troubleshooting and Writes where the new subsection inserts)
    - .planning/REQUIREMENTS.md (lines 60-69 — the canonical descriptions for DIFF-01..09)
    - hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/differentiators.py (confirms the 9 tool names exist as registered)
  </read_first>
  <action>
Two edits in `hpe-networking-mcp/docs/TOOLS.md`:

**Edit 1 — Header tool count.** Find:
```
## Aruba OS 8 / Mobility Conductor (38 tools + 9 prompts)
```
Replace with:
```
## Aruba OS 8 / Mobility Conductor (47 tools + 9 prompts)
```

**Edit 2 — Insert new subsection.** Locate the existing line that reads `### Writes (12)` inside the AOS8 section (around line 1979). Insert the following Markdown block IMMEDIATELY BEFORE that `### Writes (12)` line — preserving one blank line before and one blank line after the new subsection:

```markdown
### Differentiators (9)

AOS8-unique read tools that go beyond Aruba Central parity — Conductor
hierarchy, effective configuration after inheritance, RF neighbors,
cluster state, IPsec tunnels, and a unified per-MD health roll-up.

| Tool | Purpose |
|---|---|
| `aos8_get_md_hierarchy` | Conductor → Managed Device tree with config_path for each node |
| `aos8_get_effective_config` | Resolved config a specific MD or AP group sees after inheritance |
| `aos8_get_pending_changes` | Staged Conductor changes not yet persisted via `aos8_write_memory` |
| `aos8_get_rf_neighbors` | ARM neighbor graph for an AP — co-channel and adjacent-channel |
| `aos8_get_cluster_state` | AP cluster membership, master/standby roles, failover state |
| `aos8_get_air_monitors` | APs in air-monitor mode with scan results |
| `aos8_get_ap_wired_ports` | Wired downlink port configuration and state for APs |
| `aos8_get_ipsec_tunnels` | Site-to-site IPsec and Remote AP tunnel state |
| `aos8_get_md_health_check` | Unified per-MD health: APs + clients + alarms + firmware in one call |

```

After the insert, the section ordering inside AOS8 must read top-to-bottom:
Health & Inventory (8) → Clients (4) → Alerts & Audit (3) → WLAN & Config (4) → Troubleshooting (7) → **Differentiators (9)** → Writes (12).

After both edits, verify:

```
grep -c "## Aruba OS 8 / Mobility Conductor (47 tools + 9 prompts)" hpe-networking-mcp/docs/TOOLS.md   # must be 1
grep -c "### Differentiators (9)" hpe-networking-mcp/docs/TOOLS.md                                       # must be 1
grep -c "aos8_get_md_hierarchy" hpe-networking-mcp/docs/TOOLS.md                                         # must be at least 1
grep -c "aos8_get_md_health_check" hpe-networking-mcp/docs/TOOLS.md                                      # must be at least 1
grep -c "## Aruba OS 8 / Mobility Conductor (38 tools" hpe-networking-mcp/docs/TOOLS.md                  # must be 0
```
  </action>
  <verify>
    <automated>cd hpe-networking-mcp &amp;&amp; bash -c "test \$(grep -c '### Differentiators (9)' docs/TOOLS.md) -eq 1 &amp;&amp; test \$(grep -c 'aos8_get_md_hierarchy' docs/TOOLS.md) -ge 1 &amp;&amp; test \$(grep -c 'aos8_get_md_health_check' docs/TOOLS.md) -ge 1 &amp;&amp; test \$(grep -c '## Aruba OS 8 / Mobility Conductor (47 tools + 9 prompts)' docs/TOOLS.md) -eq 1 &amp;&amp; test \$(grep -c '## Aruba OS 8 / Mobility Conductor (38 tools' docs/TOOLS.md) -eq 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## Aruba OS 8 / Mobility Conductor (47 tools + 9 prompts)' hpe-networking-mcp/docs/TOOLS.md` returns 1
    - `grep -c '## Aruba OS 8 / Mobility Conductor (38 tools' hpe-networking-mcp/docs/TOOLS.md` returns 0
    - `grep -c '### Differentiators (9)' hpe-networking-mcp/docs/TOOLS.md` returns 1
    - All 9 DIFF tool names appear: `for t in aos8_get_md_hierarchy aos8_get_effective_config aos8_get_pending_changes aos8_get_rf_neighbors aos8_get_cluster_state aos8_get_air_monitors aos8_get_ap_wired_ports aos8_get_ipsec_tunnels aos8_get_md_health_check; do grep -q "$t" hpe-networking-mcp/docs/TOOLS.md || echo "MISSING $t"; done` produces no MISSING output
    - The new `### Differentiators (9)` line appears in the file BEFORE the AOS8 `### Writes (12)` line: `grep -n "### Differentiators (9)\|### Writes (12)" hpe-networking-mcp/docs/TOOLS.md` shows Differentiators on a lower line number than Writes
  </acceptance_criteria>
  <done>docs/TOOLS.md AOS8 section header reads 47, the new Differentiators (9) subsection lists all 9 tools with descriptions, and section ordering is correct.</done>
</task>

<task type="auto">
  <name>Task 3: Add CHANGELOG.md [2.4.0.1] entry + bump pyproject.toml version</name>
  <files>hpe-networking-mcp/CHANGELOG.md, hpe-networking-mcp/pyproject.toml</files>
  <read_first>
    - hpe-networking-mcp/CHANGELOG.md (lines 1-30 — format reference + position to insert above [2.4.0.0])
    - hpe-networking-mcp/pyproject.toml (lines 1-15 — confirm current version field text)
    - .planning/phases/08-fix-diff-tools-production-bug/08-01-SUMMARY.md (source of Phase 8 fix description)
  </read_first>
  <action>
**Edit 1 — CHANGELOG.md.** Insert the following block IMMEDIATELY AFTER line 6 (`and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).`) and IMMEDIATELY BEFORE the existing `## [2.4.0.0] - 2026-04-28` line. Preserve one blank line between the SemVer line and the new entry, and one blank line between the new entry and the 2.4.0.0 header.

```markdown

## [2.4.0.1] - 2026-04-29

### Fixed
- **AOS8 differentiator tools (DIFF-01..09) production response-contract bug.** `differentiators.py` `_show()` and `_object()` previously returned a raw `httpx.Response` object instead of parsed JSON, causing all 9 DIFF tools (`aos8_get_md_hierarchy`, `aos8_get_effective_config`, `aos8_get_pending_changes`, `aos8_get_rf_neighbors`, `aos8_get_cluster_state`, `aos8_get_air_monitors`, `aos8_get_ap_wired_ports`, `aos8_get_ipsec_tunnels`, `aos8_get_md_health_check`) to fail in production. Refactored to use canonical `_helpers.run_show()` / `get_object()`. Test mocks updated to match the real `AOS8Client.request()` contract.
- **Code-mode `execute_description`** now lists `aos8_` as a callable platform prefix. The sandboxed `execute()` LLM was previously told only 6 platform prefixes were dispatchable, causing `Unknown tool: aos8_*` failures despite the tools being registered. Added regression test `test_server_code_mode.py` that asserts every platform prefix appears in the literal.

### Documentation
- README.md, docs/TOOLS.md tool counts corrected from 38 → **47 AOS8 tools** (26 read + 12 write + 9 differentiators). The 9 differentiator tools were added in Phase 7 but the user-facing strings were not refreshed at the time. Note for [2.4.0.0]: tool count was incorrectly stated as 38; the actual shipped count was 47.
- docs/TOOLS.md: new `### Differentiators (9)` subsection lists all 9 AOS8-unique read tools with descriptions.
- `.planning/phases/04-differentiator-tools/04-VERIFICATION.md` added — formally documents that Phase 4 was administratively merged into Phase 7 (plans 07-01/07-02/07-03) and corrected by Phase 8 (plan 08-01).
- REQUIREMENTS.md DIFF-01..09 traceability now reads "Complete".

### Tests
- New `tests/unit/test_server_code_mode.py` (2 tests) — guards code-mode `execute_description` literal against future platform-prefix drift.
- Total unit tests: 766 (was 764).
```

**Edit 2 — pyproject.toml.** Find:
```
version = "2.4.0.0"
```
Replace with:
```
version = "2.4.0.1"
```

After both edits, verify:

```
head -15 hpe-networking-mcp/CHANGELOG.md | grep -c "## \[2.4.0.1\] - 2026-04-29"   # must be 1
grep -c '## \[2.4.0.0\]' hpe-networking-mcp/CHANGELOG.md                            # must still be 1 (entry preserved)
grep -c 'version = "2.4.0.1"' hpe-networking-mcp/pyproject.toml                      # must be 1
grep -c 'version = "2.4.0.0"' hpe-networking-mcp/pyproject.toml                      # must be 0
```
  </action>
  <verify>
    <automated>cd hpe-networking-mcp &amp;&amp; bash -c "test \$(grep -c '## \[2.4.0.1\] - 2026-04-29' CHANGELOG.md) -eq 1 &amp;&amp; test \$(grep -c '## \[2.4.0.0\]' CHANGELOG.md) -eq 1 &amp;&amp; test \$(grep -c 'version = \"2.4.0.1\"' pyproject.toml) -eq 1 &amp;&amp; test \$(grep -c 'version = \"2.4.0.0\"' pyproject.toml) -eq 0"</automated>
  </verify>
  <acceptance_criteria>
    - `head -15 hpe-networking-mcp/CHANGELOG.md` shows `## [2.4.0.1] - 2026-04-29` BEFORE `## [2.4.0.0]`
    - `grep -c '## \[2.4.0.0\]' hpe-networking-mcp/CHANGELOG.md` returns 1 (existing entry untouched)
    - The new entry contains literal substrings: `differentiator tools`, `47 AOS8 tools`, `execute_description`, `aos8_get_md_hierarchy`
    - `grep -c 'version = "2.4.0.1"' hpe-networking-mcp/pyproject.toml` returns 1
    - `grep -c 'version = "2.4.0.0"' hpe-networking-mcp/pyproject.toml` returns 0
    - `cd hpe-networking-mcp && uv run pytest tests/unit -q` exits 0 (no test depends on version string; sanity check)
  </acceptance_criteria>
  <done>CHANGELOG.md has a new immutable [2.4.0.1] entry above [2.4.0.0] documenting all Phase 8 + Phase 9 fixes; pyproject.toml version is bumped to 2.4.0.1; existing 2.4.0.0 entry is preserved unchanged.</done>
</task>

</tasks>

<verification>
- README.md, docs/TOOLS.md show 47 (not 38) for AOS8 tool counts in every AOS8-context occurrence.
- docs/TOOLS.md has a `### Differentiators (9)` subsection between Troubleshooting and Writes listing all 9 DIFF tool names.
- CHANGELOG.md `## [2.4.0.1] - 2026-04-29` entry exists above `## [2.4.0.0]` and documents Phase 8 + Phase 9 fixes.
- pyproject.toml `version = "2.4.0.1"`.
- Final regression check: `cd hpe-networking-mcp && uv run pytest tests/unit -q` exits 0.
- Lint clean: `cd hpe-networking-mcp && uv run ruff check .` exits 0 (doc files not linted by ruff but should not regress).
</verification>

<success_criteria>
1. **DOCS-02** — README.md AOS8 capability row, narrative, startup log, ASCII diagram, and project tree comment all show 47.
2. **DOCS-03** — CHANGELOG.md has a new 2.4.0.1 entry that names every Phase 8 + Phase 9 fix.
3. **DOCS-04** — docs/TOOLS.md AOS8 header shows 47 and lists all 9 differentiator tools in a dedicated subsection.
4. **DOCS-01** — Re-confirmed satisfied at Phase 6 (no edit required for this plan; included in `requirements` for traceability/audit completeness per RESEARCH.md and REQUIREMENTS.md line 105).
5. pyproject.toml version reflects the patch bump.
6. No test regression.
</success_criteria>

<output>
After completion, create `.planning/phases/09-phase-4-closure-documentation-accuracy/09-02-SUMMARY.md` covering:
- Per-file edit counts (README: 5, TOOLS.md: 2, CHANGELOG.md: 1 block insertion, pyproject.toml: 1)
- Output of the verification grep commands
- Final test count + lint/mypy status
- Confirmation that DOCS-01 was not edited (Phase 6 already satisfied) — DOCS-02/03/04 newly satisfied by this plan
</output>
