---
phase: 01-platform-foundation
plan: 03
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - hpe-networking-mcp/src/hpe_networking_mcp/platforms/_common/tool_registry.py
autonomous: true
requirements: [FOUND-04, FOUND-03]
must_haves:
  truths:
    - "`REGISTRIES` dict in `tool_registry.py` has key `\"aos8\"` mapping to an empty dict literal."
    - "`_WRITE_TAG_BY_PLATFORM` dict has key `\"aos8\"` mapping to `{\"aos8_write\", \"aos8_write_delete\"}`."
    - "`_GATE_CONFIG_ATTR` dict has key `\"aos8\"` mapping to `\"enable_aos8_write_tools\"`."
    - "`is_tool_enabled` correctly gates aos8_write and aos8_write_delete tags behind `enable_aos8_write_tools=True`."
    - "Wave 0 tests `test_aos8_write_gate`, `test_aos8_write_delete_gate`, and the aos8 case in `test_records_into_the_right_platform` go GREEN after this plan (assuming Plan 02 also merged so ServerConfig accepts the kwarg)."
  artifacts:
    - path: "hpe-networking-mcp/src/hpe_networking_mcp/platforms/_common/tool_registry.py"
      provides: "AOS8 entries in REGISTRIES, _WRITE_TAG_BY_PLATFORM, _GATE_CONFIG_ATTR"
      contains: "\"aos8\""
  key_links:
    - from: "_GATE_CONFIG_ATTR['aos8']"
      to: "ServerConfig.enable_aos8_write_tools"
      via: "getattr(config, _GATE_CONFIG_ATTR[platform])"
      pattern: "enable_aos8_write_tools"
    - from: "_WRITE_TAG_BY_PLATFORM['aos8']"
      to: "ToolSpec.tags"
      via: "tag intersection check in is_tool_enabled"
      pattern: "aos8_write"
---

<objective>
Register the `aos8` platform in the three shared tool-registry dicts. Pure dict-literal addition, three lines of source change.

Purpose: Make `is_tool_enabled` recognize aos8_write / aos8_write_delete tags and gate them behind `ServerConfig.enable_aos8_write_tools`. This completes the registry plumbing so Phase 2's `AOS8Client` and Phase 3's read tools can register cleanly.
Output: Modified `hpe-networking-mcp/src/hpe_networking_mcp/platforms/_common/tool_registry.py` with three new dict entries (one per dict).

Out of scope: Creating any tool, creating `platforms/aos8/_registry.py` (deferred to Phase 2 — Pitfall 1), modifying `server.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-platform-foundation/01-CONTEXT.md
@.planning/phases/01-platform-foundation/01-RESEARCH.md
@.planning/phases/01-platform-foundation/01-VALIDATION.md
@.planning/phases/01-platform-foundation/01-01-SUMMARY.md

@hpe-networking-mcp/src/hpe_networking_mcp/platforms/_common/tool_registry.py
@hpe-networking-mcp/tests/unit/test_tool_registry.py

<interfaces>
<!-- Existing pattern in tool_registry.py (lines ~39-81 per RESEARCH.md): -->

```python
REGISTRIES: dict[str, dict[str, ToolSpec]] = {
    "apstra": {},
    "axis": {},
    "central": {},
    "clearpass": {},
    "greenlake": {},
    "mist": {},
}

_WRITE_TAG_BY_PLATFORM: dict[str, set[str]] = {
    "apstra": {"apstra_write", "apstra_write_delete"},
    "axis": {"axis_write", "axis_write_delete"},
    "central": {"central_write", "central_write_delete"},
    "clearpass": {"clearpass_write", "clearpass_write_delete"},
    "mist": {"mist_write", "mist_write_delete"},
}

_GATE_CONFIG_ATTR: dict[str, str | None] = {
    "apstra": "enable_apstra_write_tools",
    "axis": "enable_axis_write_tools",
    "central": "enable_central_write_tools",
    "clearpass": "enable_clearpass_write_tools",
    "greenlake": None,  # read-only
    "mist": "enable_mist_write_tools",
}
```
(Note: actual dict ordering may differ; the EDIT preserves whatever order exists and adds aos8 in alphabetical position.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add aos8 entries to the three registry dicts</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/platforms/_common/tool_registry.py</files>
  <read_first>
    - hpe-networking-mcp/src/hpe_networking_mcp/platforms/_common/tool_registry.py (CURRENT FILE — must read to find exact dict literal locations and current ordering of platform keys)
    - hpe-networking-mcp/tests/unit/test_tool_registry.py (the test_aos8_write_gate / test_aos8_write_delete_gate tests this implementation must satisfy)
  </read_first>
  <behavior>
    - `REGISTRIES["aos8"]` is `{}` (empty dict, ready for tools to be recorded into).
    - `_WRITE_TAG_BY_PLATFORM["aos8"]` is `{"aos8_write", "aos8_write_delete"}`.
    - `_GATE_CONFIG_ATTR["aos8"]` is `"enable_aos8_write_tools"`.
    - `is_tool_enabled(spec_with_aos8_write_tag, ServerConfig())` returns False.
    - `is_tool_enabled(spec_with_aos8_write_tag, ServerConfig(enable_aos8_write_tools=True))` returns True.
    - `record_tool(ToolSpec(platform="aos8", ...))` deposits the spec into `REGISTRIES["aos8"]`.
  </behavior>
  <action>
Open `hpe-networking-mcp/src/hpe_networking_mcp/platforms/_common/tool_registry.py`. Locate the three top-level dict literals: `REGISTRIES`, `_WRITE_TAG_BY_PLATFORM`, `_GATE_CONFIG_ATTR` (around lines 39-81 per RESEARCH.md, but verify by reading the file).

EDIT 1 — `REGISTRIES`:
Add this line inside the dict literal, in alphabetical position (between `"$last_alphabetic_before"` and `"apstra"`, since "aos8" < "apstra" alphabetically):
```python
    "aos8": {},
```

EDIT 2 — `_WRITE_TAG_BY_PLATFORM`:
Add this line inside the dict literal, alphabetically before "apstra":
```python
    "aos8": {"aos8_write", "aos8_write_delete"},
```

EDIT 3 — `_GATE_CONFIG_ATTR`:
Add this line inside the dict literal, alphabetically before "apstra":
```python
    "aos8": "enable_aos8_write_tools",
```

CRITICAL DETAILS (per D-06, D-07, D-08):
- Use exact strings: `"aos8"`, `"aos8_write"`, `"aos8_write_delete"`, `"enable_aos8_write_tools"`.
- Set literal `{"aos8_write", "aos8_write_delete"}` — NOT a list, NOT a tuple. Match the existing `apstra` entry's container type exactly.
- Trailing comma on each new line (project style).
- DO NOT reorder existing keys.
- DO NOT add `"aos8"` to any other dict in the file (e.g., if there is a `_PLATFORM_DEPENDENCIES` or similar — only the three dicts named above per FOUND-04).
- DO NOT modify `record_tool`, `is_tool_enabled`, or `ToolSpec` definitions — they already handle any platform key generically.

Verify ≤ 120 chars per line, double quotes used.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_tool_registry.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Grep `"aos8": {}` in `src/hpe_networking_mcp/platforms/_common/tool_registry.py` matches exactly once (the REGISTRIES entry).
    - Grep `"aos8": {"aos8_write", "aos8_write_delete"}` matches exactly once.
    - Grep `"aos8": "enable_aos8_write_tools"` matches exactly once.
    - Existing platform entries (`"apstra"`, `"axis"`, `"central"`, `"clearpass"`, `"greenlake"`, `"mist"`) are ALL still present unchanged: `grep -c "\"apstra\":" tool_registry.py` returns the same count as before edit.
    - `cd hpe-networking-mcp && uv run pytest tests/unit/test_tool_registry.py::TestIsToolEnabled::test_aos8_write_gate -x` PASSES.
    - `cd hpe-networking-mcp && uv run pytest tests/unit/test_tool_registry.py::TestIsToolEnabled::test_aos8_write_delete_gate -x` PASSES.
    - `cd hpe-networking-mcp && uv run pytest tests/unit/test_tool_registry.py -x -q` PASSES (no regressions on other platforms' gate tests).
    - `cd hpe-networking-mcp && uv run ruff check src/hpe_networking_mcp/platforms/_common/tool_registry.py` clean.
    - `cd hpe-networking-mcp && uv run mypy src/hpe_networking_mcp/platforms/_common/tool_registry.py --ignore-missing-imports` clean.
  </acceptance_criteria>
  <done>Three dict entries added in alphabetical position. test_aos8_write_gate and test_aos8_write_delete_gate are GREEN. test_records_into_the_right_platform aos8 case is GREEN. No regressions.</done>
</task>

</tasks>

<verification>
1. `cd hpe-networking-mcp && uv run pytest tests/unit/test_tool_registry.py -x -q` — fully PASS.
2. `cd hpe-networking-mcp && uv run python -c "from hpe_networking_mcp.platforms._common.tool_registry import REGISTRIES, _WRITE_TAG_BY_PLATFORM, _GATE_CONFIG_ATTR; assert REGISTRIES['aos8'] == {}; assert _WRITE_TAG_BY_PLATFORM['aos8'] == {'aos8_write', 'aos8_write_delete'}; assert _GATE_CONFIG_ATTR['aos8'] == 'enable_aos8_write_tools'; print('OK')"` — prints OK.
3. `cd hpe-networking-mcp && uv run ruff check src/ && uv run ruff format --check src/` clean.
</verification>

<success_criteria>
- AOS8 registered in all three dicts with correct values per D-06/D-07/D-08.
- All Wave 0 registry tests GREEN (depend on Plan 02 also being merged for the ServerConfig kwarg).
- No regression on existing platform tests in test_tool_registry.py.
- Ruff and mypy clean.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation/01-03-SUMMARY.md` documenting the three dict entries added and confirming GREEN test transition.
</output>
