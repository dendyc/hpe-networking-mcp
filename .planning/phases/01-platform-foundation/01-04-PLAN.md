---
phase: 01-platform-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - hpe-networking-mcp/docker-compose.yml
  - hpe-networking-mcp/secrets/aos8_host.example
  - hpe-networking-mcp/secrets/aos8_username.example
  - hpe-networking-mcp/secrets/aos8_password.example
  - hpe-networking-mcp/secrets/aos8_port.example
  - hpe-networking-mcp/secrets/aos8_verify_ssl.example
autonomous: true
requirements: [FOUND-05]
must_haves:
  truths:
    - "`docker-compose.yml` parses as valid YAML."
    - "`docker-compose.yml` service environment block contains `ENABLE_AOS8_WRITE_TOOLS` env var with `${ENABLE_AOS8_WRITE_TOOLS:-true}` default."
    - "`docker-compose.yml` service `secrets:` mount list includes 5 AOS8 entries: aos8_host, aos8_username, aos8_password, aos8_port, aos8_verify_ssl."
    - "`docker-compose.yml` top-level `secrets:` block defines all 5 AOS8 secrets with `file: ./secrets/<name>` pointers."
    - "All 5 `.example` files exist in `hpe-networking-mcp/secrets/` with non-empty single-line placeholder content."
  artifacts:
    - path: "hpe-networking-mcp/docker-compose.yml"
      provides: "AOS8 service env, mount list, and top-level secrets defs"
      contains: "aos8_host"
    - path: "hpe-networking-mcp/secrets/aos8_host.example"
      provides: "Placeholder host"
      min_lines: 1
    - path: "hpe-networking-mcp/secrets/aos8_username.example"
      provides: "Placeholder username"
      min_lines: 1
    - path: "hpe-networking-mcp/secrets/aos8_password.example"
      provides: "Placeholder password"
      min_lines: 1
    - path: "hpe-networking-mcp/secrets/aos8_port.example"
      provides: "Placeholder port"
      min_lines: 1
    - path: "hpe-networking-mcp/secrets/aos8_verify_ssl.example"
      provides: "Placeholder verify_ssl"
      min_lines: 1
  key_links:
    - from: "docker-compose.yml service.secrets"
      to: "docker-compose.yml top-level secrets block"
      via: "name match (e.g., 'aos8_host' in both lists)"
      pattern: "aos8_host"
    - from: "top-level secrets entry"
      to: "secrets/<name> file at runtime"
      via: "file: ./secrets/<name>"
      pattern: "file: ./secrets/aos8_"
---

<objective>
Wire AOS8 secrets and the write-gate env var into `docker-compose.yml`, and create five `.example` template files. This is the operator-facing surface — what a user actually drops files into to enable AOS8.

Purpose: Allow an operator to copy `secrets/aos8_*.example` to `secrets/aos8_*`, fill in real values, and have the running container automatically discover them at `/run/secrets/aos8_*` per D-09 and D-10.
Output:
- `docker-compose.yml` extended with 5 secret mounts, 5 top-level secret defs, and 1 env var
- 5 new `.example` template files

Out of scope: Source code (covered by Plans 02 and 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-platform-foundation/01-CONTEXT.md
@.planning/phases/01-platform-foundation/01-RESEARCH.md

@hpe-networking-mcp/docker-compose.yml
@hpe-networking-mcp/secrets/

<interfaces>
<!-- Existing pattern in docker-compose.yml (per RESEARCH.md Pattern 5): -->

```yaml
services:
  hpe-networking-mcp:
    environment:
      - ENABLE_MIST_WRITE_TOOLS=${ENABLE_MIST_WRITE_TOOLS:-true}
      - ENABLE_CENTRAL_WRITE_TOOLS=${ENABLE_CENTRAL_WRITE_TOOLS:-true}
      # ... clearpass, apstra, axis ...
      - ENABLE_AXIS_WRITE_TOOLS=${ENABLE_AXIS_WRITE_TOOLS:-true}
    secrets:
      # Mist
      - mist_api_token
      - mist_org_id
      - mist_host
      # ... central, greenlake, clearpass ...
      # Apstra
      - apstra_server
      - apstra_username
      - apstra_password
      - apstra_port
      - apstra_verify_ssl
      # Axis
      - axis_api_token

secrets:
  mist_api_token:
    file: ./secrets/mist_api_token
  # ...
  apstra_server:
    file: ./secrets/apstra_server
  # ...
  axis_api_token:
    file: ./secrets/axis_api_token
```

`.example` style (per RESEARCH.md Example 3): single-line plaintext, no trailing comments.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AOS8 to docker-compose.yml (env + mounts + top-level secrets)</name>
  <files>hpe-networking-mcp/docker-compose.yml</files>
  <read_first>
    - hpe-networking-mcp/docker-compose.yml (CURRENT FILE — must read to find exact ordering and indentation of environment block, service secrets list, and top-level secrets block)
    - .planning/phases/01-platform-foundation/01-RESEARCH.md (Pattern 5 has line-number hints but verify against current file)
  </read_first>
  <action>
Open `hpe-networking-mcp/docker-compose.yml`. Make THREE additions, all append-after-axis (per D-09, D-05's append-only spirit):

EDIT 1 — Service `environment:` block:
Find the line `- ENABLE_AXIS_WRITE_TOOLS=${ENABLE_AXIS_WRITE_TOOLS:-true}` (approximate line 17 per RESEARCH.md). On the line IMMEDIATELY AFTER it, add:
```yaml
      - ENABLE_AOS8_WRITE_TOOLS=${ENABLE_AOS8_WRITE_TOOLS:-true}
```
(Default `:-true` matches all other platforms per Open Question 2 in RESEARCH.md — runtime safety still requires elicitation; that's a Phase 5 concern.)

EDIT 2 — Service `secrets:` mount list:
Find the existing service-level `secrets:` block. After the LAST entry (likely `- axis_api_token` or another axis-related secret), add:
```yaml
      # Aruba OS 8 / Mobility Conductor (remove lines for platforms you don't use)
      - aos8_host
      - aos8_username
      - aos8_password
      - aos8_port
      - aos8_verify_ssl
```
(Match the comment-grouping style of existing platforms — find how Apstra is commented and replicate.)

EDIT 3 — Top-level `secrets:` block:
Find the top-level `secrets:` block (NOT the service-level one — it's at the file's bottom, around line 87+ per RESEARCH.md). After the LAST existing secret (likely `axis_api_token:`), add:
```yaml
  # Aruba OS 8 / Mobility Conductor
  aos8_host:
    file: ./secrets/aos8_host
  aos8_username:
    file: ./secrets/aos8_username
  aos8_password:
    file: ./secrets/aos8_password
  aos8_port:
    file: ./secrets/aos8_port
  aos8_verify_ssl:
    file: ./secrets/aos8_verify_ssl
```

CRITICAL DETAILS:
- YAML indentation MUST match existing entries (typically 2 spaces for top-level secret keys, 4 spaces for `file:` value).
- Service `secrets:` list items are typically indented 6 spaces (under `services.<name>.secrets:`).
- Service `environment:` items are typically indented 6 spaces with `- KEY=value` form.
- DO NOT reorder any existing secret or env var.
- The 5 secret names MUST match exactly what `_load_aos8()` reads via `_read_secret()`: `aos8_host`, `aos8_username`, `aos8_password`, `aos8_port`, `aos8_verify_ssl`.
- DO NOT add `external: true` or other Docker-secret modifiers — every other platform uses plain `file: ./secrets/<name>` form.

After edits, verify the file still parses as valid YAML (verify step does this).
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && python -c "import yaml; c=yaml.safe_load(open('docker-compose.yml')); svc=c['services']['hpe-networking-mcp']; assert 'aos8_host' in c['secrets'], 'aos8_host missing from top-level secrets'; assert 'aos8_username' in c['secrets']; assert 'aos8_password' in c['secrets']; assert 'aos8_port' in c['secrets']; assert 'aos8_verify_ssl' in c['secrets']; assert 'aos8_host' in svc['secrets'], 'aos8_host missing from service secrets'; assert 'aos8_username' in svc['secrets']; assert 'aos8_password' in svc['secrets']; assert 'aos8_port' in svc['secrets']; assert 'aos8_verify_ssl' in svc['secrets']; assert any('AOS8_WRITE' in e for e in svc['environment']), 'ENABLE_AOS8_WRITE_TOOLS missing from environment'; assert c['secrets']['aos8_host']['file'] == './secrets/aos8_host'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import yaml; yaml.safe_load(open('hpe-networking-mcp/docker-compose.yml'))"` succeeds (valid YAML).
    - Grep `ENABLE_AOS8_WRITE_TOOLS` in `hpe-networking-mcp/docker-compose.yml` matches exactly once.
    - Grep `${ENABLE_AOS8_WRITE_TOOLS:-true}` matches once.
    - Grep `- aos8_host\|- aos8_username\|- aos8_password\|- aos8_port\|- aos8_verify_ssl` matches all 5 (service mounts).
    - Grep `file: ./secrets/aos8_host\|file: ./secrets/aos8_username\|file: ./secrets/aos8_password\|file: ./secrets/aos8_port\|file: ./secrets/aos8_verify_ssl` matches all 5 (top-level defs).
    - Grep `aos8_host:` (with trailing colon) matches once (top-level def header).
    - Grep `apstra_server:\|axis_api_token:` still present (existing platforms unchanged).
    - The verify automated command above prints `OK`.
  </acceptance_criteria>
  <done>docker-compose.yml has env var + 5 service mounts + 5 top-level defs for AOS8. YAML parses. All 5 names align with what `_load_aos8` reads. Existing platform entries unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create 5 .example secret template files</name>
  <files>
    hpe-networking-mcp/secrets/aos8_host.example,
    hpe-networking-mcp/secrets/aos8_username.example,
    hpe-networking-mcp/secrets/aos8_password.example,
    hpe-networking-mcp/secrets/aos8_port.example,
    hpe-networking-mcp/secrets/aos8_verify_ssl.example
  </files>
  <read_first>
    - hpe-networking-mcp/secrets/apstra_server.example (style reference)
    - hpe-networking-mcp/secrets/apstra_verify_ssl.example (boolean style reference)
    - .planning/phases/01-platform-foundation/01-RESEARCH.md (Example 3 has exact target content)
  </read_first>
  <action>
Verify the directory `hpe-networking-mcp/secrets/` exists and inspect 1-2 existing `*.example` files to confirm the convention (single-line plaintext, no trailing newline beyond what the editor writes by default, no comments inside the file). Per D-10, content should be descriptive placeholder text matching existing examples.

Create these 5 files with EXACTLY this single-line content:

1. `hpe-networking-mcp/secrets/aos8_host.example`:
   ```
   your-conductor-hostname-or-ip
   ```

2. `hpe-networking-mcp/secrets/aos8_username.example`:
   ```
   admin
   ```

3. `hpe-networking-mcp/secrets/aos8_password.example`:
   ```
   replace-with-real-password
   ```

4. `hpe-networking-mcp/secrets/aos8_port.example`:
   ```
   4343
   ```

5. `hpe-networking-mcp/secrets/aos8_verify_ssl.example`:
   ```
   true
   ```

Each file is a single line of text. The Write tool will add a trailing newline by default — that's fine and matches existing `.example` files (verify by inspecting `apstra_server.example`).

CRITICAL:
- File names MUST end in `.example` exactly — no `.template`, no `.sample`, no `.txt`.
- Content is plaintext only — no YAML, no JSON, no shell quoting.
- DO NOT create `aos8_host`, `aos8_username`, etc. (without `.example`) — those are real secrets, never committed.
- DO NOT include any real credentials, sample tokens that look real, or anything that resembles production data.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && for f in aos8_host aos8_username aos8_password aos8_port aos8_verify_ssl; do test -s "secrets/$f.example" && echo "OK: $f.example" || echo "MISSING: $f.example"; done</automated>
  </verify>
  <acceptance_criteria>
    - File `hpe-networking-mcp/secrets/aos8_host.example` exists, non-empty, content is `your-conductor-hostname-or-ip` (plus optional trailing newline).
    - File `hpe-networking-mcp/secrets/aos8_username.example` exists, non-empty, content is `admin`.
    - File `hpe-networking-mcp/secrets/aos8_password.example` exists, non-empty, content is `replace-with-real-password`.
    - File `hpe-networking-mcp/secrets/aos8_port.example` exists, non-empty, content is `4343`.
    - File `hpe-networking-mcp/secrets/aos8_verify_ssl.example` exists, non-empty, content is `true`.
    - No file `hpe-networking-mcp/secrets/aos8_host` (without `.example`) exists — verify with `test ! -e hpe-networking-mcp/secrets/aos8_host`.
    - No file `hpe-networking-mcp/secrets/aos8_username`, `aos8_password`, `aos8_port`, `aos8_verify_ssl` (without `.example`) exists.
    - The verify command above prints `OK: aos8_host.example`, `OK: aos8_username.example`, `OK: aos8_password.example`, `OK: aos8_port.example`, `OK: aos8_verify_ssl.example` (5 OK lines, 0 MISSING lines).
    - `git status` shows the 5 new `.example` files as untracked/new.
  </acceptance_criteria>
  <done>5 single-line `.example` template files exist with placeholder content. None of the corresponding non-example files exist. Operator can `cp aos8_host.example aos8_host` and edit to fill in real value.</done>
</task>

</tasks>

<verification>
1. YAML validity: `cd hpe-networking-mcp && python -c "import yaml; yaml.safe_load(open('docker-compose.yml')); print('valid')"` prints `valid`.
2. All 5 example files: `cd hpe-networking-mcp && for f in aos8_host aos8_username aos8_password aos8_port aos8_verify_ssl; do test -s "secrets/$f.example" || { echo "MISSING $f.example"; exit 1; }; done && echo OK` prints `OK`.
3. No accidental real secret files: `cd hpe-networking-mcp && for f in aos8_host aos8_username aos8_password aos8_port aos8_verify_ssl; do test ! -e "secrets/$f" || { echo "FORBIDDEN: secrets/$f exists"; exit 1; }; done && echo OK` prints `OK`.
4. Comprehensive structural check: the verify command in Task 1 prints `OK`.
</verification>

<success_criteria>
- `docker-compose.yml` env var, 5 mount entries, 5 top-level secret defs all present and YAML-valid.
- 5 `.example` files exist with correct placeholder content.
- No real (non-`.example`) AOS8 secret files committed.
- All 5 secret names match `_load_aos8()`'s `_read_secret(...)` calls (operator drops file → server reads it at `/run/secrets/<name>`).
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation/01-04-SUMMARY.md` documenting the docker-compose.yml diff (3 sections added) and the 5 example file paths created.
</output>
