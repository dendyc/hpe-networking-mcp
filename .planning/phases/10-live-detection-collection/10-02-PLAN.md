---
phase: 10-live-detection-collection
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md
autonomous: false
requirements:
  - DETECT-01
  - COLLECT-01
  - COLLECT-02
  - COLLECT-03
  - COLLECT-04

must_haves:
  truths:
    - "When AOS8 is reachable at session start, the skill announces 'AOS8 API mode — live data' before Stage 0 begins"
    - "When AOS8 is NOT reachable, the skill proceeds silently into Stage 0 with no announcement and no UX change for AOS6/IAP/unreachable-AOS8 sessions"
    - "Stage 1 AOS8 live-mode procedure narrates collection in 4 grouped batches (COLLECT-01, -02, -03, -04) using only the 9 catalog AOS8 tools"
    - "Per-batch failure of any tool triggers an exact-CLI-command paste fallback (no full-table reprint, no VSG anchors in fallback prose) and continues to the next batch"
    - "AOS6 and IAP Stage 1 paste tables remain unchanged byte-for-byte"
    - "Skill frontmatter `tools:` list includes all 9 required AOS8 tools (DETECT-01 + COLLECT-01..04)"
    - "Skill regression tests pass — both `test_skill_tool_references.py` (regex enforcement, post-Plan-01) and `test_skill_aos8_live_detection.py` (parametrized presence)"
  artifacts:
    - path: "hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md"
      provides: "Updated skill with Stage -1 detection, AOS8 Stage 1 live procedure, and frontmatter additions"
      contains: "AOS8 API mode"
  key_links:
    - from: "skill Stage -1 detection block"
      to: "health() tool"
      via: "session-start probe before Stage 0"
      pattern: "health\\(\\)"
    - from: "skill Stage 1 AOS8 Batch 1"
      to: "aos8_get_md_hierarchy + aos8_get_effective_config"
      via: "live-mode batch narration"
      pattern: "aos8_get_md_hierarchy"
    - from: "skill Stage 1 AOS8 Batch 2"
      to: "aos8_get_ap_database"
      via: "live-mode batch narration"
      pattern: "aos8_get_ap_database"
    - from: "skill Stage 1 AOS8 Batch 3"
      to: "aos8_get_cluster_state + aos8_show_command"
      via: "live-mode batch narration"
      pattern: "aos8_get_cluster_state"
    - from: "skill Stage 1 AOS8 Batch 4"
      to: "aos8_get_clients + aos8_get_bss_table + aos8_get_active_aps + aos8_get_ap_wired_ports"
      via: "live-mode batch narration"
      pattern: "aos8_get_active_aps"
    - from: "frontmatter `tools:` list"
      to: "9 AOS8 tool names"
      via: "YAML list addition"
      pattern: "aos8_get_md_hierarchy.*aos8_get_ap_wired_ports"
---

<objective>
Rewrite the AOS8 portion of `aos-migration-readiness.md` so the skill auto-detects
AOS8 connectivity at session start and, when reachable, gathers all Stage 1 data
through live AOS8 API calls instead of CLI paste. Implements DETECT-01 and
COLLECT-01..04 verbatim per the locked decisions in 10-CONTEXT.md (D-01..D-09).

Purpose: drop the operator's data-collection burden from ~16 manual CLI commands
+ paste to ~30 seconds of API calls, while preserving the existing paste flow
for AOS6, IAP, and unreachable-AOS8 sessions.

Output: updated single skill file (markdown only — no Python). Frontmatter
extended with 9 AOS8 tool names. Stage -1 detection block added before Stage 0.
AOS8 Stage 1 section restructured with live-mode (4 batches) + paste-fallback
sub-paths. AOS6 and IAP sections untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-live-detection-collection/10-CONTEXT.md
@.planning/phases/10-live-detection-collection/10-RESEARCH.md
@.planning/phases/10-live-detection-collection/10-VALIDATION.md
@.planning/phases/10-live-detection-collection/10-01-PLAN.md

@hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md
@hpe-networking-mcp/src/hpe_networking_mcp/skills/infrastructure-health-check.md
@hpe-networking-mcp/CLAUDE.md

<interfaces>
<!-- Verified AOS8 tool decorator names (from 10-RESEARCH.md, source-confirmed) -->

The 9 AOS8 tools available in v1.0 catalog. Use these EXACT names in skill prose
and frontmatter — typos will fail Plan 01's regex test:

| Tool name                  | File location (in hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/) | Required arg(s)                    | Replaces CLI command(s)                             |
|----------------------------|-----------------------------------------------------------------------------------|------------------------------------|------------------------------------------------------|
| aos8_get_md_hierarchy      | differentiators.py:49                                                             | (none beyond ctx)                  | show configuration node-hierarchy                    |
| aos8_get_effective_config  | differentiators.py:72                                                             | object_name, config_path="/md"     | show configuration effective detail <node> (object-scoped) |
| aos8_get_ap_database       | health.py:46                                                                      | config_path="/md"                  | show ap database long                                |
| aos8_get_cluster_state     | differentiators.py:157                                                            | (none beyond ctx)                  | show lc-cluster group-membership                     |
| aos8_show_command          | troubleshooting.py:82                                                             | command (any "show ..." string)    | show running-config / show local-user db / show inventory / show port status / show trunk |
| aos8_get_clients           | clients.py:31                                                                     | (filters optional)                 | show ap association + show user-table                |
| aos8_get_bss_table         | health.py:117                                                                     | (none beyond ctx)                  | show ap essid                                        |
| aos8_get_active_aps        | health.py:66                                                                      | (none beyond ctx)                  | show ap active                                       |
| aos8_get_ap_wired_ports    | differentiators.py:201                                                            | ap_name (per AP — iterate)         | show ap lldp neighbors                               |

`health()` is already in the skill's frontmatter (line 23 — verified) — no
change needed for DETECT-01's tool reference, only the prose addition.
</interfaces>

<resolution_for_d04_d02_contradiction>
<!-- See 10-RESEARCH.md "Pitfall 3" — D-04 says paste table is "removed entirely"
     but D-02 says paste flow is unchanged when AOS8 is unreachable.

     RESOLUTION (planner decision, documented per Pitfall 3 guidance):
     The `If source = aos8` Stage 1 section in the rewritten skill has TWO
     sub-paths under one heading:

       1. **Live-mode sub-path** (used when Stage -1 detection announced
          "AOS8 API mode"): API-only, four-batch narration, no CLI table.
       2. **Paste-fallback sub-path** (used when Stage -1 was silent OR live
          batches all fail): keeps a brief 16-row paste table — exactly the
          existing one — labeled clearly as fallback.

     This honors D-04 (live path is API-only — no paste table appears in the
     live narration) AND D-02 (the paste-driven flow exists when AOS8 is
     unreachable). The table is NOT deleted from the file; it is moved into
     a sub-section labeled as the unreachable-fallback. -->
</resolution_for_d04_d02_contradiction>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Stage -1 detection block + extend frontmatter tools list</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md</files>
  <read_first>
    - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md (full 487 lines — must see current frontmatter at lines 1-24, current Stage 0 at lines 54-72, AOS6/IAP sections at 105-134 to know what to leave alone)
    - hpe-networking-mcp/src/hpe_networking_mcp/skills/infrastructure-health-check.md (the `health()` detect-and-branch pattern — Step 1 procedure)
    - .planning/phases/10-live-detection-collection/10-CONTEXT.md (D-01, D-02, D-03 — the locked detection decisions)
  </read_first>
  <action>
    Two edits to `hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md`:

    **EDIT 1 — Frontmatter `tools:` list (line 23).**

    Current line 23 (verified):
    ```
    tools: [health, central_get_scope_tree, central_get_devices, central_get_aps, central_get_sites, central_get_site_name_id_mapping, central_recommend_firmware, central_get_config_assignments, central_get_server_groups, central_get_wlan_profiles, central_get_roles, central_get_named_vlans, clearpass_get_network_devices, clearpass_get_device_groups, clearpass_get_server_certificates, clearpass_get_local_users, greenlake_get_subscriptions, greenlake_get_workspace, greenlake_get_devices]
    ```

    Replace line 23 with (append the 9 AOS8 tools at the end of the list, before the closing bracket):
    ```
    tools: [health, central_get_scope_tree, central_get_devices, central_get_aps, central_get_sites, central_get_site_name_id_mapping, central_recommend_firmware, central_get_config_assignments, central_get_server_groups, central_get_wlan_profiles, central_get_roles, central_get_named_vlans, clearpass_get_network_devices, clearpass_get_device_groups, clearpass_get_server_certificates, clearpass_get_local_users, greenlake_get_subscriptions, greenlake_get_workspace, greenlake_get_devices, aos8_get_md_hierarchy, aos8_get_effective_config, aos8_get_ap_database, aos8_get_cluster_state, aos8_show_command, aos8_get_clients, aos8_get_bss_table, aos8_get_active_aps, aos8_get_ap_wired_ports]
    ```

    DO NOT modify the `platforms:` field (line 21) — per 10-RESEARCH.md Open
    Question 2, `platforms: [central, aos8]` is QUALITY-02 work assigned to
    Phase 13. Leave `platforms: [central]` as-is.

    DO NOT modify any other frontmatter field (`name`, `title`, `description`,
    `tags`).

    **EDIT 2 — Insert a new `Stage -1` block immediately before the existing
    `### Stage 0 — Operator interview` heading (currently line 54).**

    Insert this block, exactly, between the line `## Procedure — 6 stages, ~50 granular checks` (line 52) and the next heading:

    ```markdown
    ### Stage -1 — Session-start AOS8 detection (DETECT-01)

    Before beginning the Stage 0 operator interview, call `health()` once and
    inspect the per-platform status.

    **If `aos8.status == "ok"`** (AOS8 platform is configured and reachable):
    > Announce to the operator, verbatim:
    > **"AOS8 API mode — live data. Stage 1 collection will run via API; no CLI paste required for the AOS8 source path."**
    >
    > Then proceed to Stage 0. The Stage 1 AOS8 section will use the live-mode sub-path (API calls in four grouped batches — COLLECT-01..04). The paste table is not used unless an individual API batch fails.

    **Otherwise** (AOS8 not configured, status is `unavailable` / `degraded` /
    not present, OR no AOS8 secrets are mounted):
    > Proceed silently to Stage 0. Make no announcement. The paste-driven flow
    > is unchanged for AOS6 sessions, IAP sessions, and AOS8 sessions where
    > the platform is unreachable. Stage 1 will use the paste-fallback
    > sub-path for the chosen source.

    This detection step never blocks the audit — failures and unreachables
    silently fall through to the existing paste flow.

    ```

    Notes:
    - Use the literal heading text `### Stage -1 — Session-start AOS8 detection (DETECT-01)` so the requirement ID is greppable in the file.
    - The insertion point is between the section divider `## Procedure — 6 stages, ~50 granular checks` and `### Stage 0 — Operator interview (mandatory before data collection)`. Preserve the surrounding blank lines.
    - Do NOT modify the `## Procedure — 6 stages` heading text. (The "6 stages" wording becomes slightly inaccurate with the new Stage -1 — that's intentional and acceptable; renaming the procedure header is out of scope and would touch unrelated lines.)
    - Do NOT touch the AOS6 (~lines 105-118) or IAP (~lines 119-134) sections in this task. Those edits would be regressions.

    After editing, run from `hpe-networking-mcp/`:
    `uv run pytest tests/unit/test_skill_tool_references.py tests/unit/test_skill_aos8_live_detection.py -x -q`
    Both test files (created/updated in Plan 01) must exit 0.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py tests/unit/test_skill_aos8_live_detection.py tests/unit/test_skills.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File `hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` exists
    - `grep -c "aos8_get_md_hierarchy" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns >= 1 (frontmatter)
    - `grep -c "aos8_get_ap_wired_ports" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns >= 1 (frontmatter)
    - `grep -c "aos8_get_effective_config\|aos8_get_ap_database\|aos8_get_cluster_state\|aos8_show_command\|aos8_get_clients\|aos8_get_bss_table\|aos8_get_active_aps" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns >= 7
    - File contains the literal string `Stage -1 — Session-start AOS8 detection (DETECT-01)`
    - File contains the literal string `AOS8 API mode — live data`
    - File contains the literal string `proceed silently to Stage 0` (case-insensitive OK — verifies D-02 silent fallback)
    - File still contains literal string `platforms: [central]` (NOT `[central, aos8]` — QUALITY-02 deferred to Phase 13)
    - `pytest tests/unit/test_skill_tool_references.py tests/unit/test_skill_aos8_live_detection.py tests/unit/test_skills.py -x -q` exits 0
    - File line count <= 510 (within 500-line conventional cap with small headroom — Stage -1 adds ~20 lines; Task 2 will add more)
  </acceptance_criteria>
  <done>Frontmatter lists all 9 AOS8 tools. Stage -1 detection block exists before Stage 0. Detection regression tests pass. AOS6/IAP sections untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Replace AOS8 Stage 1 paste section with live-mode + paste-fallback sub-paths</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md</files>
  <read_first>
    - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md (full file — Task 1's edits already applied; need to see current AOS8 Stage 1 block at ~lines 80-104, AOS6 block at 105-118, IAP block at 119-134, all to remain untouched except the AOS8 sub-section)
    - .planning/phases/10-live-detection-collection/10-CONTEXT.md (D-04..D-09 — collection batches and partial-failure policy)
    - .planning/phases/10-live-detection-collection/10-RESEARCH.md (Pitfalls 4 and 5 — `aos8_get_effective_config` is object-scoped, `aos8_get_ap_wired_ports` is per-AP)
  </read_first>
  <action>
    Single edit to the AOS8 Stage 1 section of
    `hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md`.

    **Boundaries of the edit:**
    - START: line beginning with `#### If source = \`aos8\` (anchor: VSG §1671-§1873)` (currently line 80)
    - END: the line immediately before `#### If source = \`aos6\` (anchor: VSG §225-§588)` (currently line 105)
    - Verify the AOS6 heading and everything below it remains UNCHANGED byte-for-byte.

    **Replace the entire AOS8 sub-section (heading + 16-row table + the
    "Operator also captures..." paragraph) with the block below.**

    Block to insert (verbatim):

    ```markdown
    #### If source = `aos8` (anchor: VSG §1671-§1873)

    Two sub-paths — chosen by Stage -1's detection result.

    ##### AOS8 live-mode sub-path — used when Stage -1 announced "AOS8 API mode"

    No CLI paste required. The audit calls AOS8 tools directly and narrates
    progress in **four grouped batches**, each mapping to one COLLECT-* requirement.
    On any tool error the skill falls back per-batch to an exact-CLI paste request
    for that batch only — collection in other batches is preserved.

    **Batch 1 — MD hierarchy + per-node effective config (COLLECT-01)**

    > "Retrieving MD hierarchy and per-node effective configuration..."

    1. Call `aos8_get_md_hierarchy()`. Expected: tree of `/md`, `/md/<region>`, `/md/<region>/<site>`, `/md/<region>/<site>/<ap-group>` nodes — replaces `show configuration node-hierarchy` (VSG §1834-§1835).
    2. For each migration-relevant config object type — `ap_sys_prof`, `virtual_ap`, `ssid_prof`, `aaa_prof`, `aaa_server_group`, `wlan_ssid_profile`, `reg_domain_profile`, `arm_profile`, `dot11a_radio_prof`, `dot11g_radio_prof` — call `aos8_get_effective_config(object_name="<type>", config_path="/md")`. The tool is **object-scoped** (one named object per call), not a full config dump. Iterate the list above; the structured result feeds Stage 3 rule checks.
    3. As a complementary text capture for the report, also call `aos8_show_command(command="show configuration effective detail")`.

    *On batch 1 error:* tell the operator: *"API call to retrieve the MD hierarchy / effective config failed. Please run `show configuration node-hierarchy` and `show configuration effective detail <node>` (per node) on the Mobility Conductor and paste the output."* Then continue to Batch 2.

    **Batch 2 — Full AP inventory (COLLECT-02)**

    > "Retrieving the full AP inventory..."

    Call `aos8_get_ap_database()`. Expected: per-AP `model`, `mac`, `serial`, `ip_mode` (DHCP / static), `group`, `ap_name`, `ip` — replaces `show ap database long` (VSG §1740, §1851-§1852). Stage 3 RULES-04 (static AP IP) will reuse this `ip_mode` field directly.

    *On batch 2 error:* tell the operator: *"API call to retrieve the AP database failed. Please run `show ap database long` on the Mobility Conductor and paste the output."* Then continue to Batch 3.

    **Batch 3 — Cluster state + running-config + local-user db + inventory + ports (COLLECT-03)**

    > "Retrieving cluster state, running configuration, local users, and switch port state..."

    1. Call `aos8_get_cluster_state()`. Expected: cluster L2 / L3 status — replaces `show lc-cluster group-membership` (VSG §2399). Stage 5 CUTOVER-01 will reuse this for the L2-connected pre-flight check.
    2. Call `aos8_show_command(command="show running-config")`. Captures controller firmware version, RADIUS server groups, NAS-IP, VRRP VIP, AP system profiles, captive portal, user roles, ACLs (VSG §1815).
    3. Call `aos8_show_command(command="show local-user db")`. Captures local-user count for Stage 3 RULES-03 cross-check against ClearPass.
    4. Call `aos8_show_command(command="show inventory")`. Captures controller serial / MAC / model.
    5. Call `aos8_show_command(command="show port status")` and `aos8_show_command(command="show trunk")`. Captures switch-side port state and VLAN trunking for AP-uplink baseline (VSG §1881-§1884).

    *On batch 3 error:* identify which step(s) failed and ask the operator for **only the failed CLI commands**, by name (e.g., *"`show running-config` and `show local-user db` failed via API. Please run those two commands and paste the output."*). Do not reprint the full table; do not include VSG anchors in the paste request. Then continue to Batch 4.

    **Batch 4 — Client baseline + BSS/SSID table + active APs + AP wired ports (COLLECT-04)**

    > "Retrieving the live client baseline, SSID table, active AP RF state, and AP-uplink LLDP data..."

    1. Call `aos8_get_clients()`. Expected: aggregate counts (clients-by-SSID, total clients, users-with-roles) — replaces `show ap association` + `show user-table` (VSG §1858-§1861). Used as the post-cutover client-count baseline.
    2. Call `aos8_get_bss_table()`. Expected: SSID name + AP-broadcasting count per SSID + radio band — replaces `show ap essid` (VSG §1864-§1865).
    3. Call `aos8_get_active_aps()`. Expected: active AP list with channel / TX power / client count — replaces `show ap active` (VSG §1866-§1867).
    4. For each AP from Batch 2's `ap_database` (or a representative sample if the fleet is large), call `aos8_get_ap_wired_ports(ap_name="<ap_name>")`. The tool is **per-AP**, not bulk — iterate. Replaces `show ap lldp neighbors` (VSG §1862-§1864). Use this for the cutover wiring reference.

    *On batch 4 error:* identify which call(s) failed and ask the operator: *"`show ap association` / `show user-table` / `show ap essid` / `show ap active` / `show ap lldp neighbors` failed via API — please run the failed command(s) and paste the output."* Continue to Stage 2.

    After all four batches complete (live or hybrid), present a compiled
    Stage 1 inventory table to the operator (AP count, controller list, cluster
    state, local-user count, SSID count) before transitioning to Stage 2 parsing.

    ##### AOS8 paste-fallback sub-path — used when Stage -1 was silent (AOS8 unreachable or not configured)

    The skill is in paste mode for the AOS8 source. Use the same 16-command
    bundle the operator was using before v1.1; the API path is unavailable.

    | # | Where | Command | Purpose (VSG anchor) |
    |---|---|---|---|
    | 1 | Mobility Conductor | `show configuration node-hierarchy` | Plan AOS 10 Site / Site Collection / Device Group structure (§1834-§1835) |
    | 2 | Mobility Conductor | `show configuration committed <node>` (per node from #1) | What's committed at each node (§1847-§1850) |
    | 3 | Mobility Conductor | `show configuration effective detail <node>` (per node from #1) | Resolved/inherited config at each node (§1847-§1850) |
    | 4 | Mobility Conductor | `show ap database long` | AP inventory: model, MAC, serial (§1740, §1851-§1852) |
    | 5 | Mobility Controller (each) | `show ap-group` then `show ap-group <group_name>` per group | AP-group → VAP / RF-profile mapping (§1756-§1761) |
    | 6 | Mobility Controller (each) | `show ap association` and `show user-table` | Active client baseline for post-cutover diff (§1769-§1770) |
    | 7 | Mobility Controller (each) | `show ap lldp neighbors` | Switch / port per AP (cutover troubleshooting reference) (§1787) |
    | 8 | Mobility Controller (each) | `show ap essid` | Active SSID + AP-per-SSID counts (§1796) |
    | 9 | Mobility Controller (each) | `show ap active` | Active AP list + radio statistics (§1806) |
    | 10 | Mobility Controller (each) | `show running-config` | Full config: firmware version, RADIUS server groups, NAS-IP value, VRRP VIP, auth servers (§1815) |
    | 11 | Mobility Controller (each) | `show port status` | Identify active ports (§1881-§1882) |
    | 12 | Mobility Controller (each) | `show trunk` | VLAN/port assignments (§1883-§1884) |
    | 13 | Mobility Controller (each) | `show controller-ip` | Controller management IP + VLAN (§1885-§1886) |
    | 14 | Mobility Controller (each) | `show local-user db` | Local users (need to be re-homed in AOS 10 — VSG §1898-§1899) |
    | 15 | Mobility Controller (each) | `show inventory` | Serial + MAC of controller (§1900-§1901) |
    | 16 | Mobility Controller (each) | `show lc-cluster group-membership` | Cluster L2/L3 status — required pre-flight before single-controller upgrade (§2399) |

    Operator also captures, if available:
    - AirWave Group + Site structure (VSG §1820-§1827, cross-validation against CLI)
    - Configuration backup (TAR/snapshot) of each Mobility Controller per VSG §2435 — *"Always perform a backup and save a copy of the appliance configuration prior to any upgrade."*

    ```

    **Critical: do not touch any line outside the AOS8 sub-section.**
    Verify after the edit:
    - `grep -n "If source = \`aos6\`" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns exactly one hit, anchored on a single line.
    - `grep -n "If source = \`iap\`" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns exactly one hit.
    - The AOS6 sub-section content (3 paragraphs about AOS6 standalone Conductor / Conductor-Member / AP Override / HA pattern naming + AirWave bullets) is byte-identical to the pre-edit baseline.
    - The IAP sub-section's 10-row command table is byte-identical to the pre-edit baseline.

    Per the project file-size rule (CLAUDE.md: max 500 lines per file), the
    rewritten file may slightly exceed 500 lines. After the edit, run
    `wc -l hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md`.
    If the count exceeds 530, trim verbose VSG anchor commentary in the new
    live-mode block (NOT in the paste-fallback table — that must stay byte-equal
    to the original) until the file is <= 530 lines. Document the exception in
    the SUMMARY for this plan.

    Final test run from `hpe-networking-mcp/`:
    `uv run pytest tests/unit/test_skill_tool_references.py tests/unit/test_skill_aos8_live_detection.py tests/unit/test_skills.py -x -q`
    All three test files must exit 0.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py tests/unit/test_skill_aos8_live_detection.py tests/unit/test_skills.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File contains the literal heading `##### AOS8 live-mode sub-path — used when Stage -1 announced "AOS8 API mode"`
    - File contains the literal heading `##### AOS8 paste-fallback sub-path — used when Stage -1 was silent (AOS8 unreachable or not configured)`
    - File contains all 4 batch headings: `**Batch 1 — MD hierarchy + per-node effective config (COLLECT-01)**`, `**Batch 2 — Full AP inventory (COLLECT-02)**`, `**Batch 3 — Cluster state + running-config + local-user db + inventory + ports (COLLECT-03)**`, `**Batch 4 — Client baseline + BSS/SSID table + active APs + AP wired ports (COLLECT-04)**`
    - Each of the 9 AOS8 tools appears at least once in the skill body (in addition to frontmatter): `grep -c "\baos8_get_md_hierarchy\b" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` >= 2 (1 frontmatter + 1+ body); same for the other 8 names
    - File still contains literal string `If source = \`aos6\`` (AOS6 section preserved)
    - File still contains literal string `If source = \`iap\`` (IAP section preserved)
    - File still contains literal string `IAP cluster Virtual Controller` (IAP table preserved)
    - File contains the 16-row paste-fallback table — verified by grepping the unique CLI commands: `grep -c "show lc-cluster group-membership\|show ap database long\|show configuration node-hierarchy\|show controller-ip" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` >= 4
    - File line count <= 530 (`wc -l` output)
    - `pytest tests/unit/test_skill_tool_references.py tests/unit/test_skill_aos8_live_detection.py tests/unit/test_skills.py -x -q` exits 0
    - `pytest tests/unit/ -x -q` (full unit suite) exits 0 — no regression in any other test
  </acceptance_criteria>
  <done>AOS8 Stage 1 has live-mode (4 batches) + paste-fallback sub-paths. All 9 AOS8 tools referenced in skill body. AOS6 and IAP sections byte-unchanged. All unit tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator-mode smoke test (manual — DETECT-01 announcement)</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md</files>
  <read_first>
    - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md (post-Task-2 state — what the AI is reading at runtime)
    - hpe-networking-mcp/src/hpe_networking_mcp/config.py (to identify exact AOS8 secret file names for Scenario A setup)
  </read_first>
  <action>
    Pause for manual operator verification. The two LLM-prose behaviors required
    by DETECT-01 (announce when reachable, silent when not) cannot be unit-tested
    statically — they require running the skill against a real MCP client and
    observing the AI’s first message. Operator runs the three scenarios in
    the how-to-verify section below and reports back via resume-signal.
  </action>
  <verify>
    <automated>echo "manual-only — DETECT-01 LLM-prose behavior; see how-to-verify scenarios A/B/C"</automated>
  </verify>
  <acceptance_criteria>
    - Scenario A: AI emits the announcement (or close paraphrase containing "AOS8 API mode" and "live data") before any Stage 0 question
    - Scenario B: AI does NOT mention "AOS8 API mode" anywhere in its first response; proceeds directly to Stage 0 question 1
    - Scenario C: AI presents the AOS6 OR IAP paste table verbatim from the file (operator confirms by spot-checking 2-3 command names against `git show HEAD~:hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md`)
    - Operator returns "approved" via resume-signal; any failure description is logged in the plan SUMMARY for revision
  </acceptance_criteria>
  <done>All three scenarios pass; operator approval received via resume-signal.</done>
  <what-built>
    The rewritten `aos-migration-readiness` skill now has a Stage -1 detection
    block that calls `health()` at session start. When AOS8 is reachable, it
    announces "AOS8 API mode — live data" before Stage 0. When AOS8 is not
    configured, it proceeds silently. The 9 required AOS8 tools are wired into
    Stage 1's four-batch live-mode path. AOS6 and IAP paste paths are unchanged.

    Automated tests already passed: `test_skill_tool_references.py` (regex
    enforcement) and `test_skill_aos8_live_detection.py` (parametrized presence
    of all 9 AOS8 tools) — meaning every name in the skill body and frontmatter
    is a real catalog entry. What can't be automated is the LLM's behavior
    when the skill loads — that's prose interpretation, manual-only per
    10-VALIDATION.md.
  </what-built>
  <how-to-verify>
    Two scenarios. Run each, check the announcement vs. silence behavior.

    **Scenario A — AOS8 reachable (live-mode path expected):**
    1. Ensure AOS8 secrets are present in `secrets/` (or wherever `SECRETS_DIR` points): `aos8_host`, `aos8_username`, `aos8_password` (or whatever the v1.0 platform expects — check `hpe-networking-mcp/src/hpe_networking_mcp/config.py` for the expected secret file names).
    2. Start the server: from `hpe-networking-mcp/`, run `SECRETS_DIR=./secrets uv run python -m hpe_networking_mcp` (stderr will show platform init).
    3. From an MCP client (Claude Desktop or `mcp` CLI), call `skills_load(name="aos-migration-readiness")`.
    4. Without doing anything else, ask the AI: *"I want to do an AOS8 to AOS10 migration readiness audit."*
    5. **Expect:** the AI's first response includes the announcement *"AOS8 API mode — live data."* (or close paraphrase) BEFORE asking any Stage 0 interview questions.
    6. Then proceed through Stage 0 normally; reach Stage 1 and confirm the AI is calling AOS8 tools (not asking for paste).

    **Scenario B — AOS8 NOT configured (silent paste-mode path expected):**
    1. Stop the server. Remove or rename the AOS8 secret files so the platform initializes as `unavailable`.
    2. Restart the server.
    3. Same skills_load + same audit prompt.
    4. **Expect:** the AI proceeds directly to Stage 0 interview with NO mention of "AOS8 API mode" or "live data". Stage 1 prompts for paste exactly as it did in v1.0.

    **Scenario C — AOS6/IAP regression check:**
    1. With AOS8 either configured or not (doesn't matter for this scenario), pick `aos6` or `iap` as the source in Stage 0.
    2. **Expect:** Stage 1 prompts for the AOS6 or IAP paste table exactly as before. Verify a few command names against the pre-v1.1 wording (`show running-config`, `show clients`, etc.) — content should be byte-identical.

    Confirm the three scenarios behave as expected. If Scenario B produces an
    accidental announcement, that's a DETECT-01 D-02 violation. If Scenario A
    skips the announcement, that's a DETECT-01 violation. If Scenario C
    differs from v1.0 wording, that's an AOS6/IAP regression.
  </how-to-verify>
  <resume-signal>Type "approved" if all three scenarios pass. If any fail, describe which scenario, the actual vs. expected behavior, and the relevant skill prose so it can be revised.</resume-signal>
</task>

</tasks>

<verification>
- All Plan 01 tests still pass
- `cd hpe-networking-mcp && uv run pytest tests/unit/ -x -q` exits 0 (no regressions)
- `cd hpe-networking-mcp && wc -l src/hpe_networking_mcp/skills/aos-migration-readiness.md` <= 530
- `git diff hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` shows changes ONLY in: line 23 (frontmatter tools list), the inserted Stage -1 block, and the AOS8 sub-section. Zero changed lines in AOS6 or IAP sub-sections.
- Manual smoke test (Task 3) approved
</verification>

<success_criteria>
- DETECT-01: skill announces live-data mode when AOS8 reachable; silent otherwise
- COLLECT-01..04: each batch's tool calls are present in skill prose with the correct tool names and arguments
- AOS6 and IAP sub-sections preserved byte-for-byte (no regression)
- Frontmatter contains all 9 AOS8 tools; both Plan 01 tests pass
- File still parses as valid skill (covered by `test_skills.py` if present, else server startup smoke)
</success_criteria>

<output>
After completion, create `.planning/phases/10-live-detection-collection/10-02-SUMMARY.md`
with: exact line ranges modified, final file line count, manual smoke test result
(approved-by + scenarios verified), and any deviation from the plan (e.g., trimming
performed for the 530-line cap).
</output>
