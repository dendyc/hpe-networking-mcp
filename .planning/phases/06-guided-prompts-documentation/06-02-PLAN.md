---
phase: 06-guided-prompts-documentation
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - hpe-networking-mcp/INSTRUCTIONS.md
  - hpe-networking-mcp/README.md
  - hpe-networking-mcp/docs/TOOLS.md
  - hpe-networking-mcp/CHANGELOG.md
  - hpe-networking-mcp/pyproject.toml
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05

must_haves:
  truths:
    - "An operator can read repo-root INSTRUCTIONS.md and learn AOS8 config_path semantics, the write_memory contract, the show_command passthrough behavior, Conductor-vs-standalone differences, and the index of all 9 guided prompts."
    - "An operator scanning README.md sees an AOS8 row in the platform capability table, an AOS8 secrets reference section, and an updated tool count reflecting 38 AOS8 tools + 9 prompts."
    - "An operator scanning docs/TOOLS.md sees the AOS8 tool reference section listing all 38 tools (across health/clients/alerts/wlan/troubleshooting/writes) and the 9 prompts."
    - "CHANGELOG.md has a 2.4.0.0 entry describing the AOS8 platform addition."
    - "pyproject.toml version field equals 2.4.0.0."
  artifacts:
    - path: "hpe-networking-mcp/INSTRUCTIONS.md"
      provides: "Operator-facing AOS8 documentation (NEW file at repo root, distinct from src/hpe_networking_mcp/INSTRUCTIONS.md)"
      contains: "config_path"
    - path: "hpe-networking-mcp/README.md"
      provides: "Updated capability table, AOS8 secrets section, updated auto-disable example, updated tool counts"
      contains: "AOS8"
    - path: "hpe-networking-mcp/docs/TOOLS.md"
      provides: "AOS8 tool reference section after Axis"
      contains: "## Aruba OS 8"
    - path: "hpe-networking-mcp/CHANGELOG.md"
      provides: "2.4.0.0 release notes"
      contains: "[2.4.0.0]"
    - path: "hpe-networking-mcp/pyproject.toml"
      provides: "Version bumped from 2.3.0.1 to 2.4.0.0"
      contains: 'version = "2.4.0.0"'
  key_links:
    - from: "hpe-networking-mcp/INSTRUCTIONS.md"
      to: "hpe-networking-mcp/src/hpe_networking_mcp/INSTRUCTIONS.md"
      via: "explicit one-line cross-reference at top of new file (Pitfall 3 mitigation)"
      pattern: "src/hpe_networking_mcp/INSTRUCTIONS.md"
    - from: "hpe-networking-mcp/README.md"
      to: "hpe-networking-mcp/INSTRUCTIONS.md"
      via: "AOS8 section links to new INSTRUCTIONS.md for deeper guidance"
      pattern: "INSTRUCTIONS.md"
    - from: "hpe-networking-mcp/docs/TOOLS.md"
      to: "9 AOS8 prompts listed by name"
      via: "Aruba OS 8 section subheading 'Guided Prompts'"
      pattern: "aos8_triage_client"
---

<objective>
Implement Phase 6 Plan 2: Update / create all user-facing documentation reflecting the AOS8 platform addition.

Purpose: Satisfy DOCS-01..05 — make AOS8 discoverable, usable, and properly versioned for downstream operators and the project CLAUDE.md Documentation Checklist (which mandates README + CHANGELOG + INSTRUCTIONS + docs/TOOLS.md + pyproject.toml updates IN THE SAME PR as the code change).

Output:
- New repo-root `INSTRUCTIONS.md` (operator-facing — distinct from in-package AI-facing one)
- Updated `README.md` capability table, secrets reference, auto-disable example, tool counts
- Updated `docs/TOOLS.md` with AOS8 section after Axis
- Updated `CHANGELOG.md` with 2.4.0.0 entry
- Updated `pyproject.toml` version 2.3.0.1 → 2.4.0.0
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-guided-prompts-documentation/06-CONTEXT.md
@.planning/phases/06-guided-prompts-documentation/06-RESEARCH.md
@hpe-networking-mcp/README.md
@hpe-networking-mcp/CHANGELOG.md
@hpe-networking-mcp/docs/TOOLS.md
@hpe-networking-mcp/pyproject.toml
@hpe-networking-mcp/src/hpe_networking_mcp/INSTRUCTIONS.md
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/prompts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create new repo-root INSTRUCTIONS.md operator-facing doc</name>
  <files>hpe-networking-mcp/INSTRUCTIONS.md</files>
  <read_first>
    - hpe-networking-mcp/src/hpe_networking_mcp/INSTRUCTIONS.md (existing AI-facing in-package file — DO NOT EDIT, but read to ensure no duplication and to add cross-reference link)
    - hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/prompts.py (created in Plan 06-01 — source of truth for the 9 prompt names + signatures listed in the prompt index section)
    - hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py (TOOLS dict — confirm tool name spelling for show_command, write_memory references)
    - .planning/phases/06-guided-prompts-documentation/06-CONTEXT.md (D-06 — required sections; D-03 — prompt parameter signatures)
    - .planning/phases/06-guided-prompts-documentation/06-RESEARCH.md (Pattern 4 — recommended section order; Pitfall 3 — repo-root vs in-package distinction)
  </read_first>
  <action>
Create new file `hpe-networking-mcp/INSTRUCTIONS.md` (at repo root, alongside README.md — NOT inside src/). This is the operator-facing companion to the in-package AI-facing instructions. Use this exact section structure (per Pattern 4 from RESEARCH.md):

```markdown
# AOS8 / Mobility Conductor — Operator Instructions

> Operator-facing reference for the Aruba OS 8 / Mobility Conductor platform module.
> For the AI-facing system prompt that ships inside the container, see
> [src/hpe_networking_mcp/INSTRUCTIONS.md](src/hpe_networking_mcp/INSTRUCTIONS.md).

## Overview

The AOS8 module is the seventh platform exposed by `hpe-networking-mcp`. It surfaces 38
tools (8 health, 4 client, 3 alert, 4 WLAN, 7 troubleshooting, 12 write) plus 9 guided
prompts that drive an AI assistant through recognizable AOS8 operator workflows. See
[README.md](README.md) for setup, secrets, and platform enablement; this document
covers the AOS8-specific concepts an operator must understand to use the tools
correctly.

## Connecting to a Conductor vs. a Standalone Controller

The AOS8 module connects to a single AOS8 endpoint via HTTPS on the configured `aos8_port`
(default 4343). The endpoint can be either a **Mobility Conductor** (managing one or more
Managed Devices) or a **standalone controller** (no Conductor; manages its own APs).

| Topology | What `aos8_host` points at | What `config_path` looks like |
|---|---|---|
| Mobility Conductor + MDs | Conductor IP/hostname | `/md`, `/md/<MD-name>`, `/md/<AP-group>` |
| Standalone controller | Controller IP/hostname | `/mm/mynode` (the node's own scope) |

The AI uses the same tool surface for both topologies; only the `config_path` value
differs. Tools that require a `config_path` parameter accept either form.

## `config_path` Semantics

`config_path` is the AOS8 hierarchical scope identifier. It tells the Conductor *where*
a configuration object lives in the inheritance tree.

| Path | Meaning |
|---|---|
| `/md` | All Managed Devices under the Conductor |
| `/md/<MD-name>` | A specific Managed Device |
| `/md/<AP-group-name>` | A specific AP group beneath /md |
| `/mm/mynode` | The local node (used on standalone controllers) |

Inheritance flows top-down: an SSID profile defined at `/md` is visible to every MD;
the same name defined at `/md/<MD-name>` overrides it for that MD. Always pass the
**most specific** scope at which you want the change to take effect — never assume the
default.

Every write tool (WRITE-01..09) requires an explicit `config_path` parameter. There is
no default. This is intentional — operating against the wrong scope is the most common
self-inflicted AOS8 outage.

## The `aos8_write_memory` Contract

AOS8 distinguishes **running config** from **startup config**. Every write tool
(`aos8_manage_*`) modifies the running config and returns a response containing a
`requires_write_memory_for` field listing the `config_path` values whose changes need to
be persisted to startup config.

**`aos8_write_memory` is never called automatically.** It is always an explicit operator
action. The flow is:

1. Operator calls a write tool (e.g., `aos8_manage_ssid_profile`) with explicit `config_path`.
2. Response includes `requires_write_memory_for: ["/md/site-a"]` (for example).
3. Operator reviews the change and confirms the intent.
4. Operator explicitly calls `aos8_write_memory` with `config_path="/md/site-a"`.

This two-step contract exists to make staged-change rollback trivial (a controller
reboot reverts unsaved changes) and to prevent the AI from silently persisting changes
the operator has not approved. The `aos8_pre_change_check` guided prompt ends with a
reminder of this contract.

## Using `aos8_show_command`

`aos8_show_command` is a passthrough for any AOS8 CLI `show` command. The response is
structured JSON wherever the AOS8 API returns table-formatted data; the `_meta` field
(column-schema metadata) is stripped before the JSON is returned to the AI to keep the
payload focused.

Useful invocations:

| Command | Purpose |
|---|---|
| `show version` | Conductor + MD firmware versions |
| `show ap database` | Full AP inventory |
| `show user-table` | Active client list |
| `show configuration committed` | Current saved-to-startup config baseline |
| `show configuration pending` | Staged changes not yet persisted via write_memory |
| `show ap radio-summary` | Per-radio channel/power/utilization |

The command string is preserved as-typed (case-sensitive) — only the `show` prefix is
case-insensitive. Always include the `config_path` for commands that produce
scope-specific output.

## Guided Prompt Index

The AOS8 module ships with 9 guided prompts. Each is a multi-step workflow the AI will
follow when invoked, capturing tool outputs and producing a summary with recommended
next actions. MCP clients (e.g., Claude Desktop) surface these in the slash-command
menu.

| Prompt | Parameters | Workflow |
|---|---|---|
| `aos8_triage_client` | `mac_address: str` | Find a client, check AP health, review auth/association events, identify likely root cause |
| `aos8_triage_ap` | `ap_name: str` | Deep-dive an AP: radio state, clients, alarms, ARM history, event timeline |
| `aos8_health_check` | (none) | Network-wide health: controllers, AP counts, clients, alarms, firmware drift |
| `aos8_audit_change` | (none) | Recent audit-trail review with high-risk-change flagging |
| `aos8_rf_analysis` | `config_path: str = "/md"` | Channel distribution, co-channel clusters, ARM oscillation, interferers/rogues |
| `aos8_wlan_review` | (none) | SSID/VAP/AP-group/role inventory and consistency check |
| `aos8_client_flood` | `config_path: str = "/md"` | High-client-count / failed-connection investigation at a scope |
| `aos8_compare_md_config` | `md_path_1: str`, `md_path_2: str` | Side-by-side effective-config diff between two MDs or AP groups |
| `aos8_pre_change_check` | `config_path: str` | Pre-maintenance checklist: alarms, controller stats, audit trail, pending changes, write_memory reminder |

For SSID/VAP/AP-group/role/VLAN/AAA/ACL/netdestination changes — and especially before
any maintenance window — invoke `aos8_pre_change_check` first to capture the
GO/NO-GO baseline.
```

Save the file at `hpe-networking-mcp/INSTRUCTIONS.md`. Do not modify
`hpe-networking-mcp/src/hpe_networking_mcp/INSTRUCTIONS.md` — that is the AI-facing
file and lives at a different layer (Pitfall 3).
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && python -c "import pathlib; p = pathlib.Path('INSTRUCTIONS.md'); s = p.read_text(encoding='utf-8'); req = ['config_path', 'write_memory', 'show_command', 'Conductor', 'standalone', 'Guided Prompt Index', 'aos8_triage_client', 'aos8_triage_ap', 'aos8_health_check', 'aos8_audit_change', 'aos8_rf_analysis', 'aos8_wlan_review', 'aos8_client_flood', 'aos8_compare_md_config', 'aos8_pre_change_check', 'src/hpe_networking_mcp/INSTRUCTIONS.md']; missing = [r for r in req if r not in s]; assert not missing, missing; print('OK len=', len(s))"</automated>
  </verify>
  <acceptance_criteria>
    - File `hpe-networking-mcp/INSTRUCTIONS.md` exists (NEW file at repo root).
    - File `hpe-networking-mcp/src/hpe_networking_mcp/INSTRUCTIONS.md` was NOT modified (`git diff --name-only` does NOT list it).
    - File contains a section heading containing the literal `Connecting to a Conductor vs. a Standalone`.
    - File contains a section heading containing the literal `config_path`.
    - File contains a section heading containing the literal `write_memory`.
    - File contains a section heading containing the literal `aos8_show_command`.
    - File contains a section heading containing the literal `Guided Prompt Index`.
    - File contains all 9 prompt names: `aos8_triage_client`, `aos8_triage_ap`, `aos8_health_check`, `aos8_audit_change`, `aos8_rf_analysis`, `aos8_wlan_review`, `aos8_client_flood`, `aos8_compare_md_config`, `aos8_pre_change_check`.
    - File contains the cross-reference link to `src/hpe_networking_mcp/INSTRUCTIONS.md` (Pitfall 3 mitigation).
  </acceptance_criteria>
  <done>Operator-facing INSTRUCTIONS.md present at repo root; covers all 5 D-06 topics; in-package file untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md (capability table, secrets, auto-disable, tool counts) and docs/TOOLS.md (AOS8 section)</name>
  <files>hpe-networking-mcp/README.md, hpe-networking-mcp/docs/TOOLS.md</files>
  <read_first>
    - hpe-networking-mcp/README.md (current state — must locate every tool-count occurrence; grep for the existing per-platform count strings: "35 + 2 prompts", "73 + 12 prompts", existing Axis row, ASCII architecture diagram, capability table headers)
    - hpe-networking-mcp/docs/TOOLS.md (current state — must see how the existing platform sections are structured; mirror the Axis section layout for AOS8)
    - hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py (TOOLS dict — authoritative tool list per category)
    - hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/prompts.py (9 prompt names with signatures)
    - .planning/phases/06-guided-prompts-documentation/06-RESEARCH.md (Open Question 2 — README count math; Open Question 3 — Guided Prompts row should be ✅ for AOS8)
  </read_first>
  <action>
**Part A — README.md changes:**

1. **Capability table — add AOS8 column or row.** Locate the per-platform capability table. Add an AOS8 entry with:
   - Tool count cell: `38 + 9 prompts`
   - Guided Prompts row: `✅` (per RESEARCH.md Open Question 3)
   - Auto-disable: `✅` (same as other platforms — gracefully disables when secrets absent)
   - Write tools: `✅ (gated)` consistent with how Mist/Central/etc. are flagged
   - Mirror the cell values used by Mist/Central/Axis for every other capability row

2. **Tool-count strings — grep and update every occurrence.** Search README.md for:
   - "35 + 2 prompts" / "73 + 12 prompts" / per-platform counts in the capability footer
   - "default tool surface" sentence
   - ASCII architecture diagram tool counts
   Update each occurrence to include `AOS8: 38 + 9 prompts`. Update any aggregate total strings (e.g., "X total tools across N platforms") accordingly: total tools becomes prior_total + 38; total prompts becomes prior_total + 9; platform count becomes 7.

3. **Secrets reference section — add Aruba OS 8 subsection.** After the existing Axis secrets subsection, insert:

   ```markdown
   ### Aruba OS 8 / Mobility Conductor

   | Secret file | Required | Default | Purpose |
   |---|---|---|---|
   | `aos8_host` | yes | — | Conductor or standalone controller hostname/IP |
   | `aos8_username` | yes | — | API username with sufficient role |
   | `aos8_password` | yes | — | API password |
   | `aos8_port` | no | `4343` | HTTPS port (Mobility Conductor API port) |
   | `aos8_verify_ssl` | no | `true` | Set to `false` for self-signed certs (logged as WARNING) |

   Set `ENABLE_AOS8_WRITE_TOOLS=true` to expose the 12 AOS8 write tools (gated by elicitation middleware; default `false`).
   ```

4. **Auto-disable example — include AOS8.** Find the existing example showing platform auto-disable behavior (typically lists Mist/Central/etc.). Add AOS8 to the example list: e.g., "If the AOS8 secrets are absent, the AOS8 platform is auto-disabled and the server starts without it — no traceback."

5. **ASCII architecture diagram — add AOS8 box.** If README contains an ASCII diagram of the platform-module layout, add an `aos8` box alongside the others (mist / central / greenlake / clearpass / apstra / axis / **aos8**) with its `38 + 9 prompts` count.

**Part B — docs/TOOLS.md changes:**

After the existing Axis section (typically the last platform section in the file), append a new top-level section:

```markdown
## Aruba OS 8 / Mobility Conductor (38 tools + 9 prompts)

Tools are exposed in dynamic mode by default via 3 meta-tools (`aos8_list_tools`,
`aos8_get_tool_schema`, `aos8_invoke_tool`). Set `MCP_TOOL_MODE=static` to expose
each underlying tool individually. Write tools require `ENABLE_AOS8_WRITE_TOOLS=true`.

### Health & Inventory (8)

| Tool | Purpose |
|---|---|
| `aos8_get_controllers` | List all controllers/MDs under the Conductor with status and role |
| `aos8_get_ap_database` | Full AP database (name, MAC, IP, model, status, AP group) |
| `aos8_get_active_aps` | Currently up APs with association counts |
| `aos8_get_ap_detail` | Detailed stats for a single AP by name or MAC |
| `aos8_get_bss_table` | BSS table — radio/BSSID associations at a scope |
| `aos8_get_radio_summary` | Per-AP radio state: channel, power, utilization, noise floor |
| `aos8_get_version` | AOS8 software version on Conductor and MDs |
| `aos8_get_licenses` | Installed licenses and feature entitlements |

### Clients (4)

| Tool | Purpose |
|---|---|
| `aos8_get_clients` | List connected wireless clients at a config_path scope |
| `aos8_find_client` | Find client by MAC address, IP address, or username |
| `aos8_get_client_detail` | Full association/auth/connection info for a single client |
| `aos8_get_client_history` | Historical connection events for a client |

### Alerts & Audit (3)

| Tool | Purpose |
|---|---|
| `aos8_get_alarms` | Active alarms with severity, category, and timestamp |
| `aos8_get_audit_trail` | Configuration audit log (who changed what, when) |
| `aos8_get_events` | System event log filterable by type/severity/time |

### WLAN & Config (4)

| Tool | Purpose |
|---|---|
| `aos8_get_ssid_profiles` | List all SSID profiles with key settings |
| `aos8_get_virtual_aps` | Virtual AP profiles mapped to SSIDs and AP groups |
| `aos8_get_ap_groups` | AP group list with member APs and applied profiles |
| `aos8_get_user_roles` | Defined user roles and policy assignments |

### Troubleshooting (7)

| Tool | Purpose |
|---|---|
| `aos8_ping` | Ping from a controller to a target IP |
| `aos8_traceroute` | Traceroute from a controller to a target IP |
| `aos8_show_command` | Passthrough for any AOS8 CLI show command (`_meta` stripped) |
| `aos8_get_logs` | Recent system log entries filterable by severity |
| `aos8_get_controller_stats` | CPU, memory, uptime, session counts on a controller |
| `aos8_get_arm_history` | ARM channel-change and power-adjustment history |
| `aos8_get_rf_monitor` | RF monitor data: interference and rogue detections |

### Writes (12) — gated behind `ENABLE_AOS8_WRITE_TOOLS=true`

All write tools require an explicit `config_path` parameter (no default) and return a
`requires_write_memory_for` field listing the config_paths needing persistence via
`aos8_write_memory`.

| Tool | Tag | Purpose |
|---|---|---|
| `aos8_manage_ssid_profile` | `aos8_write` / `aos8_write_delete` | Create/update/delete SSID profile |
| `aos8_manage_virtual_ap` | `aos8_write` / `aos8_write_delete` | Create/update/delete virtual AP profile |
| `aos8_manage_ap_group` | `aos8_write` / `aos8_write_delete` | Create/update/delete AP group |
| `aos8_manage_user_role` | `aos8_write` / `aos8_write_delete` | Create/update/delete user role |
| `aos8_manage_vlan` | `aos8_write` / `aos8_write_delete` | Create/update/delete VLAN |
| `aos8_manage_aaa_server` | `aos8_write` / `aos8_write_delete` | Create/update/delete AAA server |
| `aos8_manage_aaa_server_group` | `aos8_write` / `aos8_write_delete` | Create/update/delete AAA server group |
| `aos8_manage_acl` | `aos8_write` / `aos8_write_delete` | Create/update/delete session ACL |
| `aos8_manage_netdestination` | `aos8_write` / `aos8_write_delete` | Create/update/delete network destination |
| `aos8_disconnect_client` | `aos8_write` | Force-disconnect a client by MAC |
| `aos8_reboot_ap` | `aos8_write` | Reboot a specific AP by name |
| `aos8_write_memory` | `aos8_write` | Persist staged config to startup config (explicit operator action only) |

### Guided Prompts (9)

| Prompt | Parameters | Workflow |
|---|---|---|
| `aos8_triage_client` | `mac_address: str` | Triage a client connectivity problem |
| `aos8_triage_ap` | `ap_name: str` | Deep-dive an AP's health |
| `aos8_health_check` | — | Network-wide health assessment |
| `aos8_audit_change` | — | Recent audit-trail review with risk flagging |
| `aos8_rf_analysis` | `config_path: str = "/md"` | RF environment report |
| `aos8_wlan_review` | — | SSID/VAP/AP-group/role consistency check |
| `aos8_client_flood` | `config_path: str = "/md"` | High client count / failed connection investigation |
| `aos8_compare_md_config` | `md_path_1: str`, `md_path_2: str` | Side-by-side effective-config diff |
| `aos8_pre_change_check` | `config_path: str` | Pre-maintenance checklist (ends with write_memory reminder) |

See [INSTRUCTIONS.md](../INSTRUCTIONS.md) for `config_path` semantics and the
`write_memory` contract.
```

If docs/TOOLS.md has a top-of-file overview/summary table that lists per-platform counts, also update that table to include an AOS8 row with `38 tools, 9 prompts`.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && python -c "import pathlib; r = pathlib.Path('README.md').read_text(encoding='utf-8'); t = pathlib.Path('docs/TOOLS.md').read_text(encoding='utf-8'); req_r = ['AOS8','aos8_host','38 + 9 prompts','ENABLE_AOS8_WRITE_TOOLS']; req_t = ['Aruba OS 8','aos8_get_controllers','aos8_manage_ssid_profile','aos8_write_memory','aos8_triage_client','aos8_pre_change_check']; mr=[x for x in req_r if x not in r]; mt=[x for x in req_t if x not in t]; assert not mr, ('README missing', mr); assert not mt, ('TOOLS missing', mt); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the literal string "AOS8" in at least one capability table cell.
    - README.md contains all five secret names: "aos8_host", "aos8_username", "aos8_password", "aos8_port", "aos8_verify_ssl".
    - README.md contains "ENABLE_AOS8_WRITE_TOOLS".
    - README.md contains the literal string "38 + 9 prompts" (AOS8 tool count cell).
    - README.md auto-disable example mentions AOS8 (grep "AOS8" near auto-disable / disabled wording).
    - docs/TOOLS.md contains a top-level section heading "## Aruba OS 8" (or equivalent with "Aruba OS 8" in the title).
    - docs/TOOLS.md lists all 38 AOS8 tools (grep each of the 38 tool names; at minimum verify aos8_get_controllers, aos8_manage_ssid_profile, aos8_write_memory present).
    - docs/TOOLS.md contains all 9 prompt names in the Guided Prompts subsection.
    - docs/TOOLS.md contains a link reference to ../INSTRUCTIONS.md.
    - The verify command above prints "OK".
  </acceptance_criteria>
  <done>README.md fully updated (capability row + secrets section + auto-disable + tool counts in every location); docs/TOOLS.md has complete AOS8 reference section; both files cross-link to repo-root INSTRUCTIONS.md.</done>
</task>

<task type="auto">
  <name>Task 3: Bump version to 2.4.0.0 in pyproject.toml and add CHANGELOG entry</name>
  <files>hpe-networking-mcp/pyproject.toml, hpe-networking-mcp/CHANGELOG.md</files>
  <read_first>
    - hpe-networking-mcp/pyproject.toml (current state — confirm version line is at line 7: version = "2.3.0.1")
    - hpe-networking-mcp/CHANGELOG.md (current state — must see the existing entry format, the most recent version block (2.3.0.1), and the Keep-a-Changelog section conventions like "### Added", "### Changed")
    - .planning/phases/06-guided-prompts-documentation/06-RESEARCH.md (Pitfall 5 — 4-segment versioning; recommends 2.4.0.0 as minor bump for new platform)
  </read_first>
  <action>
**Part A — pyproject.toml:**

In `hpe-networking-mcp/pyproject.toml`, change the version line:

From: `version = "2.3.0.1"`
To:   `version = "2.4.0.0"`

Do not modify any other line.

**Part B — CHANGELOG.md:**

Insert a new section at the top of CHANGELOG.md (immediately after any "# Changelog" / "## [Unreleased]" header but before the previous most-recent version entry, matching the style used for prior entries). Use today's date (2026-04-28). Use this exact content:

```markdown
## [2.4.0.0] - 2026-04-28

### Added
- **Aruba OS 8 / Mobility Conductor platform module** (seventh platform).
  - 38 tools across 6 categories: 8 health/inventory, 4 client, 3 alert/audit, 4 WLAN/config, 7 troubleshooting, 12 write
  - 9 guided prompts: aos8_triage_client, aos8_triage_ap, aos8_health_check, aos8_audit_change, aos8_rf_analysis, aos8_wlan_review, aos8_client_flood, aos8_compare_md_config, aos8_pre_change_check
  - Token-reusing UIDARUBA session client with single-flight 401 refresh, asyncio.Lock-serialized token rotation, lazy login (deferred to first tool call), and explicit aclose() that logs out on shutdown
  - Write tools gated behind `ENABLE_AOS8_WRITE_TOOLS` (default false); every write returns `requires_write_memory_for`
  - `aos8_write_memory` is the only path to persist staged config — never auto-called
  - SSL verification enabled by default; opt-out emits a startup WARNING
  - Five Docker secrets: `aos8_host`, `aos8_username`, `aos8_password`, `aos8_port` (default 4343), `aos8_verify_ssl`
- New repo-root **INSTRUCTIONS.md** — operator-facing documentation covering AOS8 config_path semantics, write_memory contract, show_command passthrough, Conductor-vs-standalone behavior, and the guided-prompt index. Distinct from the in-package AI-facing src/hpe_networking_mcp/INSTRUCTIONS.md.
- AOS8 tool reference section in **docs/TOOLS.md**.
- AOS8 row in README.md capability table; AOS8 secrets reference section; AOS8 added to platform auto-disable example.

### Changed
- README.md tool counts and architecture diagram updated to include AOS8 (38 + 9 prompts).
- Bumped version to 2.4.0.0 (minor — additive platform).

### Tests
- 11+ new unit tests in tests/unit/test_aos8_prompts.py covering prompt registration and non-empty return contract for all 9 PROMPT-01..09 prompts.
- Phase-5 baseline of 737 tests remains green; total now 748+ tests passing.
```

Preserve all existing CHANGELOG entries below the new block. Do not delete or rewrite the 2.3.0.1 entry or any prior entry.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && python -c "import pathlib; v = pathlib.Path('pyproject.toml').read_text(encoding='utf-8'); c = pathlib.Path('CHANGELOG.md').read_text(encoding='utf-8'); assert 'version = \"2.4.0.0\"' in v; assert '2.3.0.1' not in v; assert '[2.4.0.0]' in c; assert '2026-04-28' in c; assert 'Aruba OS 8' in c; assert '2.3.0.1' in c; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains exactly: `version = "2.4.0.0"`
    - pyproject.toml no longer contains the string `2.3.0.1`
    - CHANGELOG.md top entry contains the literal heading `## [2.4.0.0] - 2026-04-28`
    - CHANGELOG.md 2.4.0.0 block contains the literal phrase "Aruba OS 8"
    - CHANGELOG.md 2.4.0.0 block contains all 9 prompt names
    - CHANGELOG.md 2.4.0.0 block mentions "INSTRUCTIONS.md", "docs/TOOLS.md", and "ENABLE_AOS8_WRITE_TOOLS"
    - CHANGELOG.md still contains the prior 2.3.0.1 entry (no deletions of historical entries)
    - The verify command above prints "OK"
  </acceptance_criteria>
  <done>Version bumped to 2.4.0.0 in pyproject.toml; CHANGELOG.md has complete 2.4.0.0 entry referencing AOS8 platform addition; prior history preserved.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run end-to-end checks:

```
cd hpe-networking-mcp && python -c "import pathlib; assert 'version = \"2.4.0.0\"' in pathlib.Path('pyproject.toml').read_text(encoding='utf-8'); assert '[2.4.0.0]' in pathlib.Path('CHANGELOG.md').read_text(encoding='utf-8'); assert pathlib.Path('INSTRUCTIONS.md').exists(); assert 'AOS8' in pathlib.Path('README.md').read_text(encoding='utf-8'); assert 'Aruba OS 8' in pathlib.Path('docs/TOOLS.md').read_text(encoding='utf-8'); print('ALL DOCS OK')"
```

And re-run the full test suite to confirm no regressions from doc-only changes:

```
cd hpe-networking-mcp && uv run pytest tests/ -q
```

Expected: ">= 748 tests passed".
</verification>

<success_criteria>
- New repo-root INSTRUCTIONS.md exists; covers all 5 D-06 topics (config_path, write_memory, show_command, Conductor-vs-standalone, guided-prompt index)
- README.md has AOS8 row, AOS8 secrets section, updated auto-disable example, updated tool counts everywhere
- docs/TOOLS.md has AOS8 section with all 38 tools + 9 prompts listed
- CHANGELOG.md has 2.4.0.0 entry; pyproject.toml at 2.4.0.0
- In-package src/hpe_networking_mcp/INSTRUCTIONS.md untouched (Pitfall 3 honored)
- Project CLAUDE.md Documentation Checklist satisfied (all 5 doc files updated in this PR)
</success_criteria>

<output>
After completion, create `.planning/phases/06-guided-prompts-documentation/06-02-SUMMARY.md` documenting:
- All 5 files modified/created with brief description per file
- Confirmation in-package src/hpe_networking_mcp/INSTRUCTIONS.md was NOT modified
- Final tool/prompt count strings used in README/TOOLS.md
- Confirmation full pytest suite still green (>= 748 tests)
- Phase 6 complete — proceed to Phase 7 (Testing & Integration)
</output>
