---
phase: 03-read-tools
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/clients.py
autonomous: true
requirements: [READ-09, READ-10, READ-11, READ-12]

must_haves:
  truths:
    - "All 4 clients-category tools (READ-09..12) are implemented and decorated"
    - "test_aos8_read_clients.py is fully GREEN"
    - "find_client supports MAC, IP, and username selectors and rejects 'no selector'"
    - "get_client_history does NOT accept config_path (per CONTEXT D-05)"
  artifacts:
    - path: "hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/clients.py"
      provides: "4 AOS8 client read tools"
      contains: "aos8_find_client"
  key_links:
    - from: "hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/clients.py"
      to: "_helpers.run_show"
      via: "import + call"
      pattern: "from hpe_networking_mcp.platforms.aos8.tools._helpers import"
---

<objective>
Implement READ-09..12 in `tools/clients.py` per CONTEXT D-04 (config_path on READ-09..11) and D-05 (no config_path on READ-12).

Output: 1 new file `clients.py` (~190 lines per RESEARCH).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-read-tools/03-CONTEXT.md
@.planning/phases/03-read-tools/03-RESEARCH.md
@.planning/phases/03-read-tools/03-01-SUMMARY.md
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/_helpers.py
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/__init__.py
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/central/tools/clients.py
@hpe-networking-mcp/tests/unit/test_aos8_read_clients.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement aos8/tools/clients.py — READ-09..12</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/clients.py</files>
  <behavior>
    Per-tool contract (must satisfy `tests/unit/test_aos8_read_clients.py`):
    - READ-09 `aos8_get_clients(ctx, config_path: str = "/md")` → `run_show(client, "show user-table", config_path=config_path)`. Action verb: "list clients".
    - READ-10 `aos8_find_client(ctx, mac: str | None = None, ip: str | None = None, username: str | None = None, config_path: str = "/md")` → exactly one selector required. If 0 or >1 set, return string `"Must provide exactly one of mac, ip, or username"`. Else build command:
        - mac → `f"show user-table mac {mac}"`
        - ip → `f"show user-table ip {ip}"`
        - username → `f"show user-table name {username}"`
      Then `run_show(client, command, config_path=config_path)`. Action verb: "find client".
    - READ-11 `aos8_get_client_detail(ctx, mac: str, config_path: str = "/md")` → `run_show(client, f"show user-table verbose mac {mac}", config_path=config_path)`. Action verb: "fetch client detail".
    - READ-12 `aos8_get_client_history(ctx, mac: str)` → `run_show(client, f"show ap association history client-mac {mac}")`. NO config_path. Action verb: "fetch client history".
  </behavior>
  <read_first>
    hpe-networking-mcp/tests/unit/test_aos8_read_clients.py,
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/_helpers.py,
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/__init__.py,
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/_registry.py,
    .planning/phases/03-read-tools/03-RESEARCH.md
  </read_first>
  <action>
    Create `hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/clients.py`. Use the same imports and structure as Plan 02's `health.py`. For each of the 4 tools:
    1. `@tool(name="aos8_<exact>", annotations=READ_ONLY)` decorator.
    2. `async def`, first param `ctx: Context`, return type `dict[str, Any] | str`.
    3. Google-style docstring.
    4. Body wraps `await run_show(...)` in `try/except Exception as exc: return format_aos8_error(exc, "<verb>")` with `# noqa: BLE001`.
    5. For `aos8_find_client`, count non-None selectors via `sum(s is not None for s in (mac, ip, username))`; reject `!= 1` with the exact error string above.

    File MUST be ruff/mypy clean and under 500 lines.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_aos8_read_clients.py -x -q && uv run ruff check src/hpe_networking_mcp/platforms/aos8/tools/clients.py && uv run mypy src/hpe_networking_mcp/platforms/aos8/tools/clients.py</automated>
  </verify>
  <done>
    - `test_aos8_read_clients.py` fully green.
    - `grep -c "@tool(name=\"aos8_" hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/clients.py` returns 4.
    - Ruff + mypy clean.
    - Full suite still passes.
  </done>
</task>

</tasks>

<verification>
- `cd hpe-networking-mcp && uv run pytest tests/unit/ -q` — full suite green.
</verification>

<success_criteria>
4 tools (`aos8_get_clients`, `aos8_find_client`, `aos8_get_client_detail`, `aos8_get_client_history`) defined in clients.py; all related red tests now green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-read-tools/03-03-SUMMARY.md`.
</output>
