---
phase: 03-read-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hpe-networking-mcp/tests/conftest.py
  - hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/__init__.py
  - hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/_helpers.py
  - hpe-networking-mcp/tests/unit/fixtures/aos8/show_switches.json
  - hpe-networking-mcp/tests/unit/fixtures/aos8/show_ap_database.json
  - hpe-networking-mcp/tests/unit/fixtures/aos8/show_user_table.json
  - hpe-networking-mcp/tests/unit/fixtures/aos8/show_alarms.json
  - hpe-networking-mcp/tests/unit/fixtures/aos8/show_audit_trail.json
  - hpe-networking-mcp/tests/unit/fixtures/aos8/ssid_prof.json
  - hpe-networking-mcp/tests/unit/fixtures/aos8/show_log_system.json
  - hpe-networking-mcp/tests/unit/fixtures/aos8/show_user_table_verbose.json
  - hpe-networking-mcp/tests/unit/test_aos8_read_health.py
  - hpe-networking-mcp/tests/unit/test_aos8_read_clients.py
  - hpe-networking-mcp/tests/unit/test_aos8_read_alerts.py
  - hpe-networking-mcp/tests/unit/test_aos8_read_wlan.py
  - hpe-networking-mcp/tests/unit/test_aos8_read_troubleshooting.py
  - hpe-networking-mcp/tests/unit/test_aos8_init.py
autonomous: true
requirements: [READ-01, READ-02, READ-03, READ-04, READ-05, READ-06, READ-07, READ-08, READ-09, READ-10, READ-11, READ-12, READ-13, READ-14, READ-15, READ-16, READ-17, READ-18, READ-19, READ-20, READ-21, READ-22, READ-23, READ-24, READ-25, READ-26]

must_haves:
  truths:
    - "Test collection succeeds with aos8._registry stub installed"
    - "Failing red tests exist for all 26 READ tools across 5 category test files"
    - "JSON fixtures for representative show command + ssid_prof responses exist"
    - "Helpers module (strip_meta, run_show, get_object, format_aos8_error) exists"
    - "READ_ONLY annotations constant exported from aos8/tools/__init__.py"
  artifacts:
    - path: "hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/__init__.py"
      provides: "READ_ONLY ToolAnnotations constant for aos8 read tools"
    - path: "hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/_helpers.py"
      provides: "strip_meta, run_show, get_object, format_aos8_error helpers"
      contains: "def strip_meta"
    - path: "hpe-networking-mcp/tests/unit/test_aos8_read_health.py"
      provides: "Red tests for READ-01..08"
    - path: "hpe-networking-mcp/tests/unit/test_aos8_read_clients.py"
      provides: "Red tests for READ-09..12"
    - path: "hpe-networking-mcp/tests/unit/test_aos8_read_alerts.py"
      provides: "Red tests for READ-13..15"
    - path: "hpe-networking-mcp/tests/unit/test_aos8_read_wlan.py"
      provides: "Red tests for READ-16..19"
    - path: "hpe-networking-mcp/tests/unit/test_aos8_read_troubleshooting.py"
      provides: "Red tests for READ-20..26"
    - path: "hpe-networking-mcp/tests/unit/test_aos8_init.py"
      provides: "TOOLS dict + register_tools wiring tests"
  key_links:
    - from: "hpe-networking-mcp/tests/conftest.py"
      to: "hpe_networking_mcp.platforms.aos8._registry"
      via: "platform_registries tuple in _install_registry_stubs"
      pattern: "platforms\\.aos8\\._registry"
---

<objective>
Wave 0 scaffold for Phase 3 Read Tools. Per CONTEXT D-09 and RESEARCH Pitfall 5: write all failing red tests across all 26 read tools (5 category test files + init test), create JSON fixtures, create the shared `_helpers.py` module skeleton, create the `tools/__init__.py` with `READ_ONLY` annotations constant, and extend `tests/conftest.py` so `_install_registry_stubs()` includes `aos8._registry`. Implementation comes in Plans 02-06.

Purpose: Establish a green test-collection baseline with red implementation tests so subsequent category plans can flip individual tests green incrementally.

Output: 7 new test files (6 test modules + fixture dir), `_helpers.py`, `tools/__init__.py`, and 1 conftest.py edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-read-tools/03-CONTEXT.md
@.planning/phases/03-read-tools/03-RESEARCH.md
@.planning/phases/03-read-tools/03-VALIDATION.md
@hpe-networking-mcp/CLAUDE.md
@hpe-networking-mcp/tests/conftest.py
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/_registry.py
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/client.py
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/central/tools/__init__.py
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/central/tools/clients.py

<interfaces>
<!-- Critical contracts referenced by Wave 0 scaffolding -->

From hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/client.py:
```python
class AOS8APIError(Exception): ...
class AOS8AuthError(AOS8APIError): ...

class AOS8Client:
    async def request(
        self,
        method: Literal["GET", "POST"],
        path: str,
        *,
        params: dict[str, Any] | None = None,
        json_body: Any = None,
        data: Any = None,
        timeout: float | None = None,
    ) -> httpx.Response: ...
```

From hpe-networking-mcp/src/hpe_networking_mcp/platforms/central/tools/__init__.py (pattern to mirror):
```python
from mcp.types import ToolAnnotations

READ_ONLY = ToolAnnotations(
    readOnlyHint=True,
    destructiveHint=False,
    idempotentHint=True,
    openWorldHint=True,
)
```

From hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/_registry.py (decorator):
```python
def tool(**tool_kwargs) -> Callable: ...   # @tool(name="...", annotations=READ_ONLY)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend conftest registry stubs + create tools/__init__.py + _helpers.py</name>
  <files>
    hpe-networking-mcp/tests/conftest.py,
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/__init__.py,
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/_helpers.py
  </files>
  <read_first>
    hpe-networking-mcp/tests/conftest.py,
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/central/tools/__init__.py,
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/client.py,
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/_registry.py,
    .planning/phases/03-read-tools/03-RESEARCH.md
  </read_first>
  <action>
    1. Edit `hpe-networking-mcp/tests/conftest.py` — add `"hpe_networking_mcp.platforms.aos8._registry",` to the `platform_registries` tuple inside `_install_registry_stubs()` (currently lines 24-30). Insert in alphabetical order (after `apstra`, before `central`). Do NOT remove or change any other entry.

    2. Create `hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/__init__.py` with EXACTLY this content (matches central/tools/__init__.py pattern):
       ```python
       """AOS8 tool annotations and shared module init."""

       from mcp.types import ToolAnnotations

       READ_ONLY = ToolAnnotations(
           readOnlyHint=True,
           destructiveHint=False,
           idempotentHint=True,
           openWorldHint=True,
       )

       __all__ = ["READ_ONLY"]
       ```

    3. Create `hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/_helpers.py` with the four helpers per RESEARCH Pattern 2:
       - `def strip_meta(body: Any) -> Any` — when body is a dict, return new dict with keys `"_meta"` and `"_global_result"` removed; otherwise return body unchanged.
       - `async def run_show(client: AOS8Client, command: str, *, config_path: str | None = None) -> Any` — builds params `{"command": command}` plus `"config_path": config_path` when not None, calls `await client.request("GET", "/v1/configuration/showcommand", params=params)`, returns `strip_meta(response.json())`.
       - `async def get_object(client: AOS8Client, object_path: str, *, config_path: str = "/md") -> Any` — calls `await client.request("GET", f"/v1/configuration/object/{object_path}", params={"config_path": config_path})`, returns `strip_meta(response.json())`.
       - `def format_aos8_error(exc: BaseException, action: str) -> str` — render branches for `AOS8AuthError`, `AOS8APIError`, `httpx.HTTPStatusError` (include `exc.response.status_code`), `httpx.HTTPError`, fallback `Unexpected error`. Each returns the action verb in the message (e.g. `f"AOS8 API error while attempting to {action}: {exc}"`).
       - All functions have Google-style docstrings; module docstring at top; `from __future__ import annotations`; type hints throughout; double quotes; trailing commas on multi-line. Imports: `httpx`, `from typing import Any`, `from hpe_networking_mcp.platforms.aos8.client import AOS8APIError, AOS8AuthError, AOS8Client`. File MUST stay under 100 lines.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run python -c "from hpe_networking_mcp.platforms.aos8.tools import READ_ONLY; from hpe_networking_mcp.platforms.aos8.tools._helpers import strip_meta, run_show, get_object, format_aos8_error; assert strip_meta({'_meta':[1],'_global_result':{'s':'0'},'data':[{'a':1}]}) == {'data':[{'a':1}]}; print('OK')"</automated>
  </verify>
  <done>
    - `grep -n "platforms.aos8._registry" hpe-networking-mcp/tests/conftest.py` returns a hit inside `_install_registry_stubs`.
    - `hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/__init__.py` defines `READ_ONLY` and exports it.
    - `hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/_helpers.py` defines `strip_meta`, `run_show`, `get_object`, `format_aos8_error` (verifiable via the verify command).
    - `cd hpe-networking-mcp && uv run pytest tests/unit/ -q --collect-only > /dev/null` succeeds with no collection errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create JSON fixtures for representative AOS8 responses</name>
  <files>
    hpe-networking-mcp/tests/unit/fixtures/aos8/show_switches.json,
    hpe-networking-mcp/tests/unit/fixtures/aos8/show_ap_database.json,
    hpe-networking-mcp/tests/unit/fixtures/aos8/show_user_table.json,
    hpe-networking-mcp/tests/unit/fixtures/aos8/show_user_table_verbose.json,
    hpe-networking-mcp/tests/unit/fixtures/aos8/show_alarms.json,
    hpe-networking-mcp/tests/unit/fixtures/aos8/show_audit_trail.json,
    hpe-networking-mcp/tests/unit/fixtures/aos8/ssid_prof.json,
    hpe-networking-mcp/tests/unit/fixtures/aos8/show_log_system.json
  </files>
  <read_first>
    .planning/phases/03-read-tools/03-RESEARCH.md
  </read_first>
  <action>
    Create the `tests/unit/fixtures/aos8/` directory and write 8 JSON fixture files matching AOS8 showcommand response shape (per RESEARCH "Code Examples" — every showcommand response includes `_meta` and `_global_result`):

    1. `show_switches.json` — `{"_meta":["IP Address","Name","Role","Status","Version"],"_global_result":{"status":"0","status_str":"Success"},"Switch List":[{"IP Address":"10.0.0.1","Name":"MD-01","Role":"MD","Status":"up","Version":"8.10.0.0"},{"IP Address":"10.0.0.2","Name":"MM-01","Role":"master","Status":"up","Version":"8.10.0.0"}]}`

    2. `show_ap_database.json` — `{"_meta":["Name","Group","IP Address","Status","Flags","Switch IP","Standby IP","Wired MAC Address","Serial #","AP Type"],"_global_result":{"status":"0","status_str":"Success"},"AP Database":[{"Name":"AP-01","Group":"default","IP Address":"10.1.0.50","Status":"Up 1d:2h:3m","Flags":"","Switch IP":"10.0.0.1","Standby IP":"0.0.0.0","Wired MAC Address":"00:1a:1e:00:00:01","Serial #":"AB1234567","AP Type":"535"}]}`

    3. `show_user_table.json` — `{"_meta":["IP","MAC","Name","Role","Age(d:h:m)","Auth","VPN link","AP name","Roaming","Tunnel id","Phy","Profile","Forward mode","Type","Host Name","User Type"],"_global_result":{"status":"0","status_str":"Success"},"Users":[{"IP":"10.1.10.5","MAC":"aa:bb:cc:dd:ee:01","Name":"alice","Role":"employee","Age(d:h:m)":"00:01:23","Auth":"802.1x","AP name":"AP-01","Phy":"a-VHT-80sgi-2ss","Profile":"corp_ssid","Type":"WIRELESS","Host Name":"alice-laptop"}]}`

    4. `show_user_table_verbose.json` — verbose variant: same shape but include extra keys `{"Essid/Bssid/Phy":"corp/00:1a:1e:11:22:33/a-VHT-80sgi-2ss","Auth Server":"radius1","ACL":"allowall","BW Contract":"none","Vlan":"100"}` merged into the single user dict.

    5. `show_alarms.json` — `{"_meta":["Severity","Name","Source","Time","Description"],"_global_result":{"status":"0","status_str":"Success"},"Alarms":[{"Severity":"Major","Name":"AP-DOWN","Source":"AP-01","Time":"2026-04-28 12:34:56","Description":"AP went down"}]}`

    6. `show_audit_trail.json` — `{"_meta":["Time","User","IP","Command","Result"],"_global_result":{"status":"0","status_str":"Success"},"Audit Trail":[{"Time":"2026-04-28 09:00:00","User":"admin","IP":"10.0.0.100","Command":"wlan ssid-profile corp","Result":"Success"}]}`

    7. `ssid_prof.json` — config-object shape (no `_meta`): `{"_data":[{"ssid_prof":[{"profile-name":"corp","essid":{"essid":"CorpWiFi"},"opmode":{"wpa2-aes":true}}]}],"_global_result":{"status":"0"}}`

    8. `show_log_system.json` — `{"_meta":["timestamp","severity","message"],"_global_result":{"status":"0","status_str":"Success"},"System Log":[{"timestamp":"2026-04-28T09:00:00","severity":"info","message":"Startup complete"}]}`

    Each file MUST be valid JSON parseable by `json.loads`.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run python -c "import json,glob; [json.loads(open(p).read()) for p in glob.glob('tests/unit/fixtures/aos8/*.json')]; print('all parse')"</automated>
  </verify>
  <done>
    - All 8 fixture files exist under `tests/unit/fixtures/aos8/`.
    - Each parses as valid JSON.
    - `show_switches.json` contains `"_meta"` and `"_global_result"` keys (verify command parses successfully).
    - `ssid_prof.json` contains `"_data"` and `"_global_result"` (no `_meta`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write all 26 red tests + init wiring tests</name>
  <files>
    hpe-networking-mcp/tests/unit/test_aos8_read_health.py,
    hpe-networking-mcp/tests/unit/test_aos8_read_clients.py,
    hpe-networking-mcp/tests/unit/test_aos8_read_alerts.py,
    hpe-networking-mcp/tests/unit/test_aos8_read_wlan.py,
    hpe-networking-mcp/tests/unit/test_aos8_read_troubleshooting.py,
    hpe-networking-mcp/tests/unit/test_aos8_init.py
  </files>
  <behavior>
    All tests MUST FAIL with `ImportError`/`ModuleNotFoundError` until Plans 02-06 implement the tool modules. They define the exact contract each tool must satisfy.

    Per-test contract (mirror RESEARCH Pattern 5):
    - Each test imports the tool function from its target module (which does not yet exist).
    - Each test mocks `client.request` via `unittest.mock.AsyncMock(return_value=mock_response)` where `mock_response.json.return_value = <fixture or inline body>`.
    - Each test asserts that `client.request.assert_awaited_once_with("GET", <expected path>, params=<expected params>)`.
    - Tests for tools that should strip `_meta` additionally assert `"_meta" not in result and "_global_result" not in result`.
  </behavior>
  <read_first>
    .planning/phases/03-read-tools/03-RESEARCH.md,
    .planning/phases/03-read-tools/03-CONTEXT.md,
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/central/tools/clients.py,
    hpe-networking-mcp/tests/unit/test_aos8_client.py,
    hpe-networking-mcp/tests/conftest.py
  </read_first>
  <action>
    Create 6 pytest files. EVERY test file starts with:
    ```python
    from __future__ import annotations
    import json
    from pathlib import Path
    from unittest.mock import AsyncMock, MagicMock
    import httpx
    import pytest
    pytestmark = pytest.mark.unit
    _FIXTURES = Path(__file__).parent / "fixtures" / "aos8"

    def _load(name: str) -> dict:
        return json.loads((_FIXTURES / name).read_text())

    def _make_ctx(body):
        response = MagicMock(spec=httpx.Response)
        response.json.return_value = body
        response.text = json.dumps(body) if not isinstance(body, str) else body
        client = MagicMock()
        client.request = AsyncMock(return_value=response)
        ctx = MagicMock()
        ctx.lifespan_context = {"aos8_client": client}
        return ctx, client
    ```

    **File 1: `test_aos8_read_health.py`** — covers READ-01..08. One test per tool plus one `test_all_health_tools_read_only` that imports `from hpe_networking_mcp.platforms._common.tool_registry import REGISTRIES` and asserts every entry in `REGISTRIES["aos8"]` whose category is `"health"` has `readOnlyHint=True` (skip if registry empty for now — `pytest.skip` with reason "registered after Plan 02"). Required tests (each asserts `client.request.assert_awaited_once_with("GET", "/v1/configuration/showcommand", params=<EXACT PARAMS BELOW>)`):
      - READ-01 `test_get_controllers`: import `aos8_get_controllers` from `hpe_networking_mcp.platforms.aos8.tools.health`, body=`_load("show_switches.json")`, params=`{"command":"show switches"}`, assert `_meta`/`_global_result` stripped.
      - READ-02 `test_get_ap_database_passes_config_path`: `aos8_get_ap_database`, call with `config_path="/md/site1"`, params=`{"command":"show ap database","config_path":"/md/site1"}`.
      - READ-02 `test_get_ap_database_default_config_path`: call with no kwargs, params include `"config_path":"/md"`.
      - READ-03 `test_get_active_aps`: `aos8_get_active_aps`, params=`{"command":"show ap active","config_path":"/md"}`.
      - READ-04 `test_get_ap_detail_by_name`: `aos8_get_ap_detail(ctx, ap_name="AP-01")`, params=`{"command":"show ap details ap-name AP-01","config_path":"/md"}`.
      - READ-04 `test_get_ap_detail_by_mac`: `aos8_get_ap_detail(ctx, ap_mac="00:1a:1e:00:00:01")`, params include `"command":"show ap details ap-mac 00:1a:1e:00:00:01"`.
      - READ-04 `test_get_ap_detail_requires_one_selector`: call with neither — assert returned value is a string starting with `"Error"` or `"Must"`. Do NOT raise.
      - READ-05 `test_get_bss_table`: `aos8_get_bss_table`, params=`{"command":"show ap bss-table","config_path":"/md"}`.
      - READ-06 `test_get_radio_summary`: `aos8_get_radio_summary`, params=`{"command":"show ap radio-summary","config_path":"/md"}`.
      - READ-07 `test_get_version`: `aos8_get_version`, params=`{"command":"show version"}` — NO config_path.
      - READ-08 `test_get_licenses`: `aos8_get_licenses`, params=`{"command":"show license"}` — NO config_path.

    **File 2: `test_aos8_read_clients.py`** — READ-09..12, importing from `...tools.clients`:
      - READ-09 `test_get_clients`: `aos8_get_clients`, params=`{"command":"show user-table","config_path":"/md"}`, assert `_meta` stripped.
      - READ-10 `test_find_client_by_mac`: `aos8_find_client(ctx, mac="aa:bb:cc:dd:ee:01")`, params=`{"command":"show user-table mac aa:bb:cc:dd:ee:01","config_path":"/md"}`.
      - READ-10 `test_find_client_by_ip`: `aos8_find_client(ctx, ip="10.1.10.5")`, params=`{"command":"show user-table ip 10.1.10.5","config_path":"/md"}`.
      - READ-10 `test_find_client_by_username`: `aos8_find_client(ctx, username="alice")`, params=`{"command":"show user-table name alice","config_path":"/md"}`.
      - READ-10 `test_find_client_requires_selector`: call with no selector — returns error string.
      - READ-11 `test_get_client_detail`: `aos8_get_client_detail(ctx, mac="aa:bb:cc:dd:ee:01")`, params=`{"command":"show user-table verbose mac aa:bb:cc:dd:ee:01","config_path":"/md"}`.
      - READ-12 `test_get_client_history`: `aos8_get_client_history(ctx, mac="aa:bb:cc:dd:ee:01")`, params=`{"command":"show ap association history client-mac aa:bb:cc:dd:ee:01"}` — NO config_path.

    **File 3: `test_aos8_read_alerts.py`** — READ-13..15, importing from `...tools.alerts`:
      - READ-13 `test_get_alarms`: `aos8_get_alarms`, params=`{"command":"show alarms","config_path":"/md"}`.
      - READ-14 `test_get_audit_trail`: `aos8_get_audit_trail`, params=`{"command":"show audit-trail"}` — NO config_path.
      - READ-15 `test_get_events`: `aos8_get_events`, params=`{"command":"show events","config_path":"/md"}`.

    **File 4: `test_aos8_read_wlan.py`** — READ-16..19, importing from `...tools.wlan`. These hit `/v1/configuration/object/<obj>` (NOT showcommand):
      - READ-16 `test_get_ssid_profiles`: body=`_load("ssid_prof.json")`, assert `client.request.assert_awaited_once_with("GET", "/v1/configuration/object/ssid_prof", params={"config_path": "/md"})` and assert `"_global_result" not in result`.
      - READ-17 `test_get_virtual_aps`: assert path `/v1/configuration/object/virtual_ap`, params `{"config_path": "/md"}`.
      - READ-18 `test_get_ap_groups`: assert path `/v1/configuration/object/ap_group`, params `{"config_path": "/md"}`.
      - READ-19 `test_get_user_roles`: assert path `/v1/configuration/object/role`, params `{"config_path": "/md"}`.
      - All four also: `test_<name>_passes_config_path` — call with `config_path="/md/branch1"` and assert that value flows into `params["config_path"]`.

    **File 5: `test_aos8_read_troubleshooting.py`** — READ-20..26, importing from `...tools.troubleshooting`:
      - READ-20 `test_ping`: `aos8_ping(ctx, dest="8.8.8.8")`, params=`{"command":"ping 8.8.8.8"}` — NO config_path. Bypasses prefix check.
      - READ-21 `test_traceroute`: `aos8_traceroute(ctx, dest="8.8.8.8")`, params=`{"command":"traceroute 8.8.8.8"}`.
      - READ-22 `test_show_command_accepts_show`: `aos8_show_command(ctx, command="show version")`, params=`{"command":"show version"}`.
      - READ-22 `test_show_command_strips_leading_whitespace`: `command="  show version"` — must still call with `params={"command":"show version"}`.
      - READ-22 `test_show_command_case_insensitive`: `command="SHOW version"` — accepted; params reflect normalized lowercase or original-case as appropriate (assert `client.request.assert_awaited_once`).
      - READ-22 `test_show_command_rejects_non_show`: `command="reload"` — `client.request` MUST NOT be awaited; result is a string containing `"Only 'show' commands"`.
      - READ-22 `test_show_command_rejects_showtech_injection`: `command="showtech"` — rejected (no trailing space after `show`).
      - READ-22 `test_show_command_strips_meta`: body has `_meta` and `_global_result` — both absent in result.
      - READ-22 `test_show_command_handles_text_response`: configure `response.json` to raise `ValueError("not json")`, set `response.text="raw output"` — result is `{"output":"raw output"}`.
      - READ-23 `test_get_logs_default_count`: `aos8_get_logs(ctx)` — params=`{"command":"show log system 100"}`.
      - READ-23 `test_get_logs_custom_count`: `aos8_get_logs(ctx, count=50)` — params=`{"command":"show log system 50"}`.
      - READ-23 `test_get_logs_rejects_oversized_count`: `count=2000` — returns error string, no request awaited.
      - READ-24 `test_get_controller_stats_aggregates`: configure `client.request` to be called multiple times (use `AsyncMock(side_effect=[r1, r2, r3])`); assert `client.request.await_count == 3` and result is a dict with keys `"cpu"`, `"memory"`, `"uptime"`.
      - READ-25 `test_get_arm_history`: params=`{"command":"show ap arm history","config_path":"/md"}`.
      - READ-26 `test_get_rf_monitor`: params=`{"command":"show ap monitor stats","config_path":"/md"}`.

    **File 6: `test_aos8_init.py`** — wiring tests:
      - `test_tools_dict_complete`: import `from hpe_networking_mcp.platforms.aos8 import TOOLS`. Assert all 26 expected names appear across the categories. Expected keys: `"health"` (8), `"clients"` (4), `"alerts"` (3), `"wlan"` (4), `"troubleshooting"` (7). Assert flatten count == 26 and the exact set equals `{"aos8_get_controllers","aos8_get_ap_database","aos8_get_active_aps","aos8_get_ap_detail","aos8_get_bss_table","aos8_get_radio_summary","aos8_get_version","aos8_get_licenses","aos8_get_clients","aos8_find_client","aos8_get_client_detail","aos8_get_client_history","aos8_get_alarms","aos8_get_audit_trail","aos8_get_events","aos8_get_ssid_profiles","aos8_get_virtual_aps","aos8_get_ap_groups","aos8_get_user_roles","aos8_ping","aos8_traceroute","aos8_show_command","aos8_get_logs","aos8_get_controller_stats","aos8_get_arm_history","aos8_get_rf_monitor"}`.
      - `test_register_tools_dynamic_mode`: build a real `FastMCP` instance, build a `ServerConfig` with `tool_mode="dynamic"` (use existing factory or MagicMock with attribute), call `register_tools(mcp, config)`, assert return value == 26 and `REGISTRIES["aos8"]` has 26 entries.
      - `test_register_tools_static_mode`: same but `tool_mode="static"`, assert return == 26.

    Every file must be ruff-clean (line length 120, double quotes, trailing commas) and stay under 500 lines.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_aos8_read_health.py tests/unit/test_aos8_read_clients.py tests/unit/test_aos8_read_alerts.py tests/unit/test_aos8_read_wlan.py tests/unit/test_aos8_read_troubleshooting.py tests/unit/test_aos8_init.py --collect-only -q</automated>
  </verify>
  <done>
    - All 6 test files exist and pytest can collect them (no SyntaxError, no ImportError on the test files themselves — only on the tool modules under test).
    - `pytest --collect-only` reports >= 40 collected tests across these files.
    - `cd hpe-networking-mcp && uv run pytest tests/unit/ -q` shows the existing 645 tests still pass; new tests fail with import/module errors (expected red state).
    - `cd hpe-networking-mcp && uv run ruff check tests/unit/test_aos8_read_*.py tests/unit/test_aos8_init.py` exits 0.
  </done>
</task>

</tasks>

<verification>
- All Wave 0 artifacts exist on disk.
- Existing 645 tests still pass: `cd hpe-networking-mcp && uv run pytest tests/unit/ -q --ignore=tests/unit/test_aos8_read_health.py --ignore=tests/unit/test_aos8_read_clients.py --ignore=tests/unit/test_aos8_read_alerts.py --ignore=tests/unit/test_aos8_read_wlan.py --ignore=tests/unit/test_aos8_read_troubleshooting.py --ignore=tests/unit/test_aos8_init.py` passes.
- New AOS8 read tests collect and fail with module-not-found (red baseline).
- Ruff and mypy clean on the new helper module: `cd hpe-networking-mcp && uv run ruff check src/hpe_networking_mcp/platforms/aos8/tools/ && uv run mypy src/hpe_networking_mcp/platforms/aos8/tools/`.
</verification>

<success_criteria>
1. `tests/conftest.py` `_install_registry_stubs()` includes `aos8._registry` (grep-verified).
2. `aos8/tools/__init__.py` exports `READ_ONLY` ToolAnnotations.
3. `aos8/tools/_helpers.py` defines `strip_meta`, `run_show`, `get_object`, `format_aos8_error` (importable + smoke test passes).
4. 8 JSON fixture files exist and parse.
5. 6 test files exist with red tests for all 26 READ requirements + wiring.
6. Existing test suite remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-read-tools/03-01-SUMMARY.md`.
</output>
