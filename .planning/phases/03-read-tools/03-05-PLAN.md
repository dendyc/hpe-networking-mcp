---
phase: 03-read-tools
plan: 05
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/wlan.py
autonomous: true
requirements: [READ-16, READ-17, READ-18, READ-19]

must_haves:
  truths:
    - "All 4 WLAN-category tools (READ-16..19) are implemented and decorated"
    - "test_aos8_read_wlan.py is fully GREEN"
    - "All WLAN tools route through get_object() (config-object endpoint), NOT run_show"
    - "All WLAN tools accept config_path with default '/md' (per CONTEXT D-04)"
  artifacts:
    - path: "hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/wlan.py"
      provides: "4 AOS8 WLAN/config read tools (config-object endpoint)"
      contains: "aos8_get_ssid_profiles"
  key_links:
    - from: "hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/wlan.py"
      to: "_helpers.get_object"
      via: "import + call"
      pattern: "get_object\\(client"
---

<objective>
Implement READ-16..19 in `tools/wlan.py`. Unlike other categories, WLAN tools hit `/v1/configuration/object/<obj>` via `get_object()` helper — NOT the showcommand endpoint.

Output: 1 new file `wlan.py` (~190 lines per RESEARCH).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-read-tools/03-CONTEXT.md
@.planning/phases/03-read-tools/03-RESEARCH.md
@.planning/phases/03-read-tools/03-01-SUMMARY.md
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/_helpers.py
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/__init__.py
@hpe-networking-mcp/tests/unit/test_aos8_read_wlan.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement aos8/tools/wlan.py — READ-16..19 (config-object endpoint)</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/wlan.py</files>
  <behavior>
    All 4 tools call `get_object(client, "<object_path>", config_path=config_path)` — NOT `run_show`.
    - READ-16 `aos8_get_ssid_profiles(ctx, config_path: str = "/md")` → `get_object(client, "ssid_prof", config_path=config_path)`. Verb: "fetch SSID profiles".
    - READ-17 `aos8_get_virtual_aps(ctx, config_path: str = "/md")` → `get_object(client, "virtual_ap", config_path=config_path)`. Verb: "fetch virtual APs".
    - READ-18 `aos8_get_ap_groups(ctx, config_path: str = "/md")` → `get_object(client, "ap_group", config_path=config_path)`. Verb: "fetch AP groups".
    - READ-19 `aos8_get_user_roles(ctx, config_path: str = "/md")` → `get_object(client, "role", config_path=config_path)`. Verb: "fetch user roles".
  </behavior>
  <read_first>
    hpe-networking-mcp/tests/unit/test_aos8_read_wlan.py,
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/_helpers.py,
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/__init__.py,
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/_registry.py
  </read_first>
  <action>
    Create `hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/wlan.py`. Imports include `get_object` and `format_aos8_error` from `_helpers` (NOT `run_show`). 4 tool functions, each decorated `@tool(name="aos8_<exact>", annotations=READ_ONLY)`, each `async def ... -> dict[str, Any] | str`, each Google-style docstring, body pattern:
    ```python
    client = ctx.lifespan_context["aos8_client"]
    try:
        return await get_object(client, "<object_path>", config_path=config_path)
    except Exception as exc:  # noqa: BLE001
        return format_aos8_error(exc, "<verb>")
    ```
    Use the exact object_path and verb pairs from `<behavior>`. File ruff/mypy clean and under 500 lines.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_aos8_read_wlan.py -x -q && uv run ruff check src/hpe_networking_mcp/platforms/aos8/tools/wlan.py && uv run mypy src/hpe_networking_mcp/platforms/aos8/tools/wlan.py</automated>
  </verify>
  <done>
    - `test_aos8_read_wlan.py` fully green.
    - `grep -c "@tool(name=\"aos8_" hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/wlan.py` returns 4.
    - `grep -c "get_object(client" hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/wlan.py` returns 4.
    - `grep -c "run_show" hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/wlan.py` returns 0.
    - Ruff + mypy clean. Full suite green.
  </done>
</task>

</tasks>

<verification>
- `cd hpe-networking-mcp && uv run pytest tests/unit/ -q` — green.
</verification>

<success_criteria>
4 tools (`aos8_get_ssid_profiles`, `aos8_get_virtual_aps`, `aos8_get_ap_groups`, `aos8_get_user_roles`) defined; wlan test file green; tools use `get_object` not `run_show`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-read-tools/03-05-SUMMARY.md`.
</output>
