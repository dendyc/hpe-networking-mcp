---
phase: 03-read-tools
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/alerts.py
autonomous: true
requirements: [READ-13, READ-14, READ-15]

must_haves:
  truths:
    - "All 3 alerts-category tools (READ-13..15) are implemented and decorated"
    - "test_aos8_read_alerts.py is fully GREEN"
    - "get_alarms and get_events accept config_path; get_audit_trail does NOT (per CONTEXT D-04/D-05)"
  artifacts:
    - path: "hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/alerts.py"
      provides: "3 AOS8 alerts/audit read tools"
      contains: "aos8_get_alarms"
  key_links:
    - from: "hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/alerts.py"
      to: "_helpers.run_show"
      via: "import + call"
      pattern: "from hpe_networking_mcp.platforms.aos8.tools._helpers import"
---

<objective>
Implement READ-13..15 in `tools/alerts.py`.

Output: 1 new file `alerts.py` (~150 lines per RESEARCH).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-read-tools/03-CONTEXT.md
@.planning/phases/03-read-tools/03-RESEARCH.md
@.planning/phases/03-read-tools/03-01-SUMMARY.md
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/_helpers.py
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/__init__.py
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/central/tools/alerts.py
@hpe-networking-mcp/tests/unit/test_aos8_read_alerts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement aos8/tools/alerts.py — READ-13..15</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/alerts.py</files>
  <behavior>
    - READ-13 `aos8_get_alarms(ctx, config_path: str = "/md")` → `run_show(client, "show alarms", config_path=config_path)`. Verb: "list alarms".
    - READ-14 `aos8_get_audit_trail(ctx)` → `run_show(client, "show audit-trail")`. NO config_path. Verb: "fetch audit trail".
    - READ-15 `aos8_get_events(ctx, config_path: str = "/md")` → `run_show(client, "show events", config_path=config_path)`. Verb: "fetch events".
  </behavior>
  <read_first>
    hpe-networking-mcp/tests/unit/test_aos8_read_alerts.py,
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/_helpers.py,
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/__init__.py,
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/_registry.py
  </read_first>
  <action>
    Create `hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/alerts.py` mirroring the structure of Plan 02's `health.py`. 3 tool functions, each decorated `@tool(name="aos8_<exact>", annotations=READ_ONLY)`, each `async def`, each returns `dict[str, Any] | str`, each has Google-style docstring, each wraps `await run_show(...)` in `try/except Exception as exc: return format_aos8_error(exc, "<verb>")` with `# noqa: BLE001`. Use the exact verbs and signatures from `<behavior>`. File ruff/mypy clean and under 500 lines.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_aos8_read_alerts.py -x -q && uv run ruff check src/hpe_networking_mcp/platforms/aos8/tools/alerts.py && uv run mypy src/hpe_networking_mcp/platforms/aos8/tools/alerts.py</automated>
  </verify>
  <done>
    - `test_aos8_read_alerts.py` fully green.
    - `grep -c "@tool(name=\"aos8_" hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/tools/alerts.py` returns 3.
    - Ruff + mypy clean. Full suite green.
  </done>
</task>

</tasks>

<verification>
- `cd hpe-networking-mcp && uv run pytest tests/unit/ -q` — green.
</verification>

<success_criteria>
3 tools (`aos8_get_alarms`, `aos8_get_audit_trail`, `aos8_get_events`) defined; alerts test file green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-read-tools/03-04-SUMMARY.md`.
</output>
