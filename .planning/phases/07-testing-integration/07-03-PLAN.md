---
phase: 07-testing-integration
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py
autonomous: true
requirements: [TEST-06]
must_haves:
  truths:
    - "TOOLS dict in aos8/__init__.py contains 'differentiators' key with 9 tool names"
    - "register_tools() returns 47 (was 38)"
    - "test_aos8_init.py passes (EXPECTED_TOTAL=47)"
    - "Full pytest tests/unit suite is GREEN — 6 existing platform suites untouched and passing"
  artifacts:
    - path: "hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py"
      provides: "TOOLS['differentiators'] = [...9 names]"
      contains: "differentiators"
  key_links:
    - from: "TOOLS['differentiators']"
      to: "differentiators.py module"
      via: "importlib.import_module loop in register_tools()"
      pattern: "importlib.import_module.*differentiators"
---

<objective>
Wire `TOOLS["differentiators"]` into `platforms/aos8/__init__.py` so the
register_tools() importlib loop picks up differentiators.py automatically.
Then run the full unit test suite to confirm:
- All AOS8 tests pass (read + write + diff + security + init + client + config + prompts)
- All 6 existing platform test suites (Mist, Central, GreenLake, ClearPass, Apstra, Axis) pass unmodified
- Final tool count: 47 (8 health + 4 clients + 3 alerts + 4 wlan + 7 troubleshooting + 9 differentiators + 12 writes)

Purpose: Final integration gate. Phase 7 closes by proving non-regression.
Output: One updated file (TOOLS dict entry); full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-testing-integration/07-CONTEXT.md
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py
@hpe-networking-mcp/tests/unit/test_aos8_init.py
@hpe-networking-mcp/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire TOOLS["differentiators"] in aos8/__init__.py</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py</files>
  <read_first>
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py
    hpe-networking-mcp/tests/unit/test_aos8_init.py
  </read_first>
  <action>
    Edit `src/hpe_networking_mcp/platforms/aos8/__init__.py`. Add a new
    "differentiators" entry to the TOOLS dict, BETWEEN the existing
    "troubleshooting" entry and the "writes" entry. Insert verbatim:

    ```python
        "differentiators": [
            "aos8_get_md_hierarchy",
            "aos8_get_effective_config",
            "aos8_get_pending_changes",
            "aos8_get_rf_neighbors",
            "aos8_get_cluster_state",
            "aos8_get_air_monitors",
            "aos8_get_ap_wired_ports",
            "aos8_get_ipsec_tunnels",
            "aos8_get_md_health_check",
        ],
    ```

    Do NOT modify any other key, function, import, or comment in __init__.py.
    The existing `register_tools()` importlib loop already iterates the TOOLS
    dict and imports `hpe_networking_mcp.platforms.aos8.tools.{category}` for
    each key — adding the "differentiators" key is sufficient to wire the new
    module.

    Verify by reading the file post-edit:
    - "differentiators" key MUST appear after "troubleshooting" and before "writes" in the dict literal
    - Exactly 9 names in the list
    - No other diff in the file
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_aos8_init.py -v</automated>
  </verify>
  <done>
    test_aos8_init.py passes — EXPECTED_TOTAL=47 matches the registered count.
  </done>
  <acceptance_criteria>
    - File `src/hpe_networking_mcp/platforms/aos8/__init__.py` contains literal `"differentiators": [`
    - File contains literal `"aos8_get_md_hierarchy"` (under the new key)
    - File contains literal `"aos8_get_md_health_check"`
    - File contains literal `"aos8_get_ap_wired_ports"`
    - File contains literal `"aos8_get_ipsec_tunnels"`
    - "differentiators" key appears AFTER the line containing `"troubleshooting": [` (verify by line numbers via grep -n)
    - "differentiators" key appears BEFORE the line containing `"writes": [`
    - `git diff src/hpe_networking_mcp/platforms/aos8/__init__.py` shows ONLY the addition of the differentiators block (no other changes)
    - `uv run pytest tests/unit/test_aos8_init.py -v` exits 0
    - `uv run ruff check src/hpe_networking_mcp/platforms/aos8/__init__.py` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full suite regression — all 7 platforms green, no existing tests modified</name>
  <files>(verification only — no source edits)</files>
  <read_first>
    hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py
  </read_first>
  <action>
    Run the complete unit test suite and the pre-push checklist:

    1. **Verify no unintended modifications to other-platform tests** (D-07 constraint):
       Run `git diff --name-only tests/unit/` — output MUST contain ONLY:
       - test_aos8_read_differentiators.py (new)
       - test_aos8_security.py (new)
       - test_aos8_init.py (modified — DIFF expectations)
       - Optionally test_aos8_config.py (one test added if TEST-05 gap was found)
       - test_aos8_*.py NEW fixture files (under tests/unit/fixtures/aos8/)

       If any of `test_mist_*`, `test_central_*`, `test_greenlake_*`,
       `test_clearpass_*`, `test_apstra_*`, `test_axis_*` appear in the diff,
       STOP and revert those files — D-07 forbids modification.

    2. **Run full unit suite**:
       ```
       cd hpe-networking-mcp && uv run pytest tests/unit -q
       ```
       Expected: ALL tests pass. Exit code 0. Approximate count ~759 tests
       (748 existing + 9 DIFF tests + 2 security tests + adjusted init tests + maybe 1 new TEST-05 test).
       If any test fails, INVESTIGATE — TEST-06 fails the phase if any
       existing platform test breaks. Do NOT modify other-platform tests
       to make them pass.

    3. **Run AOS8-only filter as a sanity sample**:
       ```
       cd hpe-networking-mcp && uv run pytest tests/unit -k aos8 -q
       ```
       Expected: all AOS8 tests pass.

    4. **Run pre-push checklist** (CLAUDE.md mandate):
       ```
       cd hpe-networking-mcp && uv run ruff check .
       cd hpe-networking-mcp && uv run ruff format --check .
       cd hpe-networking-mcp && uv run mypy src/ --ignore-missing-imports
       ```
       All three must exit 0.

    5. **Document results in summary**: Capture exact test count, the per-platform breakdown if pytest output supplies it, and confirm pre-push checklist.

    NOTE: Per Phase 7 RESEARCH "Project Constraints" section, CLAUDE.md
    mandates a CHANGELOG/README/version bump for new tool surfaces. However,
    Phase 7 CONTEXT.md (D-06 + Phase Boundary "Not in scope: documentation
    updates beyond what Phase 6 already completed") says no doc updates are
    in scope. RESOLVE: this is a documentation gap to flag in the SUMMARY,
    not work to perform here. The user explicitly excluded doc updates from
    Phase 7 — log it as a follow-up for a Phase 8 / patch release. Do NOT
    edit README/CHANGELOG/TOOLS.md/INSTRUCTIONS.md/pyproject.toml in this plan.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit -q</automated>
  </verify>
  <done>
    Full unit suite green. Ruff (check + format), mypy clean.
    No edits to non-AOS8 test files (D-07 honored).
  </done>
  <acceptance_criteria>
    - `uv run pytest tests/unit -q` exits 0
    - Pytest output shows test count ≥ 757 (748 baseline + at least 9 DIFF + 2 security; allow buffer)
    - `uv run pytest tests/unit -k aos8 -q` exits 0
    - `uv run ruff check .` (run from hpe-networking-mcp dir) exits 0
    - `uv run ruff format --check .` exits 0
    - `uv run mypy src/ --ignore-missing-imports` exits 0
    - `git diff --name-only tests/unit/ | grep -E '(test_mist|test_central|test_greenlake|test_clearpass|test_apstra|test_axis)'` returns NO output (i.e. exits non-zero with empty stdout — no other-platform test files modified)
    - `git diff --name-only src/` shows ONLY `src/hpe_networking_mcp/platforms/aos8/__init__.py` and `src/hpe_networking_mcp/platforms/aos8/tools/differentiators.py` (the latter from Plan 07-02)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Phase 7 closure gates:

1. Full pytest tests/unit -q exits 0
2. Test count ≥ 757
3. ruff check, ruff format --check, mypy all clean
4. No other-platform test files modified (`git diff --name-only` review)
5. AOS8 tool count: 47 verified by test_aos8_init.py
6. TEST-01 (existing test_aos8_client.py), TEST-02 (5 read suites + new diff suite), TEST-03 (existing test_aos8_write.py), TEST-04 (client logging + new tool-layer security), TEST-05 (test_aos8_config.py), TEST-06 (full suite green) — all 6 TEST requirements satisfied.
</verification>

<success_criteria>
- TOOLS["differentiators"] wired with 9 tool names
- register_tools() reports 47 tools
- Full unit suite green (~759 tests)
- ruff + mypy clean
- 6 existing platform test suites pass unmodified (D-07)
- All 6 TEST-NN phase requirements verifiably met
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-integration/07-03-SUMMARY.md` documenting:
- Final test count from `pytest tests/unit -q` output
- AOS8 tool total: 47 confirmed
- Per-platform regression: which tests / suites were verified
- Pre-push checklist results
- Documentation gap flag: README tool count (47), CHANGELOG entry, version bump are NOT in Phase 7 scope per CONTEXT.md D-06 / Phase Boundary — flag for follow-up
- Phase 7 sign-off: all 6 TEST-NN requirements satisfied with evidence (test names + commands)
</output>
