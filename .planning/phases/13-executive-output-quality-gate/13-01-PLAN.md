---
phase: 13-executive-output-quality-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md
autonomous: true
requirements: [OUTPUT-01, OUTPUT-02, QUALITY-01, QUALITY-02, QUALITY-03]

must_haves:
  truths:
    - "Skill report opens with a freestanding 2-4 sentence executive summary paragraph (Verdict + finding counts + SE-ready sentence) BEFORE the `## AOS migration readiness —` header"
    - "`## Output formatting` section contains an `### Output hygiene (mandatory)` subsection with all 4 prohibitions (no raw JSON, no tool-call syntax in finding text, no stack traces, no ellipsis/truncation)"
    - "Stage 6 decision matrix contains a new PARTIAL row covering AOS8 live-mode batch failures with action prose listing failed checks + fallback CLI commands"
    - "Skill frontmatter `platforms:` is `[central, aos8]` (was `[central]`)"
    - "Skill frontmatter `tools:` already lists all 9 AOS8 tool names referenced in the body — no diff expected"
    - "`pytest tests/unit/test_skill_tool_references.py -k aos-migration-readiness` passes after edits"
  artifacts:
    - path: "hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md"
      provides: "Updated skill with exec summary, output hygiene rules, PARTIAL matrix row, platforms:[central, aos8]"
      contains: "### Output hygiene (mandatory)"
  key_links:
    - from: "hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md (frontmatter line 21)"
      to: "platform tag list"
      via: "YAML flow-style list `[central, aos8]`"
      pattern: "platforms: \\[central, aos8\\]"
    - from: "hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md body (`aos8_*` tool references)"
      to: "live AOS8 platform tool catalog (`platforms/aos8/_registry.py` REGISTRIES)"
      via: "`tests/unit/test_skill_tool_references.py::TestSkillToolReferences::test_skill_references_resolve` regex match against catalog"
      pattern: "aos8_(get_md_hierarchy|get_effective_config|get_ap_database|get_cluster_state|show_command|get_clients|get_bss_table|get_active_aps|get_ap_wired_ports)"
---

<objective>
Edit the `aos-migration-readiness` skill markdown to render a customer-grade migration readiness report and update its frontmatter so the AOS8 platform is advertised. Phase deliverables:

1. Insert an executive summary paragraph instruction at the top of the Stage 6 report template (before the `## AOS migration readiness —` header) — OUTPUT-01.
2. Add an "Output hygiene (mandatory)" subsection under "Output formatting" with 4 prohibitions — OUTPUT-02.
3. Add one new PARTIAL row to the Stage 6 decision matrix covering AOS8 live-mode batch failures — OUTPUT-01 / D-07.
4. Update frontmatter `platforms:` from `[central]` to `[central, aos8]` — QUALITY-02.
5. Audit `tools:` frontmatter list against AOS8 references in body (expected: zero diff) — QUALITY-01.
6. Run `tests/unit/test_skill_tool_references.py -k aos-migration-readiness` to confirm QUALITY-03.

Purpose: Close v1.1 milestone by giving SEs a copy-pasteable executive readout and gating the skill on the existing tool-reference regression test.
Output: 1 modified skill file; regression test green; full unit suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-executive-output-quality-gate/13-CONTEXT.md
@.planning/phases/13-executive-output-quality-gate/13-RESEARCH.md
@.planning/phases/13-executive-output-quality-gate/13-VALIDATION.md
@hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md
@hpe-networking-mcp/tests/unit/test_skill_tool_references.py

<interfaces>
<!-- The skill file is a runbook: it INSTRUCTS the AI on what to produce. -->
<!-- Phase 13 introduces NO new tool names. The 9 AOS8 tools already in `tools:` frontmatter are exhaustive: -->
<!--   aos8_get_md_hierarchy, aos8_get_effective_config, aos8_get_ap_database, aos8_get_cluster_state, -->
<!--   aos8_show_command, aos8_get_clients, aos8_get_bss_table, aos8_get_active_aps, aos8_get_ap_wired_ports -->
<!-- Any additional `aos8_*` token written into the body would FAIL the regression test. -->

Existing PARTIAL matrix row (skill ~line 505) — pattern to mirror:
```markdown
| Operator hasn't pasted the data bundle | Output **PARTIAL** verdict — Central-side checks complete, source-side blocked. List exactly what's needed. |
```

Existing finding format pattern (preserved, not changed):
```
**SEVERITY — Description. (source: tool_name(), Batch N) (VSG §anchor)**
```

Frontmatter line 21 current state:
```yaml
platforms: [central]
```
Target:
```yaml
platforms: [central, aos8]
```

Regression test command (QUALITY-03):
```bash
cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -k aos-migration-readiness -v
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit skill body — exec summary, output hygiene rules, PARTIAL matrix row</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md</files>
  <read_first>
    - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md (read in FULL — do not assume line numbers; locate insertion points by anchor strings, not raw line offsets)
    - .planning/phases/13-executive-output-quality-gate/13-CONTEXT.md (decisions D-01 through D-08)
    - .planning/phases/13-executive-output-quality-gate/13-RESEARCH.md (Examples 2, 3, 4 contain the exact text to insert; Pitfalls 1, 2, 5 list mistakes to avoid)
    - .planning/phases/12-central-enrichment-cutover-validation/12-CONTEXT.md (existing finding-format conventions Phase 13 must NOT break)
  </read_first>
  <action>
Make THREE edits to `hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md`. Locate insertion points by anchor strings (not absolute line numbers — file has been edited recently).

**EDIT A — Stage 6 decision matrix: insert new PARTIAL row (D-07).**

Anchor: find the existing PARTIAL matrix row containing the literal text `Operator hasn't pasted the data bundle`. Insert a NEW row IMMEDIATELY AFTER that row (keep the existing row unchanged). New row text — exactly 3 pipes per row (start, middle, end):

```
| AOS8 live mode AND one or more Stage 1 batches failed | **PARTIAL** — list which AOS8 checks could not run (e.g. "Batch 3 failed: cluster state, local-user db"). For each failed batch, include the exact CLI command(s) the operator must paste manually to complete those data points. All checks that did succeed are still reported. |
```

Verify the resulting table renders: every matrix row (existing + new) has exactly 3 `|` characters and a column-separator row of dashes is present once at the top of the table.

**EDIT B — Output formatting: insert "Output hygiene (mandatory)" subsection (D-05, D-06).**

Anchor: find the heading line `## Output formatting`. Locate the prose paragraph(s) immediately under that heading and the start of the report-template code fence (the line containing ```` ``` ```` that opens the canonical report template). Insert the following BLOCK between the prose paragraph(s) and the opening code fence:

```
### Output hygiene (mandatory)

These rules apply to every finding across Stages 3, 4, 5, and 6:

1. **No raw JSON blobs.** When citing an API response, extract and quote only the specific field value relevant to the finding. Never dump the full response dict.
2. **No tool-call syntax in finding text.** Tool names belong in the `(source: tool_name(), Batch N)` attribution at the end of the finding — never inside the finding sentence itself.
3. **No stack traces.** If a tool call raises an exception, emit a brief one-line error note and the fallback CLI command. Never include a Python traceback.
4. **No ellipsis or truncation markers.** Do not write `...`, `[truncated]`, or similar. Extract the relevant fields explicitly; if a response is large, summarise the salient values in prose.
```

Do NOT duplicate these rules into Stages 3/4/5 (D-06 forbids per-stage repetition — Pitfall 2 in RESEARCH.md).

**EDIT C — Report template: prepend executive summary paragraph instruction (D-01..D-04, D-08).**

Anchor: find the report-template code fence inside `## Output formatting`. The template currently begins with the line `## AOS migration readiness — <source: aos6/aos8/iap> → AOS 10 <target: tunnel/bridge/mixed>`. Insert the following block INSIDE the same code fence, at the very TOP, BEFORE the `## AOS migration readiness —` header line. (Per RESEARCH.md Open Question 1 recommendation: single fenced block, prepend at top.)

Insert this exact text inside the fence as the first content lines (preserve the fence; do NOT close and reopen the code fence):

```
> Open the report with this paragraph (plain prose, 2–4 sentences, never a bullet list, never a table). Three elements in order:
>   1. Verdict in bold caps — **GO** / **BLOCKED** / **PARTIAL**.
>   2. Finding counts — exact integers for REGRESSION, DRIFT, INFO.
>   3. One SE-ready sentence — name the source platform (AOS8 / AOS6 / IAP), AP count, controller count, and the key action a human SE can paste verbatim into a customer email.
>
> For PARTIAL verdict caused by AOS8 live-mode batch failure, append: "AOS8 live collection partially succeeded — <N> checks completed; <M> checks require manual CLI paste (see below)."
>
> Template (AI fills the angle-bracket placeholders at runtime; do NOT hard-code values):

**<VERDICT>** — <X> REGRESSION / <Y> DRIFT / <Z> INFO findings. This <source> deployment (<N> APs, <M> controllers) <one plain-English action sentence>. <Optional context sentence — e.g. "Live AOS8 data collection was used for all checks." or PARTIAL note if applicable.>

```

(The blank line after the template line is intentional — separates the exec summary paragraph from the existing `## AOS migration readiness —` header that follows.)

**Forbidden in this task (Pitfall 1, Pitfall 3 from RESEARCH.md):**
- Do NOT write a literal exec summary with hard-coded numbers/platforms/verdicts — the skill INSTRUCTS the AI; angle-bracket placeholders only.
- Do NOT introduce any new `aos8_*` tool name. The 9 tools listed in the existing `tools:` frontmatter are exhaustive. Adding `aos8_get_migration_status` or any other invented name will fail QUALITY-03.

After all three edits, re-read the file and confirm:
1. Decision matrix has the new row immediately after `Operator hasn't pasted the data bundle`.
2. `### Output hygiene (mandatory)` heading exists exactly once under `## Output formatting`.
3. The exec summary instruction block is INSIDE the report template code fence, BEFORE the `## AOS migration readiness —` line.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run python -c "import re; t = open('src/hpe_networking_mcp/skills/aos-migration-readiness.md', encoding='utf-8').read(); assert 'AOS8 live mode AND one or more Stage 1 batches failed' in t, 'PARTIAL row missing'; assert '### Output hygiene (mandatory)' in t, 'hygiene subsection missing'; assert 'No raw JSON blobs' in t and 'No tool-call syntax in finding text' in t and 'No stack traces' in t and 'No ellipsis or truncation markers' in t, 'one or more hygiene rules missing'; assert '**<VERDICT>**' in t, 'exec summary template placeholder missing'; assert re.search(r'<X> REGRESSION / <Y> DRIFT / <Z> INFO findings', t), 'exec summary counts placeholder missing'; assert 'AOS8 live collection partially succeeded' in t, 'PARTIAL exec-summary sentence missing'; print('OUTPUT-01 + OUTPUT-02 markers present')"</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal string `AOS8 live mode AND one or more Stage 1 batches failed` (PARTIAL row D-07)
    - File contains literal string `### Output hygiene (mandatory)` exactly once
    - File contains all 4 hygiene rule headers: `No raw JSON blobs`, `No tool-call syntax in finding text`, `No stack traces`, `No ellipsis or truncation markers`
    - File contains literal string `**<VERDICT>**` (template placeholder, not a hard-coded verdict)
    - File contains literal string `<X> REGRESSION / <Y> DRIFT / <Z> INFO findings`
    - File contains literal string `AOS8 live collection partially succeeded`
    - The exec summary instruction block appears BEFORE the line `## AOS migration readiness —` (verify by: index of `**<VERDICT>**` < index of `## AOS migration readiness —`)
    - The `### Output hygiene (mandatory)` heading appears AFTER the line `## Output formatting` (index ordering)
    - No new `aos8_*` token appears in the body that is NOT one of the 9 already-listed tools (grep `aos8_[a-z_]*\(` against the canonical 9: get_md_hierarchy, get_effective_config, get_ap_database, get_cluster_state, show_command, get_clients, get_bss_table, get_active_aps, get_ap_wired_ports)
    - File still parses as valid YAML+markdown (frontmatter delimited by `---` on lines 1 and ~24, both present)
  </acceptance_criteria>
  <done>
Skill body contains the executive summary instruction, the output hygiene rules subsection, and the new PARTIAL decision-matrix row — each in its prescribed location, with no new tool names introduced and the markdown still parses cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update frontmatter platforms tag, audit tools list, run regression test</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md</files>
  <read_first>
    - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md (lines 1-30 — frontmatter block)
    - hpe-networking-mcp/tests/unit/test_skill_tool_references.py (the regression test that gates QUALITY-03; confirm it is unchanged from Phase 10)
    - .planning/phases/13-executive-output-quality-gate/13-CONTEXT.md (decisions D-09, D-10, D-11)
    - .planning/phases/13-executive-output-quality-gate/13-RESEARCH.md (Pitfall 4 — flow-style list syntax requirement; Don't Hand-Roll table — frontmatter audit is a manual diff, the regression test is the catalog-side check)
  </read_first>
  <action>
**STEP 1 — Update `platforms:` frontmatter (D-10, QUALITY-02).**

Locate line `platforms: [central]` in the YAML frontmatter (line ~21). Replace with:

```yaml
platforms: [central, aos8]
```

Use flow-style brackets exactly as shown — do NOT use comma-string `platforms: central, aos8` (Pitfall 4 in RESEARCH.md: YAML parsers may misinterpret).

**STEP 2 — Audit `tools:` frontmatter (D-09, QUALITY-01) — expected zero diff.**

Run this audit script from `hpe-networking-mcp/`:

```bash
cd hpe-networking-mcp && uv run python -c "
import re, yaml
from pathlib import Path
text = Path('src/hpe_networking_mcp/skills/aos-migration-readiness.md').read_text(encoding='utf-8')
fm = yaml.safe_load(text.split('---', 2)[1])
declared = set(fm['tools'])
# Find every aos8_*( token in the body (matches the regression test's _TOOL_REF_PATTERN flavor)
body = text.split('---', 2)[2]
referenced_aos8 = set(re.findall(r'\baos8_[a-z_]+(?=\()', body))
canonical_9 = {'aos8_get_md_hierarchy','aos8_get_effective_config','aos8_get_ap_database','aos8_get_cluster_state','aos8_show_command','aos8_get_clients','aos8_get_bss_table','aos8_get_active_aps','aos8_get_ap_wired_ports'}
missing_in_frontmatter = referenced_aos8 - declared
unknown_aos8 = referenced_aos8 - canonical_9
print('referenced aos8 tools:', sorted(referenced_aos8))
print('missing from tools: frontmatter:', sorted(missing_in_frontmatter))
print('unknown (not in canonical 9):', sorted(unknown_aos8))
assert not missing_in_frontmatter, f'Frontmatter audit FAILED: tools referenced but not declared: {missing_in_frontmatter}'
assert not unknown_aos8, f'Unknown aos8 tool name in body — would fail regression test: {unknown_aos8}'
print('Frontmatter audit PASSED — zero diff expected by D-09 confirmed')
"
```

Expected stdout: `Frontmatter audit PASSED — zero diff expected by D-09 confirmed`. If the audit reports `missing from tools: frontmatter:` non-empty, STOP — that indicates Phase 11 or 12 introduced a tool reference that was never added to the frontmatter (D-10 forbids Phase 13 from adding new tools — escalate to user). If `unknown` is non-empty, a typo exists in the body — fix it in Task 1 territory (or surface as a bug).

**STEP 3 — Run regression test (D-11, QUALITY-03).**

```bash
cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -k aos-migration-readiness -v
```

Expected: 1 passed (or more — parametrized may run multiple cases). Zero failures.

**STEP 4 — Smoke test full unit suite (no regressions in skill loader / catalog).**

```bash
cd hpe-networking-mcp && uv run pytest tests/unit/ -q
```

Expected: full suite passes (~790 tests per STATE.md).

**Forbidden in this task:**
- Do NOT add any new tool name to the `tools:` frontmatter list (D-10). The 9 AOS8 tools listed by Phase 10 cover all references in the body. If the audit reports a missing tool, the correct fix is to remove the body reference, not extend the frontmatter (escalate to user instead).
- Do NOT change the `tags:` line — only `platforms:` changes in Phase 13.
- Do NOT commit if either pytest invocation reports a failure.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run python -c "import yaml; t = open('src/hpe_networking_mcp/skills/aos-migration-readiness.md', encoding='utf-8').read(); fm = yaml.safe_load(t.split('---', 2)[1]); assert fm['platforms'] == ['central', 'aos8'], f'platforms wrong: {fm[\"platforms\"]}'; print('QUALITY-02 OK:', fm['platforms'])" && cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -k aos-migration-readiness -v</automated>
  </verify>
  <acceptance_criteria>
    - `platforms` field in frontmatter parses (via PyYAML) to the Python list `['central', 'aos8']` — exact two elements, in that order
    - The line `platforms: [central, aos8]` is present in the file (flow-style brackets, not comma-string)
    - `tools:` frontmatter list is UNCHANGED from pre-Phase-13 (no additions, no removals) — verify by diffing against git HEAD: `git diff HEAD -- hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md | grep -E '^[+-]' | grep -v '^[+-][+-][+-]' | grep 'tools:'` must produce zero output
    - `pytest tests/unit/test_skill_tool_references.py -k aos-migration-readiness -v` exits 0 with at least 1 test passed and 0 failed
    - `pytest tests/unit/ -q` exits 0 (full unit suite green)
    - The frontmatter audit script (STEP 2) prints `Frontmatter audit PASSED — zero diff expected by D-09 confirmed`
  </acceptance_criteria>
  <done>
Frontmatter advertises `[central, aos8]`; `tools:` list is unchanged and matches every AOS8 tool referenced in the body; the targeted regression test and the full unit suite pass.
  </done>
</task>

</tasks>

<verification>
Phase-level checks (after both tasks complete):

1. Goal-backward truths verified by reading the modified skill file:
   - Exec summary instruction block precedes `## AOS migration readiness —` header inside the report template fence.
   - `### Output hygiene (mandatory)` subsection exists exactly once with all 4 prohibitions.
   - New PARTIAL decision-matrix row exists immediately after the existing paste-mode PARTIAL row.
   - `platforms: [central, aos8]` (flow-style) in frontmatter.

2. Automated:
   ```bash
   cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -v
   cd hpe-networking-mcp && uv run pytest tests/unit/ -q
   ```
   Both must exit 0.

3. Frontmatter `tools:` diff against git HEAD must be empty (D-10 — no new tool names added by Phase 13).

4. Manual content review (per VALIDATION.md "Manual-Only Verifications"):
   - Exec summary uses angle-bracket placeholders, not hard-coded values (Pitfall 1).
   - Hygiene rules appear once globally, not duplicated per-stage (Pitfall 2 / D-06).
   - Decision matrix table renders cleanly — every row has exactly 3 pipes (Pitfall 5).
</verification>

<success_criteria>
- OUTPUT-01: Executive summary instruction (with verdict + counts + SE-sentence template) is present at the top of the Stage 6 report template, before `## AOS migration readiness —`. The PARTIAL live-mode sentence template (D-08) is included.
- OUTPUT-02: `### Output hygiene (mandatory)` subsection under `## Output formatting` lists all 4 prohibitions in numbered form.
- QUALITY-01: Every `aos8_*` token referenced in skill body is present in `tools:` frontmatter (verified by audit script — expected zero diff).
- QUALITY-02: `platforms: [central, aos8]` (YAML flow-style list) in frontmatter.
- QUALITY-03: `pytest tests/unit/test_skill_tool_references.py -k aos-migration-readiness -v` passes; full unit suite (`pytest tests/unit/ -q`) passes.
- All edits in a single skill file; no Python code changes; no new tool names introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/13-executive-output-quality-gate/13-01-SUMMARY.md` documenting:
- Exact insertion points used (anchor strings + resulting line numbers post-edit) for the 3 body edits
- Frontmatter audit script output (showing zero-diff confirmation per D-09)
- Regression test output (test name + pass count + duration)
- Full unit suite pass count (expected ~790)
- Any deviations from D-01..D-11 with rationale (expected: none)
- Note that live AOS8 end-to-end runtime validation remains deferred (same partial-approval pattern as Phases 10/12 — no live AOS8 environment)
</output>
