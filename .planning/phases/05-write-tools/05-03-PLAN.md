---
phase: 05-write-tools
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py
autonomous: true
requirements:
  - WRITE-01
  - WRITE-02
  - WRITE-03
  - WRITE-04
  - WRITE-05
  - WRITE-06
  - WRITE-07
  - WRITE-08
  - WRITE-09
  - WRITE-10
  - WRITE-11
  - WRITE-12

must_haves:
  truths:
    - "TOOLS dict in aos8/__init__.py includes a 'writes' key with all 12 WRITE tool names."
    - "All pre-existing category keys remain intact and unmodified — including the Phase 4 'differentiators' key, if present (per D-02)."
    - "register_tools() reports the correct total tool count when AOS8 write tools are enabled (existing total + 12)."
    - "All tests in test_aos8_write.py pass — including test_tools_dict_includes_writes."
    - "Full unit suite stays green; no existing platform regresses."
  artifacts:
    - path: "hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py"
      provides: "TOOLS['writes'] entry with 12 tool names; importlib loop covers writes module"
      contains: "\"writes\":"
  key_links:
    - from: "platforms/aos8/__init__.py"
      to: "platforms/aos8/tools/writes.py"
      via: "importlib.import_module('hpe_networking_mcp.platforms.aos8.tools.writes') in register_tools loop"
      pattern: "importlib\\.import_module.*aos8\\.tools"
---

<objective>
Wire the 12 AOS8 WRITE tools into the platform's `TOOLS` registry so `register_tools()` imports `writes.py` and the dynamic-mode meta-tools surface them. Single edit to `aos8/__init__.py` — APPEND a new `"writes"` key to the existing TOOLS dict listing all 12 tool names. The existing importlib loop in `register_tools()` automatically picks up the new module.

Per D-02: the 5 existing read category keys (health, clients, alerts, wlan, troubleshooting) AND the Phase 4 `"differentiators"` key (if present) MUST remain untouched. This plan ADDS a key — it does not REPLACE the dict.

Purpose: Final wiring step — closes the Phase 5 contract.
Output: Updated `aos8/__init__.py` with a new `"writes"` key appended; total tool count is `(prior total) + 12`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-write-tools/05-CONTEXT.md
@.planning/phases/05-write-tools/05-RESEARCH.md
@.planning/phases/05-write-tools/05-02-SUMMARY.md
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py

<interfaces>
Current TOOLS dict shape on disk at planning time (existing — 5 keys):
```python
TOOLS: dict[str, list[str]] = {
    "health": [...8 names],
    "clients": [...4 names],
    "alerts": [...3 names],
    "wlan": [...4 names],
    "troubleshooting": [...7 names],
}
```

If Phase 4 has been executed, a sixth key `"differentiators"` may also be present.
Per D-02 (locked user decision): "Existing 5 category keys (health, clients, alerts, wlan, troubleshooting) and the Phase 4 `differentiators` key are not modified."
The executor MUST read the file before editing and PRESERVE every key found there. Do NOT replace the dict literal.

register_tools() iteration logic (existing — DO NOT change):
```python
for category, names in TOOLS.items():
    importlib.import_module(f"hpe_networking_mcp.platforms.aos8.tools.{category}")
    total += len(names)
```

So adding `"writes": [...]` to TOOLS automatically triggers `importlib.import_module("hpe_networking_mcp.platforms.aos8.tools.writes")` — no other code change required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append 'writes' key to TOOLS dict in aos8/__init__.py</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py</files>
  <read_first>
    - hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py (CURRENT TOOLS dict on disk — capture every existing key, do NOT assume a count)
    - hpe-networking-mcp/src/hpe_networking_mcp/platforms/central/__init__.py (reference for adjacent platform with write tools — confirms TOOLS-dict approach)
    - hpe-networking-mcp/tests/unit/test_aos8_write.py (test_tools_dict_includes_writes expectation — exact set of 12 names)
    - hpe-networking-mcp/tests/unit/test_aos8_init.py (existing init-module assertions to ensure compatibility)
    - .planning/phases/05-write-tools/05-CONTEXT.md (D-02: "differentiators" key MUST be preserved if present)
  </read_first>
  <behavior>
    - TOOLS["writes"] is a list[str] containing exactly these 12 names in this order: aos8_manage_ssid_profile, aos8_manage_virtual_ap, aos8_manage_ap_group, aos8_manage_user_role, aos8_manage_vlan, aos8_manage_aaa_server, aos8_manage_aaa_server_group, aos8_manage_acl, aos8_manage_netdestination, aos8_disconnect_client, aos8_reboot_ap, aos8_write_memory.
    - All pre-existing keys (the 5 read categories AND the Phase 4 `"differentiators"` key, if present) remain unchanged in identity, ordering, and content.
    - If the file on disk has 5 keys, the result is 6 keys. If the file on disk has 6 keys (Phase 4 differentiators present), the result is 7 keys.
    - register_tools() reports `(prior total) + 12` total tools — log line uses the dynamic count from `total`, no hardcoded number.
    - Importing `hpe_networking_mcp.platforms.aos8` no longer raises (writes.py exists from Plan 05-02).
  </behavior>
  <action>
    **Per locked decision D-02**, you MUST preserve every existing key in the TOOLS dict — including the Phase 4 `"differentiators"` key if Phase 4 has already been wired. DO NOT rewrite the dict literal. Use a targeted append.

    Step 1 — Read `hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py`. Identify the closing `}` of the TOOLS dict literal (the line that ends the dict, immediately before `__all__ = [...]`).

    Step 2 — Insert a new `"writes": [...]` entry as the LAST key in the dict, immediately before the closing `}`. The entry must be:
    ```python
        "writes": [
            "aos8_manage_ssid_profile",
            "aos8_manage_virtual_ap",
            "aos8_manage_ap_group",
            "aos8_manage_user_role",
            "aos8_manage_vlan",
            "aos8_manage_aaa_server",
            "aos8_manage_aaa_server_group",
            "aos8_manage_acl",
            "aos8_manage_netdestination",
            "aos8_disconnect_client",
            "aos8_reboot_ap",
            "aos8_write_memory",
        ],
    ```

    Make sure the previous last entry still ends with a trailing comma after its closing `]`.

    DO NOT touch:
    - the `"health"`, `"clients"`, `"alerts"`, `"wlan"`, `"troubleshooting"` entries (their identity, ordering, and contents are frozen).
    - the `"differentiators"` entry IF it is present in the file (Phase 4 may have added it; D-02 forbids modifying it).
    - the importlib loop in `register_tools()`.
    - the meta-tools branch.
    - the `register_tools()` signature.

    Step 3 — Update the docstring of `register_tools()`. The current docstring's `Returns:` line is:
    ```
        Returns:
            The number of underlying tools registered (always 26 in Phase 3).
    ```
    Replace it with a phase-agnostic description that does not hardcode a count:
    ```
        Returns:
            The number of underlying tools registered. Equal to the sum of
            ``len(names)`` for every entry in ``TOOLS`` (read tools plus, when
            their phases have been wired, the Phase 4 ``differentiators`` set
            and the Phase 5 ``writes`` set).
    ```
    This avoids re-encoding a fixed integer that would have to be revised again whenever a future phase appends another category.

    Step 4 — Run the Plan 05-01 write-tool tests:
    `cd hpe-networking-mcp && uv run pytest tests/unit/test_aos8_write.py -q`

    Step 5 — Run the full unit suite to confirm zero regressions:
    `cd hpe-networking-mcp && uv run pytest tests/unit/ -q`

    Anti-patterns to avoid:
    - Do NOT replace the TOOLS dict literal wholesale — that risks deleting the Phase 4 `"differentiators"` key (violates D-02).
    - Do NOT add a separate import of writes.py — the importlib loop handles it.
    - Do NOT remove or reorder any existing category key.
    - Do NOT add tool names that don't exist in writes.py — the importlib loop will succeed but the meta-tools surface will report a mismatch at runtime.
    - Do NOT hardcode an integer total in the log line — let `total` accumulate from `len(names)`.
    - Do NOT assume the dict has exactly 5 keys at edit time. Read the file first; preserve whatever is there.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_aos8_write.py tests/unit/test_aos8_init.py -q --no-header 2>&1 | tail -5</automated>
    Expected: all tests pass including test_tools_dict_includes_writes.
  </verify>
  <acceptance_criteria>
    - `grep -n "\"writes\":" hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py` returns one match.
    - `grep -E "\"aos8_(manage_(ssid_profile|virtual_ap|ap_group|user_role|vlan|aaa_server|aaa_server_group|acl|netdestination)|disconnect_client|reboot_ap|write_memory)\"" hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py | wc -l` returns 12.
    - All 5 pre-existing read keys are still present: `grep -c "\"health\":\\|\"clients\":\\|\"alerts\":\\|\"wlan\":\\|\"troubleshooting\":" hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py` returns 5.
    - D-02 preservation check (run BEFORE and AFTER the edit; must match): `cd hpe-networking-mcp && uv run python -c "from hpe_networking_mcp.platforms.aos8 import TOOLS; print(sorted(k for k in TOOLS if k != 'writes'))"` — the output before the edit and after the edit must be identical (excluding the new 'writes' key).
    - `cd hpe-networking-mcp && uv run python -c "from hpe_networking_mcp.platforms.aos8 import TOOLS; assert 'writes' in TOOLS; assert len(TOOLS['writes']) == 12; assert {'health','clients','alerts','wlan','troubleshooting'} <= set(TOOLS.keys()); print('OK')"` prints `OK`.
    - `cd hpe-networking-mcp && uv run pytest tests/unit/test_aos8_write.py::test_tools_dict_includes_writes -x -q` passes.
    - `cd hpe-networking-mcp && uv run pytest tests/unit/test_aos8_write.py -q` — all ~35 tests pass.
    - `cd hpe-networking-mcp && uv run pytest tests/unit/ -q 2>&1 | tail -3` — full suite passes; failure count is 0.
    - `cd hpe-networking-mcp && uv run ruff check src/hpe_networking_mcp/platforms/aos8/__init__.py` exits 0.
    - `cd hpe-networking-mcp && uv run ruff format --check src/hpe_networking_mcp/platforms/aos8/__init__.py` exits 0.
    - `cd hpe-networking-mcp && uv run mypy src/ --ignore-missing-imports` exits 0.
    - File line count stays well under 500: `wc -l hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py` < 100.
  </acceptance_criteria>
  <done>
    AOS8 platform surfaces all read tools plus 12 write tools; writes are gated behind ENABLE_AOS8_WRITE_TOOLS=true via the existing tag-filter machinery; all Phase 5 contract tests are green; D-02 is honored — the Phase 4 `differentiators` key (if present) is unchanged.
  </done>
</task>

</tasks>

<verification>
- `cd hpe-networking-mcp && uv run pytest tests/unit/ -q` — full suite green.
- `cd hpe-networking-mcp && uv run ruff check . && uv run ruff format --check . && uv run mypy src/ --ignore-missing-imports` exit 0.
- `cd hpe-networking-mcp && uv run python -c "from hpe_networking_mcp.platforms.aos8 import TOOLS; assert 'writes' in TOOLS and len(TOOLS['writes']) == 12; print(sum(len(v) for v in TOOLS.values()))"` prints the expected total (≥ 38; higher if Phase 4 differentiators were already wired).
- Pre-push gate (project CLAUDE.md): `docker compose -f docker-compose.yml -f docker-compose.dev.yml run --rm hpe-networking-mcp sh -c "uv run ruff check . && uv run ruff format --check . && uv run mypy src/ --ignore-missing-imports && uv run pytest tests/ -q"` succeeds.
</verification>

<success_criteria>
- TOOLS dict in `aos8/__init__.py` gains a new `"writes"` key carrying all 12 WRITE tool names.
- All previously existing keys are preserved unchanged (D-02): the 5 read categories AND the Phase 4 `"differentiators"` key if present.
- importlib loop covers `writes.py` automatically — no separate import statement added.
- All Phase 5 tests in test_aos8_write.py pass.
- Full unit suite remains green (no existing platform regressed).
- WRITE-01..WRITE-12 are now operator-callable when ENABLE_AOS8_WRITE_TOOLS=true.
</success_criteria>

<output>
After completion, create `.planning/phases/05-write-tools/05-03-SUMMARY.md` documenting: final TOOLS dict shape (record the actual key list — note explicitly whether `"differentiators"` was present), test count delta, and confirmation that ROADMAP.md Phase 5 success criteria are met. Also note that Phase 6 (Guided Prompts & Documentation) will update README/INSTRUCTIONS/TOOLS.md/CHANGELOG to surface the new tools to operators.
</output>
</content>
</invoke>