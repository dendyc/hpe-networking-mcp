---
phase: 05-write-tools
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py
autonomous: true
requirements:
  - WRITE-01
  - WRITE-02
  - WRITE-03
  - WRITE-04
  - WRITE-05
  - WRITE-06
  - WRITE-07
  - WRITE-08
  - WRITE-09
  - WRITE-10
  - WRITE-11
  - WRITE-12

must_haves:
  truths:
    - "TOOLS dict in aos8/__init__.py includes a 'writes' key with all 12 WRITE tool names."
    - "Existing 6 category keys (health, clients, alerts, wlan, troubleshooting) remain intact and unmodified."
    - "register_tools() reports 'registered 38 underlying tools' (26 read + 12 write) when AOS8 write tools are enabled."
    - "All tests in test_aos8_write.py pass — including test_tools_dict_includes_writes."
    - "Full unit suite stays green; no existing platform regresses."
  artifacts:
    - path: "hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py"
      provides: "TOOLS['writes'] entry with 12 tool names; importlib loop covers writes module"
      contains: "\"writes\":"
  key_links:
    - from: "platforms/aos8/__init__.py"
      to: "platforms/aos8/tools/writes.py"
      via: "importlib.import_module('hpe_networking_mcp.platforms.aos8.tools.writes') in register_tools loop"
      pattern: "importlib\\.import_module.*aos8\\.tools"
---

<objective>
Wire the 12 AOS8 WRITE tools into the platform's `TOOLS` registry so `register_tools()` imports `writes.py` and the dynamic-mode meta-tools surface them. Single edit to `aos8/__init__.py` — add a new `"writes"` key to the `TOOLS` dict listing all 12 tool names. The existing importlib loop in `register_tools()` automatically picks up the new module.

Purpose: Final wiring step — closes the Phase 5 contract.
Output: Updated `aos8/__init__.py` with 6 category keys (was 5) and a tool count of 38 (was 26).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-write-tools/05-CONTEXT.md
@.planning/phases/05-write-tools/05-RESEARCH.md
@.planning/phases/05-write-tools/05-02-SUMMARY.md
@hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py

<interfaces>
Current TOOLS dict shape (existing):
```python
TOOLS: dict[str, list[str]] = {
    "health": [...8 names],
    "clients": [...4 names],
    "alerts": [...3 names],
    "wlan": [...4 names],
    "troubleshooting": [...7 names],
}
```

register_tools() iteration logic (existing — DO NOT change):
```python
for category, names in TOOLS.items():
    importlib.import_module(f"hpe_networking_mcp.platforms.aos8.tools.{category}")
    total += len(names)
```

So adding `"writes": [...]` to TOOLS automatically triggers `importlib.import_module("hpe_networking_mcp.platforms.aos8.tools.writes")` — no other code change required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 'writes' key to TOOLS dict in aos8/__init__.py</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py</files>
  <read_first>
    - hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py (current TOOLS dict + register_tools function)
    - hpe-networking-mcp/src/hpe_networking_mcp/platforms/central/__init__.py (reference for adjacent platform with write tools — confirms TOOLS-dict approach)
    - hpe-networking-mcp/tests/unit/test_aos8_write.py (test_tools_dict_includes_writes expectation — exact set of 12 names)
    - hpe-networking-mcp/tests/unit/test_aos8_init.py (existing init-module assertions to ensure compatibility)
  </read_first>
  <behavior>
    - TOOLS["writes"] is a list[str] containing exactly these 12 names in this order: aos8_manage_ssid_profile, aos8_manage_virtual_ap, aos8_manage_ap_group, aos8_manage_user_role, aos8_manage_vlan, aos8_manage_aaa_server, aos8_manage_aaa_server_group, aos8_manage_acl, aos8_manage_netdestination, aos8_disconnect_client, aos8_reboot_ap, aos8_write_memory.
    - Existing 5 category keys remain unchanged in identity, ordering, and content.
    - register_tools() now reports 38 total tools (26 read + 12 write) — log line uses the dynamic count from `total`, no hardcoded number.
    - Importing `hpe_networking_mcp.platforms.aos8` no longer raises (writes.py exists from Plan 05-02).
  </behavior>
  <action>
    Edit `hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py`. Append a new `"writes"` key to the existing TOOLS dict, immediately after the `"troubleshooting"` entry (preserving alphabetical-by-execution-order grouping: read categories first, then writes last).

    The TOOLS dict must be exactly:
    ```python
    TOOLS: dict[str, list[str]] = {
        "health": [
            "aos8_get_controllers",
            "aos8_get_ap_database",
            "aos8_get_active_aps",
            "aos8_get_ap_detail",
            "aos8_get_bss_table",
            "aos8_get_radio_summary",
            "aos8_get_version",
            "aos8_get_licenses",
        ],
        "clients": [
            "aos8_get_clients",
            "aos8_find_client",
            "aos8_get_client_detail",
            "aos8_get_client_history",
        ],
        "alerts": [
            "aos8_get_alarms",
            "aos8_get_audit_trail",
            "aos8_get_events",
        ],
        "wlan": [
            "aos8_get_ssid_profiles",
            "aos8_get_virtual_aps",
            "aos8_get_ap_groups",
            "aos8_get_user_roles",
        ],
        "troubleshooting": [
            "aos8_ping",
            "aos8_traceroute",
            "aos8_show_command",
            "aos8_get_logs",
            "aos8_get_controller_stats",
            "aos8_get_arm_history",
            "aos8_get_rf_monitor",
        ],
        "writes": [
            "aos8_manage_ssid_profile",
            "aos8_manage_virtual_ap",
            "aos8_manage_ap_group",
            "aos8_manage_user_role",
            "aos8_manage_vlan",
            "aos8_manage_aaa_server",
            "aos8_manage_aaa_server_group",
            "aos8_manage_acl",
            "aos8_manage_netdestination",
            "aos8_disconnect_client",
            "aos8_reboot_ap",
            "aos8_write_memory",
        ],
    }
    ```

    Update the docstring of `register_tools()` to reflect the new total. Replace this line:
    ```
        Returns:
            The number of underlying tools registered (always 26 in Phase 3).
    ```
    with:
    ```
        Returns:
            The number of underlying tools registered (38 after Phase 5: 26 read + 12 write).
    ```

    No other changes. Do NOT modify the importlib loop, the register_tools signature, the meta-tools branch, or any of the read tool entries.

    Then run the full Plan 05-01 test suite to confirm everything turns green:
    `cd hpe-networking-mcp && uv run pytest tests/unit/test_aos8_write.py -q`

    Then run the full unit suite to confirm zero regressions:
    `cd hpe-networking-mcp && uv run pytest tests/unit/ -q`

    Expected new test count: 693 (existing) + ~35 (new write tests) = ~728 passing.

    Anti-patterns to avoid:
    - Do NOT add a separate import of writes.py — the importlib loop handles it.
    - Do NOT remove or reorder the existing 5 category keys.
    - Do NOT add tool names that don't exist in writes.py — the importlib loop will succeed but the meta-tools surface will report a mismatch at runtime.
    - Do NOT hardcode the integer 38 in the log line — let `total` accumulate from `len(names)`.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_aos8_write.py tests/unit/test_aos8_init.py -q --no-header 2>&1 | tail -5</automated>
    Expected: all tests pass including test_tools_dict_includes_writes.
  </verify>
  <acceptance_criteria>
    - `grep -n "\"writes\":" hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py` returns one match.
    - `grep -E "\"aos8_(manage_(ssid_profile|virtual_ap|ap_group|user_role|vlan|aaa_server|aaa_server_group|acl|netdestination)|disconnect_client|reboot_ap|write_memory)\"" hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py | wc -l` returns 12.
    - `grep -c "\"health\":\\|\"clients\":\\|\"alerts\":\\|\"wlan\":\\|\"troubleshooting\":\\|\"writes\":" hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py` returns 6.
    - `cd hpe-networking-mcp && uv run python -c "from hpe_networking_mcp.platforms.aos8 import TOOLS; assert set(TOOLS.keys()) == {'health','clients','alerts','wlan','troubleshooting','writes'}; assert len(TOOLS['writes']) == 12; assert sum(len(v) for v in TOOLS.values()) == 38; print('OK')"` prints `OK`.
    - `cd hpe-networking-mcp && uv run pytest tests/unit/test_aos8_write.py::test_tools_dict_includes_writes -x -q` passes.
    - `cd hpe-networking-mcp && uv run pytest tests/unit/test_aos8_write.py -q` — all ~35 tests pass.
    - `cd hpe-networking-mcp && uv run pytest tests/unit/ -q 2>&1 | tail -3` — full suite passes; failure count is 0; pass count is ≥ 728.
    - `cd hpe-networking-mcp && uv run ruff check src/hpe_networking_mcp/platforms/aos8/__init__.py` exits 0.
    - `cd hpe-networking-mcp && uv run ruff format --check src/hpe_networking_mcp/platforms/aos8/__init__.py` exits 0.
    - `cd hpe-networking-mcp && uv run mypy src/ --ignore-missing-imports` exits 0.
    - File line count stays well under 500: `wc -l hpe-networking-mcp/src/hpe_networking_mcp/platforms/aos8/__init__.py` < 100.
  </acceptance_criteria>
  <done>
    AOS8 platform surfaces 38 tools (26 read + 12 write); writes are gated behind ENABLE_AOS8_WRITE_TOOLS=true via the existing tag-filter machinery; all Phase 5 contract tests are green.
  </done>
</task>

</tasks>

<verification>
- `cd hpe-networking-mcp && uv run pytest tests/unit/ -q` — full suite green; total ≥ 728 passing.
- `cd hpe-networking-mcp && uv run ruff check . && uv run ruff format --check . && uv run mypy src/ --ignore-missing-imports` exit 0.
- `cd hpe-networking-mcp && uv run python -c "from hpe_networking_mcp.platforms.aos8 import TOOLS; print(sum(len(v) for v in TOOLS.values()))"` prints `38`.
- Pre-push gate (project CLAUDE.md): `docker compose -f docker-compose.yml -f docker-compose.dev.yml run --rm hpe-networking-mcp sh -c "uv run ruff check . && uv run ruff format --check . && uv run mypy src/ --ignore-missing-imports && uv run pytest tests/ -q"` succeeds.
</verification>

<success_criteria>
- TOOLS dict in `aos8/__init__.py` has 6 keys (was 5) with `"writes"` carrying all 12 WRITE tool names.
- importlib loop covers `writes.py` automatically — no separate import statement added.
- All Phase 5 tests in test_aos8_write.py pass.
- Full unit suite remains green (no existing platform regressed).
- WRITE-01..WRITE-12 are now operator-callable when ENABLE_AOS8_WRITE_TOOLS=true.
</success_criteria>

<output>
After completion, create `.planning/phases/05-write-tools/05-03-SUMMARY.md` documenting: final TOOLS dict shape (6 keys, 38 tools), test count delta (693 → ~728), and confirmation that ROADMAP.md Phase 5 success criteria are met. Also note that Phase 6 (Guided Prompts & Documentation) will update README/INSTRUCTIONS/TOOLS.md/CHANGELOG to surface the new tools to operators.
</output>
