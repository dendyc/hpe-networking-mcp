---
phase: 12-central-enrichment-cutover-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md
autonomous: true
requirements: [ENRICH-01, ENRICH-02, ENRICH-03, ENRICH-04]
must_haves:
  truths:
    - "Skill Stage 4 contains a `##### AOS8 live-mode sub-path — Central enrichment` block above the existing A1–A13 table"
    - "ENRICH-01 finding cites both `aos8_get_ap_database()` (Batch 2) and `central_get_aps()` and uses INFO severity"
    - "ENRICH-02 prose calls `central_recommend_firmware()` exactly once with no filter and emits a per-model AP table"
    - "ENRICH-03 prose lists one REGRESSION finding per conflicting SSID, citing `aos8_get_bss_table()` (Batch 4) and `central_get_wlan_profiles()`"
    - "ENRICH-04 prose lists one REGRESSION finding per conflicting role and one per conflicting VLAN ID, citing `aos8_get_effective_config()` (Batch 1), `central_get_roles()`, and `central_get_named_vlans()`"
    - "All ENRICH-03 / ENRICH-04 conflict findings use REGRESSION severity (not INFO copied from A7/A8/A9)"
    - "Existing A1–A13 paste-fallback table remains intact and unchanged"
    - "`pytest tests/unit/test_skill_tool_references.py` is green after the edit"
  artifacts:
    - path: "hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md"
      provides: "Stage 4 live-mode sub-path covering ENRICH-01..04"
      contains: "AOS8 live-mode sub-path — Central enrichment"
  key_links:
    - from: "Stage 4 sub-path prose"
      to: "Stage 1 Batch 2 / Batch 4 / Batch 1 context"
      via: "in-context references — no re-fetch"
      pattern: "Batch 2|Batch 4|Batch 1"
    - from: "Stage 4 sub-path prose"
      to: "Central tool catalog"
      via: "tool name references picked up by `_TOOL_REF_PATTERN`"
      pattern: "central_(recommend_firmware|get_aps|get_wlan_profiles|get_roles|get_named_vlans)"
---

<objective>
Insert a self-contained `##### AOS8 live-mode sub-path — Central enrichment (ENRICH-01..04)` block above the existing A1–A13 table in Stage 4 of `aos-migration-readiness.md`. The sub-path narrates four checks driven by AOS8 Stage 1 batch context plus a small set of Central tool calls — AP count gap (INFO), per-model firmware recommendation table (one fleet-wide call), SSID conflicts (REGRESSION, per item), and role/VLAN conflicts (REGRESSION, per item). The existing A1–A13 paste-fallback table remains intact and unchanged.

Purpose: ENRICH-01..04 — surface AP migration scope, AOS10 firmware targets, and naming collisions automatically when AOS8 live mode is active, removing four operator-paste enrichment steps and upgrading naming-conflict severity from INFO to REGRESSION.
Output: One markdown insert (~40–60 lines) inside Stage 4 of the skill file; pytest skill-reference test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-central-enrichment-cutover-validation/12-CONTEXT.md
@.planning/phases/12-central-enrichment-cutover-validation/12-RESEARCH.md
@.planning/phases/11-live-vsg-rules/11-01-SUMMARY.md
@hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Stage 4 AOS8 live-mode sub-path covering ENRICH-01..04</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md</files>
  <read_first>
    - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md (full file — focus on lines 230–273 for Stage 3 sub-path canonical pattern, and lines 360–379 for Stage 4 A1–A13 table location)
    - .planning/phases/12-central-enrichment-cutover-validation/12-CONTEXT.md (locked decisions D-01..D-08)
    - .planning/phases/12-central-enrichment-cutover-validation/12-RESEARCH.md (Pattern 1, 2, 3, 4 templates and Anti-Patterns section)
    - .planning/phases/11-live-vsg-rules/11-01-SUMMARY.md (Phase 11 sub-path implementation reference — same skill file, same pattern)
  </read_first>
  <action>
Locate Stage 4 in the skill file. Stage 4 begins at the `### Stage 4 — Central-side API checks (no operator paste needed)` heading (currently line 360). The first item under it is the existing A1–A13 markdown table (paste-fallback path) that begins at the table header row `| ID | Tool | What it confirms | Action if regression / drift |`.

Insert the following block IMMEDIATELY AFTER the `### Stage 4 — Central-side API checks (no operator paste needed)` heading and any existing intro paragraph, and BEFORE the A1 table header row. Do NOT modify, reorder, or delete any existing A1–A13 table content. Per D-01, the existing table stays intact as the paste fallback.

Block to insert verbatim (preserve markdown formatting, code fences, and exact tool names):

```markdown
#### AOS8 live-mode sub-path — Central enrichment (ENRICH-01..04, used when Stage -1 announced "AOS8 API mode")

When AOS8 live mode is active, the four Central enrichment checks (AP count gap, per-model AOS10 firmware recommendations, SSID conflicts, role/VLAN conflicts) are evaluated using AOS8 Stage 1 batch data already in context plus a small set of Central tool calls — no operator paste, no re-fetching of AOS8 batches. After this sub-path, continue with the A1–A13 Central API checks below; A4 / A7 / A8 / A9 are superseded by this sub-path's findings when live mode is active.

Each finding below uses the format **Severity — Description. (source: `tool_call(args)`, Batch N where applicable)**.

##### ENRICH-01 — AP count gap (INFO)

From the Batch 2 `aos8_get_ap_database()` response already in context, count total APs (call this `X`). Then call `central_get_aps()` with no filter and count the response (call this `Y`). Compute `Z = X - Y` (Z may be negative if Central reports more — surface that as-is). Emit one finding:

- **INFO** — Source AP count: `X`. Central onboarded: `Y`. Gap: `Z` not yet onboarded. (source: `aos8_get_ap_database()` Batch 2 + `central_get_aps()`)

*If `central_get_aps()` fails:* AP count gap (ENRICH-01) cannot complete from live data. Fall back to A4 in the table below.
*If Batch 2 was unavailable:* AOS8 AP count is unknown — operator paste of `show ap database long` required for ENRICH-01.

##### ENRICH-02 — Per-model AOS10 firmware recommendation (INFO)

Make **one call**, fleet-wide, no filter: `central_recommend_firmware()`. Central returns a fleet-wide recommendation list already grouped by device model — do NOT iterate per model. Extract the distinct AP `model` values from the Batch 2 `aos8_get_ap_database()` response, then for each distinct model walk the `central_recommend_firmware()` response and pull the recommended AOS10 firmware version for that model. Emit a compact two-column markdown table inline:

| AP Model | Recommended AOS10 Firmware |
|----------|----------------------------|
| `<model>` | `<version-from-central>` |
| `<model>` | `<version-from-central>` |

(source: distinct models from `aos8_get_ap_database()` Batch 2; recommendations from `central_recommend_firmware()`)

If Central's recommendation list omits a model present in the AOS8 fleet, emit one row per missing model with the value `no recommendation returned — verify with Aruba support`. Restrict the table to AP models only — controller firmware is owned by CUTOVER-02 in Stage 5, not by this rule.

*If `central_recommend_firmware()` fails:* per-model firmware recommendation (ENRICH-02) cannot complete. Fall back to A13 in the table below.
*If Batch 2 was unavailable:* distinct AP models are unknown — operator paste of `show ap database long` required for ENRICH-02.

##### ENRICH-03 — SSID conflict detection (REGRESSION, one finding per conflict)

From the Batch 4 `aos8_get_bss_table()` response already in context, extract the distinct AOS8 SSID names. Call `central_get_wlan_profiles()` with no `ssid` argument to retrieve the full Central WLAN profile list. For every AOS8 SSID name that already appears as a profile in the Central response, emit ONE finding (do not collapse multiple conflicts into a single bullet):

- **REGRESSION** — SSID `"<ssid_name>"` already exists in Central (profile present in `central_get_wlan_profiles()` response). A conflicting SSID name in Central blocks a clean migration. (source: `aos8_get_bss_table()` Batch 4 + `central_get_wlan_profiles()`)

If no AOS8 SSID names match any Central WLAN profile, emit a single INFO bullet: `INFO — No SSID conflicts detected against existing Central WLAN profiles. (source: aos8_get_bss_table() Batch 4 + central_get_wlan_profiles())`.

*If `central_get_wlan_profiles()` fails:* SSID conflict check (ENRICH-03) cannot complete. Fall back to A7 in the table below.
*If Batch 4 was unavailable:* AOS8 SSID list is unknown — operator paste of `show ap essid` required for ENRICH-03.

##### ENRICH-04 — Role and VLAN conflict detection (REGRESSION, one finding per conflict)

From the Batch 1 `aos8_get_effective_config()` response already in context, extract:
- distinct user-role names (from `user_role` / `aaa.user_role` objects)
- distinct named VLAN IDs (from `vlan` / `interface vlan` objects)

Call `central_get_roles()` with no `name` argument and `central_get_named_vlans()` with no `name` argument to retrieve the full Central role list and VLAN list. For every AOS8 role name that already appears in the Central role response, emit ONE finding:

- **REGRESSION** — Role `"<role_name>"` already exists in Central. A conflicting role name in Central blocks a clean migration. (source: `aos8_get_effective_config()` Batch 1 + `central_get_roles()`)

For every AOS8 VLAN ID that already appears as a named VLAN in the Central response, emit ONE finding:

- **REGRESSION** — Named VLAN ID `<vlan_id>` already mapped in Central (existing name: `"<central-vlan-name>"`). A conflicting VLAN ID in Central blocks a clean migration. (source: `aos8_get_effective_config()` Batch 1 + `central_get_named_vlans()`)

If no role names or VLAN IDs collide, emit a single INFO bullet: `INFO — No role or VLAN conflicts detected against existing Central roles / named VLANs. (source: aos8_get_effective_config() Batch 1 + central_get_roles() + central_get_named_vlans())`.

*If `central_get_roles()` fails:* role conflict check cannot complete. Fall back to A8 in the table below.
*If `central_get_named_vlans()` fails:* VLAN conflict check cannot complete. Fall back to A9 in the table below.
*If Batch 1 was unavailable:* AOS8 role names / VLAN IDs are unknown — operator paste of `show configuration effective detail` required for ENRICH-04.

After this sub-path, continue with the A1–A13 table below for any checks not superseded by ENRICH-01..04.
```

Constraints (per D-01..D-08, RESEARCH.md Anti-Patterns):
- Header level is exactly `####` (four hashes) for the sub-path container, matching the Stage 3 sub-path style at line 230. Per-rule headers below it are `#####` (five hashes), matching Stage 3 lines 236, 246, 258, 264.
- Insert ABOVE the A1–A13 table — never below or in place of it.
- Do NOT modify the A1–A13 table contents or any row severity.
- Do NOT touch the `tools:` frontmatter at line 23 — every Central + AOS8 tool name in this block is already declared there (verified RESEARCH.md "Reusable Assets").
- Do NOT iterate `central_recommend_firmware()` per model — single fleet-wide call only (D-05).
- Do NOT downgrade conflict severity to INFO — all naming conflicts are REGRESSION (D-07).
- Do NOT collapse multiple conflicts into a single bullet — one finding per conflicting item (D-08).
- Do NOT add re-fetch instructions for AOS8 data — Batch 1, Batch 2, and Batch 4 are already in context (D-03).
- Do NOT touch Stage 5, Stage 6, or anything below Stage 5's last existing line.
- Tool names referenced (must spell exactly, all are already in skill `tools:` frontmatter line 23): `aos8_get_ap_database`, `aos8_get_bss_table`, `aos8_get_effective_config`, `central_get_aps`, `central_recommend_firmware`, `central_get_wlan_profiles`, `central_get_roles`, `central_get_named_vlans`.

After inserting the block, run the skill regression test from the repo root:
```bash
cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -q
```
The test must exit 0. If it reports an unknown tool reference, the most likely cause is a typo — recheck against the eight tool names above.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AOS8 live-mode sub-path — Central enrichment" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns `1`
    - `grep -c "ENRICH-01 — AP count gap" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns `1`
    - `grep -c "ENRICH-02 — Per-model AOS10 firmware recommendation" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns `1`
    - `grep -c "ENRICH-03 — SSID conflict detection" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns `1`
    - `grep -c "ENRICH-04 — Role and VLAN conflict detection" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns `1`
    - The new sub-path block appears BEFORE the line containing `| ID | Tool | What it confirms | Action if regression / drift |` (verifiable with `grep -n` line numbers — sub-path heading line < table header line, both inside Stage 4)
    - File contains the literal substring `central_recommend_firmware()` (no-arg call) and does NOT contain `central_recommend_firmware(serial_number=` or `for each model, call central_recommend_firmware`
    - File contains the literal substring `central_get_wlan_profiles()` (no-arg call)
    - File contains the literal substring `central_get_roles()` (no-arg call)
    - File contains the literal substring `central_get_named_vlans()` (no-arg call)
    - File contains at least three occurrences of `**REGRESSION** —` inside the new sub-path block (one each for SSID, role, and VLAN conflict templates)
    - File contains the literal substring `**INFO** — Source AP count:` (ENRICH-01 finding template)
    - The 13 existing A-rows (`A1` through `A13`) all remain present in Stage 4: `grep -E "^\| A[0-9]+ \|" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md | wc -l` returns `13`
    - The string `tools:` appears at line 23 of the file unchanged (`sed -n '23p'` output identical to pre-edit) — confirm by diffing line 23 only
    - `cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -q` exits 0
  </acceptance_criteria>
  <done>Stage 4 sub-path inserted above the A1–A13 table; ENRICH-01..04 covered with locked severities and tool-name citations; existing paste-fallback table untouched; skill-reference pytest green.</done>
</task>

</tasks>

<verification>
After the task completes:

1. `cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -q` exits 0.
2. `cd hpe-networking-mcp && uv run pytest tests/unit/ -q` exits 0 (full unit suite — no regression).
3. Manual diff inspection: only the skill file is modified; the diff is contained between Stage 4's existing heading and the A1 table header row; no lines below Stage 4 (Stage 5 / Stage 6 / Decision matrix) are touched.
4. Frontmatter `tools:` line at line 23 is byte-identical to pre-edit.
</verification>

<success_criteria>
- ENRICH-01..04 are each addressed by a discrete prose block within the new Stage 4 sub-path.
- All four ENRICH requirements cite the exact tool names listed in the skill `tools:` frontmatter (no typos, no aliases).
- Severity locks honored: ENRICH-01 INFO, ENRICH-02 INFO (table presentation), ENRICH-03 REGRESSION per conflict, ENRICH-04 REGRESSION per conflict.
- `central_recommend_firmware()` invoked exactly once, fleet-wide, no filter.
- Existing A1–A13 paste-fallback table preserved verbatim.
- `tests/unit/test_skill_tool_references.py` green; full unit suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/12-central-enrichment-cutover-validation/12-01-SUMMARY.md` documenting:
- Exact line range of the inserted block
- Tool names introduced into Stage 4 (confirmed already in `tools:` frontmatter)
- Severity decisions applied (D-04, D-07, D-14)
- pytest output confirming green skill-reference + full unit suite
- Any deviations from the plan with justification
</output>
