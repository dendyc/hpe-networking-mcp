---
phase: 12-central-enrichment-cutover-validation
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md
autonomous: true
requirements: [CUTOVER-01, CUTOVER-02, CUTOVER-03]
must_haves:
  truths:
    - "Skill Stage 5 contains a `##### AOS8 live-mode sub-path — cutover prerequisites` block before the existing Phase 0–8 cutover table"
    - "CUTOVER-01 finding flags REGRESSION on any cluster state other than L2-connected, citing `aos8_get_cluster_state()` (Batch 3)"
    - "CUTOVER-02 prose makes a FRESH call to `aos8_show_command(command='show version')` (not Batch 3 reuse) and flags REGRESSION below 8.10.0.12 / 8.12.0.1, citing VSG §1643-§1649"
    - "CUTOVER-03 finding emits an INFO pre-cutover AP baseline using `aos8_get_ap_database()` Batch 2 count"
    - "Existing Phase 0–8 cutover table remains intact and unchanged"
    - "`tools:` frontmatter line 23 is unchanged"
    - "`pytest tests/unit/test_skill_tool_references.py` is green after the edit; full unit suite is green"
  artifacts:
    - path: "hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md"
      provides: "Stage 5 live-mode sub-path covering CUTOVER-01..03"
      contains: "AOS8 live-mode sub-path — cutover prerequisites"
  key_links:
    - from: "Stage 5 sub-path prose"
      to: "Stage 1 Batch 2 / Batch 3 context"
      via: "in-context references for cluster state and AP count baseline"
      pattern: "Batch 2|Batch 3"
    - from: "Stage 5 sub-path prose"
      to: "AOS8 tool catalog"
      via: "fresh `show version` call + cluster + AP database refs"
      pattern: "aos8_(show_command|get_cluster_state|get_ap_database)"
---

<objective>
Insert a self-contained `##### AOS8 live-mode sub-path — cutover prerequisites (CUTOVER-01..03)` block before the existing Phase 0–8 cutover table in Stage 5 of `aos-migration-readiness.md`. The sub-path narrates three checks: live cluster L2-connected health (REGRESSION on anything else), a FRESH controller firmware floor call against the 8.10.0.12 / 8.12.0.1 prerequisite (REGRESSION below floor, VSG §1643-§1649), and the pre-cutover AP-count baseline (INFO). The existing Phase 0–8 cutover table remains intact and unchanged.

Purpose: CUTOVER-01..03 — eliminate operator-paste prerequisites for cluster health, firmware floor, and AP baseline; auto-flag the firmware floor; provide a deterministic post-cutover AP diff baseline.
Output: One markdown insert (~25–40 lines) inside Stage 5 of the skill file; pytest skill-reference test green; full unit suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-central-enrichment-cutover-validation/12-CONTEXT.md
@.planning/phases/12-central-enrichment-cutover-validation/12-RESEARCH.md
@.planning/phases/12-central-enrichment-cutover-validation/12-01-SUMMARY.md
@hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Stage 5 AOS8 live-mode sub-path covering CUTOVER-01..03</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md</files>
  <read_first>
    - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md (full file — focus on lines 230–273 for the Stage 3 sub-path canonical pattern, lines 360–395 for the Stage 4 layout produced by 12-01 and the Stage 5 Phase 0–8 table location)
    - .planning/phases/12-central-enrichment-cutover-validation/12-CONTEXT.md (locked decisions D-09..D-14)
    - .planning/phases/12-central-enrichment-cutover-validation/12-RESEARCH.md (Pitfall 2 — controller firmware MUST be from a fresh `show version`, never from Batch 3)
    - .planning/phases/12-central-enrichment-cutover-validation/12-01-SUMMARY.md (confirm exact post-12-01 line numbers for Stage 5 boundaries)
  </read_first>
  <action>
Locate Stage 5 in the skill file. Stage 5 begins at the `### Stage 5 — Cutover sequencing + rollback validation` heading (currently line 380 prior to 12-01; recompute after 12-01 by searching for the literal heading). The first item under it is the existing Phase 0–8 cutover markdown table (paste-fallback path).

Insert the following block IMMEDIATELY AFTER the `### Stage 5 — Cutover sequencing + rollback validation` heading (and any short intro paragraph if present), and BEFORE the Phase 0 table row of the cutover table. Do NOT modify, reorder, or delete any existing Phase 0–8 table content. Per D-09, the existing cutover table stays intact for paste paths.

Block to insert verbatim (preserve markdown formatting, code fences, and exact tool names):

```markdown
#### AOS8 live-mode sub-path — cutover prerequisites (CUTOVER-01..03, used when Stage -1 announced "AOS8 API mode")

When AOS8 live mode is active, three cutover prerequisites — cluster L2-connected health, controller firmware version floor, and pre-cutover AP-count baseline — are evaluated before the operator walks through the Phase 0–8 cutover sequence below. Two checks reuse Stage 1 batch data already in context (no re-fetch); the firmware floor check makes one **fresh** `aos8_show_command(command='show version')` call because controller firmware version is not reliably surfaced by `show inventory` (Batch 3).

Each finding below uses the format **Severity — Description. (source: `tool_call(args)`, Batch N where applicable) (VSG §anchor when applicable)**.

##### CUTOVER-01 — Live cluster health (REGRESSION on anything other than L2-connected)

From the Batch 3 `aos8_get_cluster_state()` response already in context, inspect the cluster state field. If the state is anything other than `L2-connected`, emit:

- **REGRESSION** — Cluster not L2-connected: `<state>`. Resolve before single-controller upgrade. (source: `aos8_get_cluster_state()`, Batch 3)

If the state is `L2-connected`, emit a single INFO confirmation: `INFO — Cluster L2-connected (CUTOVER-01 PASS). (source: aos8_get_cluster_state(), Batch 3)`.

*If Batch 3 was unavailable:* cluster health (CUTOVER-01) cannot be evaluated from live data. Mark as **inconclusive — paste required** and consult any pasted `show lc-cluster group-membership` output.

##### CUTOVER-02 — Controller firmware floor (REGRESSION below 8.10.0.12 / 8.12.0.1, VSG §1643-§1649)

Make a **fresh** call to `aos8_show_command(command='show version')` here in Stage 5 — do NOT pull firmware from Batch 3 / `show inventory`, which does not reliably include the running OS firmware string. Parse the controller firmware version from the response. Compare against the prerequisite floor: the running version must be at least `8.10.0.12` on the 8.10 train OR at least `8.12.0.1` on the 8.12 train. If the running firmware is below both floors, emit:

- **REGRESSION** — Controller firmware `<running-version>` is below the 8.10.0.12 / 8.12.0.1 prerequisite. Upgrade the controller to a supported floor before AOS 10 migration. (source: `aos8_show_command(command='show version')`) (VSG §1643-§1649)

If the running firmware meets or exceeds the floor, emit a single INFO confirmation: `INFO — Controller firmware <running-version> meets the 8.10.0.12 / 8.12.0.1 prerequisite (CUTOVER-02 PASS). (source: aos8_show_command(command='show version')) (VSG §1643-§1649)`.

*If `aos8_show_command(command='show version')` fails:* firmware floor (CUTOVER-02) cannot be evaluated. Mark as **inconclusive — paste required** and consult any pasted `show version` output.

##### CUTOVER-03 — Pre-cutover AP-count baseline (INFO)

From the Batch 2 `aos8_get_ap_database()` response already in context, count total APs (call this `X`). Emit:

- **INFO** — Pre-cutover AP baseline: `X` APs. (source: `aos8_get_ap_database()`, Batch 2) Use this count for post-cutover diff.

*If Batch 2 was unavailable:* AP-count baseline (CUTOVER-03) cannot be captured from live data. Operator paste of `show ap database long` required for the baseline.

After this sub-path, continue with the Phase 0–8 cutover table below — those steps apply regardless of source path.
```

Constraints (per D-09..D-14, RESEARCH.md Pitfalls 2 + 7):
- Header level is exactly `####` (four hashes) for the sub-path container, matching the Stage 3 / Stage 4 sub-path style. Per-rule headers are `#####`.
- Insert ABOVE (before) the Phase 0–8 cutover table — never below or in place of it.
- Do NOT modify the Phase 0–8 table contents.
- Do NOT touch the `tools:` frontmatter at line 23 — `aos8_show_command`, `aos8_get_cluster_state`, and `aos8_get_ap_database` are all already declared there (verified RESEARCH.md "Reusable Assets").
- CUTOVER-02 MUST call `aos8_show_command(command='show version')` literally — this is a fresh call, NOT a reuse of Batch 3. The prose must NOT say "from Batch 3" anywhere near CUTOVER-02. Pulling firmware from Batch 3 produces silently wrong findings (RESEARCH.md Pitfall 2).
- Do NOT touch Stage 6, the Decision matrix, or the Output formatting section — those are Phase 13 scope (RESEARCH.md Pitfall 7).
- Tool names referenced (must spell exactly, all are already in skill `tools:` frontmatter line 23): `aos8_get_cluster_state`, `aos8_show_command`, `aos8_get_ap_database`.

After inserting the block, run the skill regression test and full unit suite from the repo root:
```bash
cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -q
cd hpe-networking-mcp && uv run pytest tests/unit/ -q
```
Both must exit 0.
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -q && cd hpe-networking-mcp && uv run pytest tests/unit/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AOS8 live-mode sub-path — cutover prerequisites" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns `1`
    - `grep -c "CUTOVER-01 — Live cluster health" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns `1`
    - `grep -c "CUTOVER-02 — Controller firmware floor" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns `1`
    - `grep -c "CUTOVER-03 — Pre-cutover AP-count baseline" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` returns `1`
    - File contains the literal substring `aos8_show_command(command='show version')` at least twice (CUTOVER-02 finding template + INFO PASS template)
    - File contains the literal substring `8.10.0.12` and `8.12.0.1` and `VSG §1643-§1649` inside the Stage 5 sub-path block
    - File contains the literal substring `Pre-cutover AP baseline:` (CUTOVER-03 INFO template)
    - File contains the literal substring `Cluster not L2-connected:` (CUTOVER-01 REGRESSION template)
    - The Stage 5 sub-path block does NOT contain the substring `Batch 3` within 200 characters of the substring `firmware` (verifies CUTOVER-02 does not pull firmware from Batch 3 — RESEARCH.md Pitfall 2). Confirm with: a manual read of the inserted block, no occurrence of "firmware...Batch 3" or "Batch 3...firmware" in proximity except inside the cluster-health prose.
    - The new sub-path heading line number is LESS THAN the line number of the first row of the existing cutover table (`grep -n "Phase 0" hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` first match line > sub-path heading line)
    - `sed -n '23p' hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` is byte-identical to the pre-edit value (i.e., `tools:` frontmatter line 23 unchanged)
    - Stage 6, Decision matrix, and Output formatting sections are unchanged: `git diff hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md` shows no changes after the line containing `### Stage 6` heading
    - `cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -q` exits 0
    - `cd hpe-networking-mcp && uv run pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
  <done>Stage 5 sub-path inserted before the Phase 0–8 cutover table; CUTOVER-01..03 covered with locked severities, fresh `show version` call, and VSG anchor citation; existing Phase 0–8 table untouched; Stage 6 / Decision matrix / Output formatting untouched; full unit suite green.</done>
</task>

</tasks>

<verification>
After the task completes:

1. `cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -q` exits 0.
2. `cd hpe-networking-mcp && uv run pytest tests/unit/ -q` exits 0 (full unit suite).
3. Manual diff inspection confirms: only the skill file is modified; the diff is contained between Stage 5's existing heading and the Phase 0 table row; no lines below Stage 5 (Stage 6 / Decision matrix / Output formatting) are touched.
4. Frontmatter `tools:` line at line 23 is byte-identical to pre-edit.
5. CUTOVER-02 prose does NOT pull firmware from Batch 3 — fresh `aos8_show_command(command='show version')` call is the sole source for the firmware-floor finding.
</verification>

<success_criteria>
- CUTOVER-01..03 are each addressed by a discrete prose block within the new Stage 5 sub-path.
- All three CUTOVER requirements cite the exact tool names listed in the skill `tools:` frontmatter.
- Severity locks honored: CUTOVER-01 REGRESSION on non-L2-connected, CUTOVER-02 REGRESSION below firmware floor with VSG §1643-§1649 cite, CUTOVER-03 INFO baseline.
- CUTOVER-02 makes a fresh `aos8_show_command(command='show version')` call, never reusing Batch 3.
- Existing Phase 0–8 cutover table preserved verbatim.
- Stage 6 + Decision matrix + Output formatting untouched (Phase 13 scope).
- `tests/unit/test_skill_tool_references.py` green; full unit suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/12-central-enrichment-cutover-validation/12-02-SUMMARY.md` documenting:
- Exact line range of the inserted block
- Confirmation that all three AOS8 tools used are pre-existing in `tools:` frontmatter
- Severity decisions applied (D-11, D-12, D-14)
- Confirmation that CUTOVER-02 uses fresh `show version` (not Batch 3)
- pytest output confirming green skill-reference + full unit suite
- Any deviations from the plan with justification
</output>
