---
phase: 260429-ta0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md
  - hpe-networking-mcp/tests/unit/test_skill_tool_references.py
autonomous: true
requirements: [ENRICH-02]
must_haves:
  truths:
    - "The Stage 4 AOS8 live-mode sub-path intro sentence lists A4 / A7 / A8 / A9 / A13 as superseded when live mode is active"
    - "The meta-tool catalog loop in test_skill_tool_references.py includes aos8 alongside the other six platforms"
  artifacts:
    - path: "hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md"
      provides: "Updated supersession sentence at line 366"
      contains: "A4 / A7 / A8 / A9 / A13 are superseded"
    - path: "hpe-networking-mcp/tests/unit/test_skill_tool_references.py"
      provides: "aos8 included in meta-tool platform tuple"
      contains: '"aos8"'
  key_links:
    - from: "aos-migration-readiness.md line 366"
      to: "A13 row in the A1–A13 table"
      via: "supersession sentence mentioning A13"
      pattern: "A13 are superseded"
---

<objective>
Fix two small polish issues identified during the v1.1 milestone audit.

Purpose: Prevent an AI in AOS8 live mode from calling `central_recommend_firmware()` a second time when it reaches the A13 row, and make the meta-tool catalog loop in the skill regression test consistent with all registered platforms.
Output: Updated skill file (one word added to one sentence) and updated test file (one platform string added to one tuple).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add A13 to the ENRICH-02 supersession sentence</name>
  <files>hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md</files>
  <action>
    At line 366, the sentence currently reads:

    "After this sub-path, continue with the A1–A13 Central API checks below; A4 / A7 / A8 / A9 are superseded by this sub-path's findings when live mode is active."

    Change it to:

    "After this sub-path, continue with the A1–A13 Central API checks below; A4 / A7 / A8 / A9 / A13 are superseded by this sub-path's findings when live mode is active."

    No other changes to the file. A13 is added because ENRICH-02 already calls `central_recommend_firmware()` — the same function as A13 — so A13 must be skipped when live mode is active to avoid a duplicate call.
  </action>
  <verify>
    <automated>grep -n "A13 are superseded" "hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md"</automated>
  </verify>
  <done>"A4 / A7 / A8 / A9 / A13 are superseded by this sub-path's findings when live mode is active." appears at line 366.</done>
</task>

<task type="auto">
  <name>Task 2: Add "aos8" to meta-tool platform tuple in skill regression test</name>
  <files>hpe-networking-mcp/tests/unit/test_skill_tool_references.py</files>
  <action>
    At line 146, the tuple currently reads:

    `for platform in ("mist", "central", "greenlake", "clearpass", "apstra", "axis"):`

    Change it to:

    `for platform in ("mist", "central", "greenlake", "clearpass", "apstra", "axis", "aos8"):`

    No other changes. AOS8 currently uses static mode (meta-tools not yet registered), so this does not cause a test failure — it only makes the catalog consistent and ensures future dynamic AOS8 meta-tools are recognized without a silent miss.

    After editing, run the full unit suite to confirm no regressions:
    `cd hpe-networking-mcp && uv run pytest tests/unit/ -q`
  </action>
  <verify>
    <automated>cd hpe-networking-mcp && uv run pytest tests/unit/test_skill_tool_references.py -q</automated>
  </verify>
  <done>The tuple includes "aos8", all skill regression tests pass (0 failures).</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -n "A13 are superseded" "hpe-networking-mcp/src/hpe_networking_mcp/skills/aos-migration-readiness.md"` returns the updated line.
2. `grep -n '"aos8"' "hpe-networking-mcp/tests/unit/test_skill_tool_references.py"` shows "aos8" in the meta-tool loop.
3. Full unit suite passes: `cd hpe-networking-mcp && uv run pytest tests/unit/ -q`
</verification>

<success_criteria>
- Line 366 of aos-migration-readiness.md lists A13 as superseded alongside A4/A7/A8/A9.
- The meta-tool loop in test_skill_tool_references.py includes "aos8".
- All unit tests pass with no regressions.
</success_criteria>

<output>
After completion, create `.planning/quick/260429-ta0-fix-enrich-02-a13-supersession-omission-/260429-ta0-SUMMARY.md`
</output>
